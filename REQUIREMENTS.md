# 요구사항과 앱별 진행표

**왜 이 문서가 있나.** 화면이 넷이다 — 보드·아이폰·워치·데스크탑(아이패드 겸용).
같은 기능을 넷에 따로 만들다 보니 **서로 모르게 어긋났다.**

2026-09-10 에 대조해 보니 이랬다.

    아이폰·워치   값을 잘 받는데  명령을 못 보낸다
    데스크탑      명령을 잘 보내는데  값을 못 받는다

둘 다 반쪽인데 **반대쪽으로** 반쪽이었다. 아무도 몰랐다.

그래서 요구사항에 번호를 붙이고, 넷 각각이 어디까지 왔는지 한 표에 적는다.
**커밋 메시지에 번호를 적으면** 나중에 무엇이 왜 바뀌었는지 따라갈 수 있다.

---

## 읽는 법

    ✅  된다. 실기기로 확인함
    🔶  일부만 된다
    ❌  없다
    —   그 화면에는 해당 없음 (일부러 안 만든 것)

★ **`—` 와 `❌` 를 구분해서 쓴다.** 안 만들기로 정한 것과 빠뜨린 것은 다르다.

---

## A. 값 받기 — 배에서 지금 무슨 일이 일어나나

| 번호 | 요구사항 | 보드 | 아이폰 | 워치 | 데스크탑 |
|---|---|---|---|---|---|
| A1 | BLE 광고를 찾아 붙는다 | ✅ | ✅ | ✅ | ✅ |
| A2 | 텔레메트리 39바이트를 푼다 | ✅ | ✅ | ✅ | ❌ |
| A3 | 속도·침로·힐을 보여준다 | ✅ | ✅ | ✅ | ❌ |
| A4 | 9축 원본을 보여준다 | ✅ | ✅ | ✅ | ❌ |
| A5 | 위성 수·HDOP·배터리 | ✅ | ✅ | ✅ | ❌ |
| A6 | **값이 없으면 없다고 보여준다** | ✅ | ✅ | ✅ | — |
| A7 | 여러 배 중에 골라 붙는다 | — | ✅ | ✅ | 🔶 |
| A8 | 신호 끊김을 알려준다 | — | ✅ | ✅ | ✅ |

**09-16 실외 세션 124 후 사용자 결정 (A3·A6): 품질 검사에서 거절된 SOG·COG는 쓸 수 없는 값이다.**
NAV-PV 속도 표식이 방금 잰 값이고 칩이 밝힌 속도 오차가 1 kn 이하일 때만 OLED·BLE에 숫자를 보낸다.
그 외에는 `---`를 보이되 HLG에는 다듬지 않은 원본을 계속 저장한다. HDOP 고정 컷은 쓰지 않는다. 운동 가속도에서도 HDG 계산값을 OLED·BLE·TXT·HLG에 동일하게 제공하고
OLED에는 `?`를 붙인다. 새 HLG의 방위 정책 번호는 3, 옛 번호 2의 거절 규칙은 그대로 해석한다.
코드·빌드·호스트 시험 완료, 보드 시험은 firmware-idf/CHECKLIST.md의 결과를 따른다. 실제 센서 입력이 없으면 `---`다.

★ **A2 가 데스크탑에서 빠져 있다.** `desktop/src/protocol.ts` 가 17줄이고
UUID 세 개뿐이다. 파서가 아예 없다. 데스크탑은 지금 **기록 분석 전용**이다.

---

## B. 명령 보내기 — 배 위에서 보드를 만진다

| 번호 | 요구사항 | 보드 | 아이폰 | 워치 | 데스크탑 |
|---|---|---|---|---|---|
| B1 | 제어 특성에 붙어 글자 명령을 보낸다 | ✅ | ✅ | ✅ | ✅ |
| B2 | 보드의 답을 받아 보여준다 | ✅ | ✅ | ✅ | ✅ |
| B3 | WiFi 켜기·이름·비밀번호 | ✅ | ❌ | ❌ | ✅ |
| B4 | **`magcal` 자력계 치우침 보정** | ✅ | ✅ | ✅ | 🔶 |
| B5 | 기록 시작·종료 | ✅ | ❌ | ❌ | ❌ |
| B6 | 배 번호·이름·notify 주기 | ✅ | ❌ | ❌ | ❌ |

★ B1·B2·B4 는 2026-09-10 에 넣었다. 아이폰과 워치가 같은 `BLEManager` 를 쓰므로
배선은 한 번만 고쳤다. 화면은 각각 만들었다 — 아이폰은 설정에 한 절,
워치는 3페이지에 단추 둘.

**09-15 워치 보정 저장 거절 보고 (B2·B4·D3):** 128점 수집 후 `안 씀/다시`, 항해 기록 중 아님.
128은 수집 한도이며 합격 표시는 아니다. 저장 시 분포·반지름·잔차 검사에서 거절하는 경로로 좁혔으나,
실제 거절 문구·원본 점을 확보하지 못해 어느 조건인지는 미확정이다.
워치·아이폰은 거절 결과를 진행과 분리해 유지하고, 가득 찼을 때 `다시 모으기`(`magcal reset`)를 제공한다.
수평 회전만 하라는 안내를 장치의 여러 자세 수집으로 고쳤다. 기록 중 거절 이유도 보드가 BLE 응답으로 돌려준다.
보정 식·수락 기준·기존 보정값은 그대로. 호스트 검증·앱 빌드 완료, 실기기 재시험은 미실시다.

★ 제어 특성이 없는 옛 펌웨어에도 **연결은 그대로 된다.** 없으면 단추만
잠그고 계기는 정상으로 돈다.

★ B4 의 데스크탑 🔶 은 "통로는 있는데 화면이 없다" 는 뜻이다.
`Link.say()` 로 아무 명령이나 보낼 수 있지만 단추가 없다.

---

## C. 기록 — SD 카드

| 번호 | 요구사항 | 보드 | 아이폰 | 워치 | 데스크탑 |
|---|---|---|---|---|---|
| C1 | 항법 1 Hz + 9축 100 Hz 저장 | ✅ | — | — | — |
| C2 | **다듬기 전 원본만 저장** | ✅ | — | — | — |
| C3 | 버튼으로 시작·종료·마킹 | ✅ | — | — | — |
| C4 | 전원이 끊기면 다음에 이어서 | ✅ | — | — | — |
| C5 | WiFi 로 파일 내보내기 | ✅ | ❌ | — | ✅ |
| C6 | 파일을 읽어 항적을 지도에 | — | ❌ | — | ✅ |
| C7 | 타임라인·영상 맞추기 | — | — | — | ✅ |
| C8 | **쓰기 실패 시 다시 쓰고, 멈추면 이유를 남기고 새 파일로 다시 건다** | ✅ | — | — | — |
| C9 | **기록이 저절로 멈추면 알린다** (보드 REC FAIL · 앱 배경 빨강, flags bit5) | ✅ | 🔶 | 🔶 | ❌ |
| C10 | **지도가 인터넷 없이도 보인다** (데스크탑·아이패드) | — | — | — | 🔶 |

★ C10 은 2026-09-15. 지도 아래 "영역 받기" 로 지금 보이는 영역을 사람이 고른 확대(10~15)까지 받는다 — 크기를 먼저 보여준다.
원본은 Source Cooperative 의 Protomaps v4 (OSM 2026-03-31, ODbL). Rust `offmap.rs` 가 목차만 읽어 필요한 타일을 묶어 받고 `<앱데이터>/maps/*.pmtiles` 로 둔다.
바탕 "오프라인 지도" 는 `offmap://` 로 그 파일에서 타일을 꺼내고, 스타일(@protomaps/basemaps, 한국어)·글꼴·아이콘은 앱 안에 있다.
확인: 크기 계산이 pmtiles CLI 와 세 영역에서 같음 · 무작위 타일 20개 내용 같음 · 맥 개발 앱에서 받기→알림 9번→목록→지우기, 받은 영역을 그림(983개, 오류 0, 한국어 이름표).
아직: 인터넷을 실제로 끊고 본 적 없음 · 묶은 앱(tauri://) 에서 글꼴 주소 · 아이패드 · 받기 중 취소·재시도.
해도(OpenSeaMap)·위성(Esri)은 미리 받기를 허락하지 않아 오프라인이 안 된다.

| C11 | **세션 관리 — 내보내기·지우기** (데스크탑·아이패드) | — | — | — | 🔶 |

★ C11 은 2026-09-15. 세션 정보 칸의 "내보내기" 는 zip 하나 (원본 HLG·TXT · nav/imu CSV · GPX · 코치 메모 JSON · README).
"세션 지우기" 는 두 번 눌러야 하고 앱 안의 HLG·TXT·메모까지 지운다 (보드 카드는 안 건드림). 받은 지도는 영역 받기 칸에서 지운다.
확인: 세션 46 zip — HLG SHA-256·TXT 원본과 같음, CSV 줄 수 = NAV 104,757 · IMU 1,046,711, 위도 0 없음, GPX 104,757점 · 지우기 — 시험 세션의 파일·목록 사라짐, 다른 세션 그대로.
CSV `utc` 는 계산값(보드 시계 + 10분 구간 기준을 직선으로 이음), `gps_utc_row` 는 원본. 세션 46: 뒤로 감 0, 원본보다 −8 ~ +869 ms.
아직: 저장 창·단추를 사람이 누르는 길 · 아이패드 저장 창 · 앱에서 zip 을 utc 고친 뒤 다시 만들어 보기.

C8·C9 는 2026-09-13. 보드는 `rec fail` 시험으로 확인했다. 앱은 코드만 넣었고 화면으로 아직 못 봤다 (🔶).
데스크탑은 39바이트 확장 패킷을 아직 안 푼다 (A2) — 그래서 ❌.

---

## D. 자세와 방위

| 번호 | 요구사항 | 보드 | 아이폰 | 워치 | 데스크탑 |
|---|---|---|---|---|---|
| D1 | 힐·피치 0점 잡기 (`level`) | ✅ | ❌ | ❌ | ❌ |
| D2 | **기울기 보정 방위** (보드·앱 같은 식, HLG 머리글에 설정) | ✅ | 🔶 | 🔶 | 🔶 |
| D3 | **자력계 치우침 빼기** | ✅ | — | — | — |
| D4 | 자기 편각 (약 8° 서편) | 🔶 | — | — | — |
| D5 | 박스가 돌아앉은 각 (COG 로 상수 하나) | ❌ | ❌ | ❌ | ❌ |
| D6 | leeway 를 값으로 내보낸다 | ❌ | ❌ | ❌ | ❌ |
| D7 | **보드가 보여준 방위를 기록에 남기고 앱이 그대로 보여준다** (HLG v1.2 줄마다 · 옛 세션은 TXT) | ✅ | — | — | 🔶 |

★ D7 은 2026-09-15. 옛 HLG 에는 방위도 설정도 없어 앱이 같은 공식을 갖고도 못 그렸다.
보드는 NAV 줄을 40바이트로 늘려 그 순간 방위를 넣는다 — 세션 84 로 확인 (64줄, 앱 재계산과 차이 중앙 0.08° 최대 0.38°).
데스크탑·아이패드는 "보드 기록 HDG" 를 그리고, 옛 세션은 TXT 를 붙여 10초 간격 보드 값과 "추정 설정으로 재계산" 을 보여준다 (세션 46: +Y +Z 155.72°, 보드 값과 차이 중앙 0.47°, 구간 여섯 곳 0.41~0.52°). 파일 읽기는 확인, 화면은 아직 (🔶).
아이폰·워치는 BLE 로 보드 값을 실시간으로 받을 뿐 기록 파일을 안 읽는다 — 그래서 `—`.

★ D4 는 2026-09-14 에 `hdg decl` 설정 칸만 생겼다. 기본 0 이고 위치별 값을 저절로 구하지 않는다 — 🔶.

★ **D2 는 2026-09-15 부터 화면·BLE·TXT 가 쓴다.** 세션 46 물 위 자료로 대조했다. 당시 설정(+Y, +Z, 155.7°)으로 2.5 kn 넘는 47,656줄의 HDG−COG 원형 흩어짐은 평평 식 13.2°, 기울기 보정 9.0° 였다.
  (예전에 여기 적었던 "평평 6.8° → 보정 4.7°" 는 틀린 인용이었다. 4.7° 는 옛 앱 comp 식(뱃머리를 보드 X 로 가정)의 값이고, 계산법도 다르다.)
식은 `firmware-rak/include/heading_tilt.h` 하나, 데스크탑·아이패드는 `desktop/src/heading.ts` 사본을 `verify.sh` 가 대조한다.
아이폰·워치는 BLE 로 받은 보드 값을 보여주므로 같은 값이다 — 기기 화면으로는 아직 못 봤다 (🔶).
데스크탑은 머리글에 설정이 있는 새 파일만 그린다 — 화면으로 아직 못 봤다 (🔶).

★ **순서가 있다** (`heading-fix-order-matters`). D5 를 D3 보다 먼저 하면
남은 치우침이 뱃머리 방향마다 다른 오차를 만들고 그게 leeway 인 척 섞인다.

    D2 기울기 → D3 치우침 → D4 편각 → D5 상수 하나 → D6 leeway

---

## E. 전원

| 번호 | 요구사항 | 보드 | 아이폰 | 워치 | 데스크탑 |
|---|---|---|---|---|---|
| E1 | 버튼 5초로 끄고 켜기 | ✅ | — | — | — |
| E2 | 깊은잠에서 헛깨지 않는다 | ✅ | — | — | — |
| E3 | 끄는 순서 (기록→SD→무전기→IMU→화면) | ✅ | — | — | — |
| E4 | 화면에 진행 막대 | ✅ | — | — | — |
| E5 | **잠자기 전류를 숫자로 안다** | ❌ | — | — | — |

★ E5 는 **멀티미터가 있어야 한다.** 전압으로 되짚는 방법을 두 번 시도해서
두 번 다 무효였다. 깬 직후 ADC 값이 못 믿을 값이라서다 (`POWER.md` 맨 위).

---

## F. 여러 배 (로라)

| 번호 | 요구사항 | 보드 | 아이폰 | 워치 | 데스크탑 |
|---|---|---|---|---|---|
| F1 | SX1262 설정과 칩 버그 셋 | ✅ | — | — | — |
| F2 | 배 번호 붙이기 | ✅ | ❌ | ✅ | ❌ |
| F3 | 차례 맞추기 (PPS) · PPS 전 생존 확인 | 🔶 | — | ✅ | ✅ |
| F4 | 남의 배를 실시간으로 받는다 | 🔶 | ❌ | ❌ | 🔶 |

★ F3 은 HDG M을 포함한 24바이트 패킷과 PPS 32슬롯 송신 일꾼까지 구현했다.
현재판 호스트 시험·IDF 빌드·두 보드 재플래시는 진행 중이다. 기존 22바이트판은
끝났다. 부팅 기본은 SX1262 sleep이며 워치나 시리얼에서 장거리 통신을 켜면
같은 모듈이 자기 슬롯 송신과 나머지 슬롯 수신을 함께 한다.
**밖에서 PPS를 본 시험과 보드 두 대 송수신은 아직 없다.** 그래서 🔶다.
PPS 전에는 5~8초 무작위 간격의 확인 신호만 보내며 위치·SOG·COG만 비우고 HDG·자세·REC·배터리는 보낸다.
워치와 함대 앱은 이를 정상 위치와 구분해 `GPS 시각 대기`로 표시한다.
F4 보드는 최근 3초의 32척 상태·RSSI·SNR·번호 충돌을 보관하고 `lora peers`로
보여준다. 수신한 24바이트와 수신 PPS 프레임·RSSI·SNR·BLE 알림 순번을 Fleet 특성으로
아이패드·맥에 전달하는 코드도 연결했다. 앱의 `함대 라이브`는 0척=전체 지도,
1척=상세, 2척=A/B 비교이며 세 번째 선택은 B를 바꾼다. 거리·방위·SOG/COG 차이·
접근/이탈 속도를 표시하고 서로 다른 수신 프레임은 경고한다. 브라우저 시험 자료로
화면과 디코더 빌드는 확인했다. PPS 전 생존 신호는 실물 두 대에서 수신 보드→BLE→맥까지
통과했지만 실외 PPS 위치 송수신 전이라 보드·데스크탑 모두 🔶다. 앱은 BLE·배별 프레임
누락률, 같은 배 번호 충돌, 다른 배의 `heard`로만 확인된 배를 숨기지 않고 표시한다.
지도 배 표식에는 SOG·HDG M을 붙인다. 배를 고르면 화면 맨 아래 전체 폭에 최근 5분
SOG·HDG·COG·HEEL·PITCH·RSSI 실시간 그래프를 각각 한 줄로 연다. 빈 SOG·COG는
GPS 시각/fix/품질 중 이유를 표시한다. 목록의 배별 `궤적 녹화`를 켜면 그 뒤 실제
위치만 지도에 남기고 수신 공백을 주황 점선으로 구분한다. Fleet BLE 콜백은 자료만
받고 화면은 최대 4 Hz로 묶어 갱신하며, 전달률 표시는 `보드→앱 BLE`라고 통로를 밝힌다.
무선 경로 시험용 `lora rate 5`는 송신기 한 대에서 60초만 동작한 뒤 1 Hz로 돌아간다.

---

## 다음에 할 일 — 번호로

지금 제일 아픈 곳 셋.

| 순위 | 번호 | 무엇 | 왜 |
|---|---|---|---|
| ~~1~~ | ~~B1·B2·B4~~ | ~~아이폰·워치 제어 통로~~ | **2026-09-10 완료** |
| 1 | **D4·D5·D6** | 편각 → 상수 → leeway | 이 배를 만든 이유가 leeway 다 |
| 2 | **A2** | 데스크탑 텔레메트리 파서 | 아이패드로 라이브를 보려면 |
| 3 | **B5·B6** | 기록 시작·배 번호를 앱에서 | 통로가 생겼으니 얹기만 하면 된다 |

**E5(잠자기 전류)는 멀티미터가 생기기 전까지 멈춰 둔다.** 코드로 더 할 게 없다.

---

## 이 문서를 쓰는 법

**기능을 하나 만들 때마다 여기 한 줄을 고친다.** 그리고 커밋 메시지 첫 줄
뒤에 번호를 적는다.

    magcal — 자력계 치우침을 뺀다  (D3, B4)

넷 중 하나만 고쳤으면 **나머지 셋의 칸을 ❌ 로 남겨 둔다.** 그게 오늘 같은
어긋남을 막는 유일한 방법이다. 다 됐다고 적고 싶은 마음을 참아야 한다.

★ **`—` 를 함부로 쓰지 않는다.** "이 화면에는 필요 없다" 는 판단이고,
그렇게 정한 이유를 옆에 적어야 한다. 귀찮아서 `—` 를 쓰면 빠진 것이 숨는다.
