# 내일 할 일

마지막 작업: 2026-08-27. **보드가 죽던 것을 고쳤고, 이제 WiFi 를 켠 채로
블루투스도 살아 있다.** 여러 기기가 한 배에 붙을 때의 규칙도 만들었다.

지금 보드는 WiFi 꺼짐 · BLE 광고 중이고 USB 로 맥에 꽂혀 있다.
맥 앱은 완성본을 구워 띄워 뒀고, 아이패드는 화면 서버로 붙어 있다.

---

## 오늘 한 것 (2026-08-27)

### 1. 받기를 누르면 보드가 죽었다

시리얼 로그가 말해 줬다.

```
Guru Meditation Error: Core 1 panic'ed
Stack canary watchpoint triggered (loopTask)
```

죽은 자리는 `netsrv.cpp` 의 `SD.open()` 이었다. 같은 함수가 스택에 4KB
버퍼를 들고 있었는데, 컴파일러는 그 자리를 함수에 들어서는 순간 잡는다.
그래서 버퍼를 쓰기도 전에 넘쳤다.

죽을 때 찍힌 주소로 자리를 세어 봤다.

```
loop()          2032 바이트
handleFile      4448 바이트   ← 4KB 버퍼
SD.open 이하    1952 바이트
합계            8432 바이트     스택은 8192 바이트     240 바이트 넘침
```

**한동안 멀쩡했던 이유**도 찾았다. 예전 판(a66ce0a)을 실제로 구워서
objdump 로 견줬다. `loop()` 이 320 → 2032 바이트로 커지면서 여유
1472 바이트를 다 먹었다. 버퍼는 처음부터 그대로였다.

고침은 `static uint8_t buf[4096]`. 교훈은 **스택에 KB 단위를 올리지 않는
것**이다. 남은 자리는 내 함수가 아니라 부르는 쪽이 정한다.

### 2. 메모리가 새고 있었다 (BLE 가 아니라 WiFi 쪽)

WiFi 를 한 번 껐다 켤 때마다 843 바이트가 안 돌아왔다.

원인 둘. `WiFi.begin` 할 때마다 웹서버에 주소 8개를 다시 등록했는데
`WebServer::stop()` 은 그 목록을 안 지운다 (지우는 건 소멸자뿐인데 전역
변수라 안 불린다). mDNS 도 끌 때 정리를 안 했다.

```
고치기 전   843 바이트/회
고친 뒤     20번 껐다 켜서 222,044 → 221,748.  줄어드는 흐름 없음
```

BLE 만 껐다 켜는 것은 8번 돌려도 1바이트도 안 줄었다. 남 탓하기 전에
갈라서 재야 했다.

### 3. WiFi 를 켜도 블루투스가 살아 있다

예전에는 WiFi 를 켜면 BLE 를 내렸다. 그러면 아이패드가 붙어 있는 동안
데스크탑이 그 배를 아예 못 찾는다.

칩이 죽는 조건은 "BLE 켠 채 **절전을 끄는 것**" 하나다. 절전을 켜면 둘이
같이 돈다. 다만 파일 속도가 209 → 58 KB/초로 떨어진다.

그래서 이렇게 나눴다.

```
평소            BLE 켬 + 절전 켬     어느 앱이든 배 찾기로 보인다
파일 보낼 때     BLE 내림 + 절전 끔    2 ms 걸린다
다 보내고 5초 뒤  BLE 다시 켬
```

DJI 와 GoPro 도 평소에 둘을 같이 켜 둔다. 우리는 안테나가 하나라 보내는
동안만 뺀다. 실측으로 2MB 가 286~340 KB/초, 예전 209 보다 빠르다.

### 4. 여러 기기가 한 배에 붙을 때

보드는 **한 번에 한 대만 상대한다.** 4MB 를 받는 동안 다른 기기의 상태
물어보기가 21.7초 기다렸다.

그래서 두 번째 앱은 **아예 안 붙인다.** 목록만 보이고 받기는 안 되는
어정쩡한 상태를 만들지 않는다.

```
앱 → 보드   wifi on ios-8c21          제 번호를 같이 보낸다
보드 → 앱   … users 1 by macos-3f9a   나를 뺀 나머지 수
앱          "다른 기기가 쓰고 있습니다 (macos-3f9a)"  거기서 끝
```

빼는 일을 보드가 하므로 앱에서 틀릴 자리가 없다. 앱 번호는 앱이 처음 켤
때 스스로 만들고(`macos-3f9a`) 보드는 30초 안 오면 잊는다. 관리할 것이 없다.

**자물쇠가 안 풀리는 일은 없다.** 세 겹이다.

1. 자물쇠는 "붙어 있는 동안" 이 아니라 "실제로 보내는 동안" 만 잡는다
2. 받아 가던 기기가 사라지면 4KB 마다 보는 `connected()` 가 바로 푼다
3. 앞사람이 조용해지면 15초 뒤에 WiFi 가 꺼지면서 기억도 비워진다

받기는 보드도 막는다 (409). 앱이 규칙을 안 지켜도 막힌다.

### 5. 그밖에

- 칸마다 크게 보기 단추(⤢). 누르면 나머지가 접힌다
- 단추가 길쭉하던 것 — 44pt 는 **닿는 자리**이지 보이는 크기가 아니다.
  `--tap`(닿는 자리)과 `--ctl`(보이는 높이)을 갈랐다
- 배 찾기가 블루투스로 못 찾으면 지난번 주소를 물어본다
- 단추를 연달아 눌러도 일은 한 번만 한다
- 받기 제한 시간을 둘로 나눴다. 첫 바이트까지 120초, 그 뒤로는 20초
- `127 dBm` 은 세기가 아니라 "못 쟀다" 는 값이다. "세기 모름" 으로 바꿨다
- BLE 답 파서를 느슨하게 고쳤다. 줄 끝을 딱 맞추면 보드에 뭘 하나
  붙일 때마다 옛 앱이 깨진다 (실제로 깨졌다)
- 블루투스로 붙다 실패하면 다시 찾아서 한 번 더 해 본다. 보드가 WiFi 를
  껐다 켜면 블루투스가 새로 올라와서, 그 전에 찾아 둔 목록으로는 못 붙는다

### 6. 붙는 데 오래 걸리던 것 — 보드가 아니라 앱이 느렸다

보드가 하는 일을 재보니 이랬다.

```
0.08초  블루투스로 답이 옴
0.69초  공유기에 붙음
1.78초  주소가 200 으로 대답
```

느린 쪽은 앱이었다. 두 군데다.

**하나.** 주소를 두드리는 제한 시간이 한 번에 6초였다. 아직 안 뜬 보드에
처음 물어보고는 그 6초를 통째로 서 있었다. 이제 1.2초씩 짧게, 0.25초마다
다시 물어본다. 전체 제한(25초)은 그대로다.

**둘.** 배 찾기가 늘 6.2초를 채웠다. 부두에서는 배가 한 대인 경우가 많다.
하나라도 찾고 2초가 지나면 끊는다. 아무것도 못 찾으면 예전대로 6초를 쓴다.

**교훈은 하나다. 느리다고 느껴지면 보드부터 의심하지 말고 걸음마다 잰다.**
처음 재본 값이 10.5초로 나왔는데, 그건 내 시험 코드가 6초 제한을 걸어서
나온 값이었다. 앱도 똑같은 실수를 하고 있었다.

### 7. 세로 영상 — 영상을 왼쪽 전체로

요즘 영상은 세로가 많다 (9:16). 세로 영상은 높이가 밑천인데 기본 배치는
영상을 납작한 칸에 넣는다.

```
전   영상칸 611x257  →  영상 145x257    칸의 76%가 검은 띠
후   영상칸 340x604  →  영상 340x604    띠가 없다. 넓이 5.5배
     지도 882x326 · 데이터 882x342      안 좁아졌다
```

```
보통                        세로 영상
┌───────┬───────┐          ┌─────┬───────────┐
│ 영상  │ 지도  │          │     │   지도    │
├───────┴───────┤          │영상 ├───────────┤
│    데이터     │          │     │  데이터   │
└───────────────┘          └─────┴───────────┘
```

영상을 영상·지도 줄에서 빼내 가운데 첫 칸으로, 데이터를 그 줄 안으로
옮긴다. 그 줄이 오른쪽 세로줄이 된다. 옮기면 통마다 담긴 칸이 달라지므로
`panes.forget()` 으로 등록을 지우고 다시 한다.

영상을 열 때 `videoHeight > videoWidth` 를 보고 알아서 바꾼다. 가로 영상을
열면 되돌린다. 사람이 설정에서 위아래/좌우를 누르면 그 뒤로는 안 건드린다.

영상 칸 너비는 영상 비율에 딱 맞춘다. 사람이 나누개를 끌었으면 안 건드린다.

**잡은 것 하나.** 처음엔 영상 칸이 160px 로 쪼그라들었다. panes 가 칸 크기를
이름으로 기억하는데 보통 배치에서 영상은 "몫 1.0" 이라, 새 배치에서 그
1.0 을 1px 로 읽었다. 등록 뒤에 폭을 다시 박아 고쳤다.

### 8. 영상만 돌리는 단추

영상 칸에 슬라이더는 원래 있었다. 재생 단추만 없었다. 재생·슬라이더·시각을
**한 줄에** 뒀다. 두 줄이던 것을 합쳐서 영상 자리가 26px 늘었다.

두 단추가 하는 일이 다르다.

```
              영상 칸의 ▶        아래 시간 막대의 ▶
싱크 전        영상만 돈다        데이터만 돈다
싱크 후        둘이 같이 돈다     둘이 같이 돈다
```

**멈추는 데 여러 번 눌러야 했다.** 앱이 "돌고 있다" 를 두 군데에 따로
들고 있어서다 — `playing` 과 `!video.paused`. 영상만 돌리는 중에 [싱크] 를
누르면 영상은 도는데 `playing` 은 꺼진 채였고, 멈추려고 누르면 코드가
깃발만 보고 오히려 다시 틀었다.

고침은 둘이다. 싱크할 때 영상이 돌면 깃발도 켠다 (어긋남을 만드는 자리를
없앰). 그리고 단추는 `running()` 으로 실제 상태도 같이 본다.

### 9. 누르는 자리 훑기 — 오늘 만든 실수를 찾아냈다

"영상 재생 단추가 잘 안 눌린다" 는 말에서 시작했다. 재보니 29x26 이었다.

**원인이 오늘 아침 내가 한 일이었다.** 아이패드 단추가 길쭉하다는 말을
듣고 `--tap`(닿는 자리)과 `--ctl`(보이는 높이)을 갈랐는데, 넓히는 규칙을
`@media (pointer: coarse)` 안에만 넣었다. 그래서 맥은 28 → 26 으로 **작아진
채 보완이 없었다.** 아이패드만 재보고 맥은 안 재봤다.

그래서 화면에 있는 누르는 것을 전부 훑었다. 네 군데가 더 나왔다.

```
input · select   맥 26 패드 34    ← 같은 실수. ::after 를 못 붙이는 물건인데
                                    --ctl 로 바꿔 놨다. --tap 으로 되돌림
해도 · 속도 색칠   38x19           .chk 를 안 붙여 뒀다
지도 확대·축소     29x29           maplibre 것. 단추 자체를 키웠다
칸 손잡이 ⤢ ✕     26x26           서로 2px 옆이라 안 넓혀 뒀다. 12px 로 벌림
```

**훑는 법.** 화면의 모든 단추·체크박스·입력칸에 대해 가운데에서 사방으로
`elementFromPoint` 를 한 픽셀씩 밀어 보며 어디까지 그 물건으로 잡히는지
센다. 그러면 "보이는 크기" 가 아니라 **실제로 눌리는 자리**가 나온다.
토큰 값을 아이패드 것으로 바꿔 한 번 더 돌리면 두 기기를 같이 본다.

각자 제 가운데를 스스로 잡는지도 같이 본다. 닿는 자리가 서로 겹치면
거기서 걸린다.

지금은 맥 34 미만도, 패드 44 미만도 없다.

### 실기기에서 눈으로 본 것 (2026-08-27 밤)

```
세로 영상                아이패드에서 열어 배치가 바뀌는 것을 봤다
아이패드·맥 동시 붙기      두 번째 앱이 거절당하는 것을 봤다
블루투스로 보드 찾기       하루 종일 이 길로 붙었다
손가락으로 끌기            그래프와 서랍이 손가락에 따라온다
```

### 아직 못 본 것

```
AP 모드에서 BLE 같이 켜기   맥 WiFi 를 보드 AP 로 옮겨야 한다. 그 방식은
                          사용자 네트워크를 두 번 끊어서 다시 안 쓴다.
                          배에서 볼 것
안드로이드                 tauri android init 도 아직 안 했다
```

### 오늘 헤맨 자리 — 다시 하지 말 것

**맥 앱을 네 시간 동안 못 올리고 있었다.** `killall "Sail Analyzer"` 가
헛돌았다. 실행 파일 이름이 `desktop` 이었기 때문이다. `open` 은 이미 떠
있으면 그 창을 앞으로 가져올 뿐이라 옛 앱을 계속 보게 된다. 코드를 여러 번
고치고 구웠는데 화면은 그대로였고, 원인을 코드에서 찾고 있었다.

**이름을 고쳤다.** `tauri.conf.json` 에 `mainBinaryName` 을 넣었다.
`npm create tauri-app` 을 `desktop` 폴더에서 돌려서 러스트 꾸러미 이름이
`desktop` 이 됐던 것이다. 아이패드는 원래부터 `Sail Analyzer` 였고
(Xcode 가 제 이름을 쓴다) 이름을 바꿔도 빌드·설치·실행 다 통과했다.

그래도 띄운 뒤에는 시작 시각을 찍어서 방금 것인지 확인한다.

```bash
pkill -f "Sail Analyzer.app/Contents/MacOS/"
open "…/Sail Analyzer.app"
ps -o lstart= -p $(pgrep -f "Sail Analyzer.app/Contents/MacOS/" | head -1)
```

---

## 데스크탑 앱 — 지금 이런 모양이다

```
데이터 열기  영상 열기  ＋마킹 │ 0:00 ~ 5:59
┌──────────────────────────────────┬─────────┬──┐
│      영상        │     지도      │  서랍    │◈│보드
│──────────────────┴──────────────│         │▤│세션
│                                  │         │⚑│마킹
│  데이터 타임라인 (가로 전체)      │         │⚙│설정
│                                  │         │ │
├──────────────────────────────────┴─────────┴──┤
│ ▶  0:36 / 5:59  ─────●────  │ 싱크  −10초 …  │  ← 시간 막대
├───────────────────────────────────────────────┤
│ 📍 0:36  ■ SOG 5.14kn      sample.mp4 …       │
└───────────────────────────────────────────────┘
```

- 칸 사이마다 나누개. 두 번 누르면 반반
- 칸 오른쪽 위 ✕ 로 접기, 남는 띠를 누르면 펴기
- 센서 이름 앞 ▾ 로 그 줄만 접기. 하나만 펴 두면 화면을 다 쓴다
- 이름 칸 · 숫자 칸 경계를 잡아 끌면 넓어진다
- 설정에 색 판 넷 (잉크·심해·종이·전기) 과 "처음 배치로 되돌리기"

### 시간 막대 — 세션의 시계다

영상·지도·그래프가 다 같은 시간을 본다. 그래서 재생은 영상 칸이 아니라
아래 시간 막대에 있다. 영상 칸을 접어도 사라지지 않는다.

```
영상 없음         세션 시간이 흐른다 (앱이 시계를 갖는다)
영상 물림          세션 시간이 흐르고 영상도 같이
영상 안 물림        세션 시간이 흐르고 영상은 가만히 있다
```

마지막 줄이 핵심이다. 아직 안 붙은 영상이 따라 움직이면 맞는 것처럼 보인다.
가만히 있어야 안 붙었다는 게 눈에 보인다. 맞출 때는 영상 칸의 제 슬라이더로
영상을 옮기고 [싱크] 를 누른다.

창이 가려지면 재생이 선다. 화면 갱신이 멈춰 우리 시계도 멈추는데 영상은
안 멈춰서 어긋나기 때문이다.

### 확인 방법 — 세 곳 다 한계가 다르다

| 자리 | 안 되는 것 |
|---|---|
| 숨은 크롬 탭 | 화면 갱신이 1초에 0번. 시간 흐르는 것·영상·지도를 못 본다 |
| 아이패드 시뮬레이터 | 눌러보기·끌기가 **아예 안 닿는다** (0.0%) |
| 맥 완성본 | 창 안을 코드로 들여다볼 수 없다 |

시뮬레이터는 앱이 스스로 하게 해두고 찍는다.

```ts
if (import.meta.env.DEV) setTimeout(() => playAll(), 2500);   // 확인 뒤 지운다
```

브라우저에서 앱 코드를 재볼 때는 **반드시 `window.__tl`** 을 쓴다. 모듈을
새로 import 하면 상태가 빈 딴 사본이 와서 멀쩡한 코드를 고장 났다고 보게 된다.

맥 앱은 창 하나만 찍는다 (`screencapture -l <창번호>`). 화면 전체를 찍으면
사장님이 열어 둔 다른 창까지 다 들어온다.

---

## 배에서 확인할 것

앱을 띄우고 **보드 → [배 찾기] → [연결]**.

```
파일 목록까지 가면 성공.
```

시리얼을 같이 열어 두면 무슨 일이 벌어지는지 보인다.

| 보일 것 | 뜻 |
|---|---|
| `[CTL] ← wifi on` | 앱이 시켰다 |
| `[CTL] → ok wifi joining … last 192.168.0.76` | 보드가 찾아올 주소를 알려줬다 |
| `[BLE] 내렸습니다` | WiFi 를 켜느라 BLE 를 껐다. 정상이다 |
| `[NET] 붙었습니다 — http://…` | 준비됐다 |

**아직 못 본 것 둘.**

- 앱을 닫으면 15초 뒤 `[NET] 앱에서 15초 동안 연락이 없어 WiFi 를 끕니다`
  가 떠야 한다. 브라우저로는 확인했고 앱으로는 못 봤다.
- AP 모드에서 노트북이 떠나면 8초 뒤 `쓰던 기기가 사라져서` 가 떠야 한다.
  확인하려면 맥 WiFi 를 보드 AP 로 옮겼다 빼야 하는데, 그러다 사용자
  네트워크를 두 번 끊었다. **그 방식은 다시 쓰지 않는다.** 배에서 볼 것.

---

## 0. 배에서 먼저 — 힐·피치·방위 0점 잡기

힐과 피치는 가속도 한 축에서 바로 구한다. 방위는 자력계의 수평 두 축으로
만든다. 어느 축인지는 보드를 어떻게 다느냐에 달려 있고, 셋 다 시리얼에서
바꿔서 NVS 에 남긴다. **케이스를 바꿔 달아도 다시 구울 일이 없다.**

### 지금 걸려 있는 값 (2026-08-27, 케이스에 넣으며 90도 돌린 뒤)

| | 축 | 그때 값 |
|---|---|---|
| 힐 | 가속 **+X** | +0.8° (평평하게 두고) |
| 피치 | 가속 **+Z** | -1.7° |
| 방위 | 자력 **atan2(+Y, +Z)** · 0점 **+155.7°** | 143.2° (아이폰 145°) |
| 위아래 | 가속 **Y** · 자력 **X** | ±1 g |

**가속도계와 자력계의 축이 서로 다르다.** 가속도로는 Y 가 위아래인데 자력
으로는 X 가 위아래다. 라이브러리가 주는 값을 그대로 쓰기 때문이다
(AK8963 은 MPU 와 축이 어긋나 있다). 그래서 `hdg` 는 힐·피치와 **따로**
고르게 만들었다.

### 어느 축인지 찾는 법 — 재서 고른다

```
위아래 축      평평하게 두고 imu. 중력이 실린 축이 위아래다 (±1 g)
자력 위아래 축  평평하게 두고 제자리에서 한 바퀴. 폭이 제일 작은 축이다
              실측: X 18 µT · Y 91 · Z 93  →  X 가 위아래
```

### 아직 못 정한 것 둘

```
방위 도는 방향   한 점만 맞췄다. 동쪽으로 돌려 우리 값이 커지면 맞고,
                작아지면 hdg -y z 로 뒤집는다
힐 부호         우현으로 기울여 양수여야 한다. 반대면 heel -x
```

책상 위 값이라 맥과 USB 케이블의 자기장이 섞여 있다. **배에 달고
`hdg off` 를 한 번 더 맞춘다.** 아이폰 나침반은 진북, 우리는 자북이다
(한국 약 8도 서편). 그 차이도 이 0점이 함께 먹는다.

기준각(level)은 아직 0 이다. 물에 띄우고 평형일 때 잡는다.

배를 평형으로 두고 이 순서로 한다.

```
heel        지금 축과 가속도 세 축 값을 본다 (pitch 도 같은 내용)
level       지금 자세를 힐 0°, 피치 0° 로 삼는다
```

**맞는 축인지 보는 법:** 배가 평형일 때 그 축이 0 g 에 가까워야 한다.
다르면 축을 바꾼다. 다시 구울 필요 없다.

```
heel x      힐을 X 축에서 (기준각은 자동으로 0 이 된다)
heel -y     좌우가 뒤집혀 있으면 부호를 뒤집는다
pitch -z    피치가 앞뒤 반대면
hdg y z     방위를 자력 atan2(Y, Z) 로
hdg -y z    도는 방향이 반대면
hdg off 12  0점 보정 (자기 편각 + 보드 어긋남을 한꺼번에)
```

축을 바꾸면 기준각이 0 으로 돌아가므로 `level` 을 다시 쳐야 한다.

**부호도 확인한다.** 해양 장비 관례(NMEA 2000 PGN 127257)를 따른다.

| 움직임 | 나와야 할 값 | 반대면 |
|---|---|---|
| 우현으로 누움 | 힐 **양수** (앱에 STBD) | `heel y` 로 되돌리기 |
| 좌현으로 누움 | 힐 **음수** (앱에 PORT) | |
| 뱃머리 들림 | 피치 **양수** | `pitch -z` |

**둘 다 손으로 기울여서 이미 확인했다.** 피치는 그대로 맞았고 힐만 뒤집었다.
배에 달고 나서 한 번 더 보면 된다.

> 방위 자체는 아직 기울기 보정이 없다. 배가 기울면 방위가 틀어진다.
> 자기 편각(한국 약 8도 서편)도 아직 안 넣었다. 미뤄둔 것 항목 참고.

---

## 0-1. 상태 확인

시리얼에서 이 두 개만 쳐 보면 된다.

```
gpscfg      모듈에 실제로 걸린 설정
smooth      다듬기 세기
```

**이렇게 나와야 정상이다.**

```
통신 속도     115200 bps
측위 간격     100 ms (= 10.0 Hz)
움직임 종류   0  Portable 일반 휴대   — 우리가 고른 값
정지 문턱값   0.00 m/s   — 꺼져 있다
쓰는 위성     GPS BeiDou GLONASS
다듬기 세기   2단계 (시상수 0.6초)
잡음 바닥     0.10 kn
```

다르면 아래로 되돌린다.

```
gpscfg mode 0
smooth 2
dead 0.10
```

---

## 1. 차로 30노트 구간 확인 ← 제일 먼저

**왜:** 걷는 속도(0~3 kn)는 확인했다. 배는 0~30 kn 인데 **위쪽 절반을 아직
안 봤다.** 같은 칩에서 모드가 안 맞아 시속 40km 가 18 로 나온 사례가 있다
(Espruino #7025). 우리가 겪은 것과 같은 고장의 반대쪽이다.

**방법:** 보드만 들고 차로 나간다. 시속 50~60km 로 달린다. 이게 27~32 노트다.

- [ ] 출발 전 `fix` 를 쳐서 기록을 비운다 (보드 리셋으로 충분)
- [ ] 천천히 출발 — 화면 숫자가 바로 따라오나
- [ ] 시속 50~60km 로 정속 주행 — **차 계기판과 화면 숫자를 비교**
- [ ] 신호 대기 — 0 으로 떨어지나, 몇 초 걸리나
- [ ] 돌아와서 `fix` — `본 최고 속도 RMC` 가 실제 최고 속도와 맞나

**판정**

| 결과 | 뜻 | 할 일 |
|---|---|---|
| 계기판과 1노트 안쪽 | 휴대 모드로 확정 | 다음 항목으로 |
| 화면이 계속 낮게 나옴 | 휴대 모드도 위쪽을 누른다 | `gpscfg mode 3`(자동차)로 재시험 |
| 반응이 3초 넘게 늦음 | 다듬기가 센 것 | `smooth 1` 로 낮춰 재시험 |

---

## 2. 다듬기 단계 실물로 고르기

**왜:** 지금 기본값 2단계(0.6초)는 **내가 정한 숫자지 재본 값이 아니다.**
실제 요트 계기는 사용자가 고른다.

- [ ] 차 안에서 `smooth 0` (원본) — 숫자가 얼마나 떠는지 본다
- [ ] `smooth 2` — 읽을 만한가
- [ ] `smooth 4` — 반응이 답답한가
- [ ] 쓸 만한 단계를 정해서 그걸로 둔다 (보드에 저장된다)

---

## 3. 가속도계로 다듬기 세기 자동 전환 ← 여기가 본 작업

**왜:** 세게 다듬으면 숫자는 안정되지만 택 할 때 늦다. 논문(PMC10007219)이
쓴 방법은 **상태를 가려 세기를 바꾸는 것**이다. 속도 오차를 최대 80% 줄였다.

| 상태 | 다듬기 |
|---|---|
| 멈춤 | 갱신 안 함 |
| 속도 변하는 중 | 약하게 (반응 빠르게) |
| 일정 속도 | 세게 (숫자 안정) |

논문은 가속도계로 가른다. **우리도 9축이 이미 붙어 있다.**

- [ ] 배 앞뒤 방향 가속도를 뽑는다 (지금 힐 계산에 쓰는 축 말고 진행 방향)
- [ ] 변화 문턱을 정한다 — 논문은 3 km/h 상당을 썼다. 요트는 더 낮아야 할 듯
- [ ] `dampingUpdate()` 안에서 `tau` 를 두 값 사이로 바꾼다
      (`kDampTau[gDampLevel]` 과 그 한두 단계 아래)
- [ ] **IMU 가 죽어도 멈추면 안 된다.** `gImuOk` 가 거짓이면 지금처럼 고정 세기
- [ ] 시리얼에 지금 어느 상태인지 찍어서 눈으로 확인

건드릴 곳: `firmware-rak/src/main.cpp` 의 `dampingUpdate()` 근처.

---

## 4. 저속에서 COG 처리

**왜:** 배가 느리면 위치 오차가 방향 계산을 지배한다. 정박 중에 침로가 빙빙
돈다. 지금은 그대로 내보내고 있다.

- [ ] 속도가 잡음 바닥 아래면 COG 를 붙들거나 안 보여준다
- [ ] 자력계 방위(HDG)로 갈아탈지 정한다 — 화면에 이미 나란히 있다
- [ ] 앱에도 같은 규칙을 넣는다 (`app/Shared/Protocol.swift`)

---

## 5. 앱 설치 ← 코드는 끝났고 올리기만 하면 된다

메인 화면에서 COG 를 **HDG(뱃머리 방향)** 로 바꿨다. 요트 계기가 하는 방식이다.
멈춰 있거나 느릴 때 GPS 침로는 빙빙 돌지만 자력계 방위는 안 흔들린다.
COG 는 상세 화면(아이폰) / 2페이지(워치)에 HDG 와 나란히 있다.

자력계가 없는 보드(옛 Feather)에서는 **라벨이 저절로 COG 로 바뀐다.**
같은 라벨로 다른 값을 보여주면 읽는 사람이 속기 때문이다.

빌드는 통과했다. 어제는 아이폰·워치가 연결돼 있지 않아 설치를 못 했다.

- [ ] 아이폰 연결 후 설치

```bash
cd app
DD=$(mktemp -d)
xcodebuild -project SailingMonitor.xcodeproj -scheme SailingMonitor \
  -destination 'id=5D03F469-2C6D-5505-91BF-EB8473431364' \
  -configuration Debug -derivedDataPath "$DD" -allowProvisioningUpdates build
xcrun devicectl device install app \
  --device 5D03F469-2C6D-5505-91BF-EB8473431364 \
  "$DD/Build/Products/Debug-iphoneos/SailingMonitor.app"
```

- [ ] 워치는 **Xcode 에서만 된다.** 스킴 `SailingMonitor Watch App` + 기기 워치 → ⌘R
      (기기 목록에 워치가 안 보이면 스킴을 먼저 바꿀 것)
- [ ] 두 화면 다 HDG 로 나오는지 눈으로 확인

## 다음에 할 일 — 순서대로

**바로 다음**: 앱 창에서 [배 찾기] → [연결] 을 실제로 눌러 보기.
BLE 쪽은 눌러 봤고 USB 쪽은 아직이다. USB 를 보려면 시리얼에서
`wifi ap` 를 쳐서 BLE 를 내려 둬야 한다 (`wifi off` 로 되돌린다).

---

보드를 깨우는 길이 셋이다. 통로만 다르고 결국 같은 명령으로 모인다.

| 있는 것 | 켜는 법 | 무엇으로 켜나 | 찾는 법 |
|---|---|---|---|
| USB | 앱에서 [연결] | **`wifi ap`** | 192.168.4.1 |
| 아무것도 없음 | 보드 버튼 5초 | **`wifi ap`** | 192.168.4.1 |
| 블루투스 + 저장된 WiFi | 앱에서 [연결] | `wifi on` (붙기) | 답에 적힌 mDNS 이름 |
| 블루투스 + 저장된 WiFi 없음 | 앱에서 [연결] | **`wifi ap`** | 192.168.4.1 |

### ★ 왜 대부분 AP 인가

**WiFi 이름·비밀번호를 보드에 넣는 게 제일 어려운 일이다.** 그런데 AP 는
그게 아예 필요 없다. 보드가 스스로 만드니까.

그래서 **아무 준비 없이 시작되는 길이 늘 있다.** 이름·비밀번호 넣기는
"있으면 편한 것" 으로 내려간다 — 노트북이 인터넷을 안 잃는 정도의 값이다.

### 1. USB 로 꽂으면 AP 모드로 — ✅ 만들었다 (2026-08-25)

블루투스가 없으면 보드 WiFi 를 켤 방법이 아예 없었다. 보드는 BLE 와
시리얼이 같은 명령을 쓰니 통로만 갈아 끼웠다. 새 규격이 없다.

**보내는 명령은 `wifi ap` 다.** 이름·비밀번호가 필요 없는 유일한 길이다.

앱 흐름 — 배 찾기 한 단추로 둘 다 본다.

```
블루투스로 찾았으면      그것만 보여준다 (케이블 없이 되니 그쪽이 낫다)
못 찾았거나 못 쓰면      USB 를 본다
```

USB 는 **아무것도 안 보내고 듣기만 해서** 배 이름을 알아낸다 — 보드가
1초에 한 번 제 이름을 뱉는다. 실측 0.3초. 남의 기기일 수도 있는데 거기에
글자를 써 넣는 건 예의가 아니다.

[연결] 을 누르면 그때 정식으로 `wifi status` → `wifi ap` 를 보낸다.

**아직 사람이 눌러 본 적은 없다.** 앱 창을 내가 못 눌러서, 앱이 스스로
해보고 파일로 떨군 값까지만 확인했다.

```
듣기만 해서 알아낸 이름: SAIL-random()  (298ms)
wifi status → status name SAIL-random() mode off ip  rec off idle 300s left 0s
wifi ap     → ok wifi ap SAIL-random() pass sailing1234 ip 192.168.4.1
전부 1429ms
```

**시험하려면 BLE 를 내려야 한다.** BLE 가 잡히면 USB 는 안 보여준다.
시리얼에서 `wifi ap` 를 치면 BLE 가 내려간다. 되돌리려면 `wifi off`.

★ 부품이 줄바꿈을 없앤다. `watch()` 말고 `readBinary()` 를 쓴다 —
`desktop/src/usb.ts` 의 주석 참조.

### 안 하기로 한 것 둘 — 갈라서 보자

**1) USB 저장장치 모드** (SD 가 맥에 드라이브로 뜨는 것)

이 보드는 `ARDUINO_USB_MODE=1` (USB JTAG/serial) 로 굽고 있다. 드라이브로
띄우려면 TinyUSB 모드(`=0`)로 바꿔야 하는데, 그러면 시리얼 포트 방식과
굽는 방법까지 같이 흔들린다. 지금 잘 도는 것을 건드릴 값이 아니다.
[확인: rakwireless/boards/rak3112.json:10]

**2) USB 시리얼로 파일까지 보내기**

이건 위와 다르다 — USB 모드를 안 건드리고 지금 시리얼로 바이트를
흘려보내는 것이다. WiFi 를 갈아탈 일이 없어져서 오지에서는 더 낫다.

**재봤더니 USB 가 더 느리다.** 보드에 `usbbench` 명령을 넣어 4 MB 를
쏟아내고 맥에서 재봤다.

    WiFi AP 직결   574 KB/초     16 MB 한 세션에 29초
    USB 시리얼     266 KB/초     16 MB 한 세션에 62초

한 세션에 30초 더 걸리는 것을 감수하고 WiFi 갈아타기를 없앨 만한가 —
배가 한두 대면 갈아타기가 20초쯤이라 거의 비긴다. 그런데 새 규격을
만들어야 하는 값이 든다.

**안 한다.** USB 는 깨우는 데만 쓰고 파일은 WiFi 로 받는다.

(이 보드의 USB 는 UART 다리가 아니라 칩에 붙은 USB 다 — 맥에서
"USB JTAG/serial debug unit", VID 303a PID 1001. 그래서 115200 같은 속도
설정은 뜻이 없다. 그런데도 full-speed USB 이론값 1.5 MB/초의 5분의 1이다.
JTAG 겸용 통로라 그런 것으로 보인다 [추측].)

### 2. 버튼 5초 + 화면에 주소 ← 아무것도 없을 때

블루투스도 안 되고 대회장 WiFi 도 없는 자리가 있다. 그러면 AP 로 가야
하는데, 지금은 그걸 켤 방법이 없다. 배는 눈앞에 있으니 버튼이 답이다.

**버튼 5초 = AP 켜기.** 고민할 것 없이 늘 AP 다 — 이름·비밀번호가
필요 없는 유일한 길이기 때문이다.

화면(RAK1921)에 붙을 곳을 다 적는다.

```
  SAIL-random()
  sailing1234
  192.168.4.1
```

**지금 버튼 코드를 고쳐야 한다.** 누르고 있는 중에 2초가 지나면 그 자리에서
기록을 시작한다 (main.cpp 의 buttonPoll). 5초를 누르려면 2초를 지나가므로
기록이 먼저 켜진다. 뗄 때 얼마나 눌렀는지 보고 정하도록 바꾼다. 대신
"먹었나" 를 알려주려고 LED 로 단계를 보여준다.

```
2초 넘김 → LED 한 번    (떼면 기록)
5초 넘김 → LED 두 번    (떼면 WiFi)
```

### 3. 앱이 192.168.4.1 을 저절로 알아채기

AP 모드 주소는 **늘 192.168.4.1** 이다. 사람이 칠 이유가 없다.

보드 서랍을 열어 두면 그 주소를 조용히 두드린다. 답하면 바로 보여준다.

```
붙어 있는 배
  SAIL-random()  ●  192.168.4.1        [목록]
```

블루투스를 못 쓰면 서랍에 할 일을 적어 둔다.

```
블루투스를 못 씁니다.
  1. 보드 버튼을 5초 누르세요
  2. 맥 WiFi 를 SAIL-… 로 바꾸세요
  3. 여기 목록이 저절로 뜹니다
```

**AP 이름은 배 이름 그대로 둔다.** 배가 여러 대여도 걸어가서 어느 배인지
보이는 편이 낫다. (오지에 30대가 같이 갈 일은 거의 없다.)

### 3-1. mDNS 목록 — 나중에

보드가 `_sail._tcp` 로 알린다. 부품은 `mdns-sd` 0.21.
**WiFi 가 꺼져 있으면 찾을 것도 없어서** 위 셋보다 값이 작다. 대회장에
WiFi 가 있고 배들이 거기 붙어 있을 때만 쓸모가 있다.

### 4. BLE 로 WiFi 이름·비밀번호 넣기 — 통로는 됐고 화면이 없다

보드는 이미 받는다. `wifi ssid <이름>` / `wifi pass <비번>` 으로 NVS 에
저장하고, `wifi scan` 으로 주변 목록도 준다 (이때 BLE 는 안 끊긴다).
`desktop/src/ble.ts` 에 `Link.wifiScan()` 까지 만들어 뒀다.

**앱에 화면만 붙이면 된다.** 지금은 보드에 저장된 값(`우리WiFi`)을 쓴다.
대회장에 가면 그 자리에서 바꿀 수 있어야 한다.

### 5. 데스크탑 앱에서 남은 것

- BLE 로 배를 찾아 연결하는 것까지는 됐다. **WiFi 이름·비밀번호 넣는
  화면만 없다.**
- 어두운 바탕 지도가 없다. 하얗게 튀는 것을 눌러 봤더니 바다와 뭍 색이
  흐려져 오히려 못 읽어서 되돌렸다. 제대로 하려면 열쇠 받는 곳을 써야 한다.
- 영상 칸은 아이패드에서 눈으로 확인했다. 시험 그림이 제대로 나온다.

### 5-1. 아이패드 · 안드로이드 태블릿 — 아이패드는 뜬다

같은 코드로 만든다. 화면 코드는 한 줄도 안 갈랐다. 기기마다 다른 것은
두 군데뿐이다.

```
러스트    Cargo.toml 의 target 조건 + lib.rs 의 cfg + capabilities/serial.json
화면      platform.ts 의 caps() — 화면은 기기를 묻지 않고 기능을 묻는다
```

**아이패드에서 USB 는 안 된다.** 부품이 iOS 에서 serialport 를 아예 뺀다.
안드로이드는 된다 (android-usb-serial 이 따로 들어 있다).

`gen/apple` 은 깃에 넣는다. Xcode 에서 손으로 만진 설정이 거기 남는데, 빼
놓으면 다른 컴퓨터에서 또 해야 한다. **Xcode 화면에서 손으로 만지지 않는다.**
`project.yml` 에 글로 적는다 — xcodegen 이 매번 그 설계도대로 다시 만들어서
화면에서 붙인 것은 지워진다.

### 5-2. 아이패드 실기기 — 돌아간다

올리는 법과 처음 한 번만 막히는 것들은 기억에 적어 뒀다
(`ipad-app-deploy-and-file-speed`). 요약하면 개발자 모드 켜기, 기기를
개발자 계정에 등록, 폴더 사본 치우기, 서명 팀을 project.yml 에 적기다.

**영상 고르기는 우리 부품을 만들었다** (`plugins/tauri-plugin-videopick`).
스위프트 한 파일이다. 왜 만들었는지는 아래 숫자가 말해 준다.

```
화면이 직접 고르기        0.1초   창이 작다 (애플이 그리는 판, 못 바꾼다)
dialog 부품 (전체 화면)   3초 +   원본을 변환해서 준다
우리 부품 (전체 화면)     1.1초   변환 없이 원본 그대로
```

핵심은 스위프트 한 줄이다.

```swift
cfg.preferredAssetRepresentationMode = .current   // 원본 그대로
```

이게 없으면 아이폰이 찍은 HEVC 영상을 호환 형식으로 다시 인코딩한다.
파일을 옮기는 게 아니라 다시 만드는 것이다. dialog 부품에 이 줄이 없다.

찍은 시각도 스위프트에서 AVURLAsset 으로 읽어 같이 보낸다. 화면 쪽에서
읽으려면 앞뒤 2MB 를 IPC 로 끌어와야 하는데 그게 4MB 에 10초쯤 걸린다.

아이클라우드에만 있는 영상은 내려받는 진행을 퍼센트로 보여준다.

**틀린 짐작을 세 번 했으니 적어 둔다.**

```
asset:// 가 느리다        아니다. 첫 그림까지 0.1초다
웹킷이 Range 를 안 준다   아니다. 2021년에 고쳐졌다
Tauri 가 통째로 읽는다    아니다. Range 를 제대로 다룬다
```

느린 것은 늘 그 앞이었다. 갈라서 재야 보인다.

- [x] 블루투스로 보드 찾기 — 된다. 2026-08-27 에 하루 종일 이 길로 붙었다
- [x] 손가락으로 그래프·서랍 끌기 — 된다 (2026-08-27 아이패드에서 확인)
- [ ] 안드로이드는 아직 `tauri android init` 도 안 했다. 영상 고르기는
      화면이 직접 고르는 길로 돌아간다 (안드로이드에는 우리 부품이 없다)

### 6. 두 배 견주기 · 영상 더

- 같은 훈련에 나간 두 배를 한 화면에
- 지도에 두 항적을 같이

---

## 알아 둘 것 — 조용히 틀릴 수 있는 곳

### 세션 번호가 1 부터 다시 시작하면 보관함이 헷갈린다

데스크탑 앱은 **모듈 + 세션 번호**로 세션을 알아본다.

```ts
entryId(module, session)  →  "3CDC75702FB4_00017"
```

보드의 번호는 두 군데가 지켜 준다 — NVS 와 카드에 있는 파일 이름. 둘이
서로를 덮어서, 파일을 지워도 NVS 가 기억하고 NVS 가 날아가도 카드가
알려준다. **둘 다 없어져야** 1 부터 다시 시작한다 (플래시를 통째로 지우고
카드도 비운 경우).

그때 옛날 1번과 새 1번이 보관함에서 **같은 것으로 합쳐진다.** 코치가 적어
둔 메모가 새 세션에 붙는다. 드물지만 조용히 틀린다.

막으려면 보관함 열쇠에 무언가를 더 넣어야 한다 — 첫 fix 시각이 있으면
그것, 없으면 보드가 켜진 뒤 지난 시간이나 boot_count 같은 것.

번호에 구멍이 생기는 것(… 9, 11, 12 …)은 정상이다. 지운 자리일 뿐이고
순서는 그대로다. 다시 촘촘하게 매기면 오히려 위험하다 — 이미 받아 둔
파일과 새 파일이 같은 번호가 된다.

---

## 미뤄둔 것

- 배에서 `level` 로 힐 0점 잡기
- 멀티미터로 배터리 전압 교차 확인
- IMU 자이로 눈금 1/32 → 1/16 (BNO085 는 ±2000 °/s 인데 칸은 ±1024 까지)
- Type A 에 `s_acc` 넣기, `imu_stat` 채우기
- 워치 Always On 근본 원인 (지금은 되지만 왜 됐는지 모름)
- 옛 Feather 보드(`firmware/`) — **검증에서 뺐다.** 폴더와 코드는 그대로다.
  필요하면 그 폴더에서 직접 `pio run`. 다시 쓸 일이 없으면 지워도 된다.

---

## 쓸 수 있는 시리얼 명령

```
heel [x|y|z]        힐을 어느 가속도 축에서 볼지 (앞에 - 로 뒤집기)
pitch [x|y|z]       피치를 어느 가속도 축에서 볼지
level               지금 자세를 힐 0°, 피치 0° 로 삼기 (배가 평형일 때)
batt                배터리 전압 실측
gpscfg              모듈에 걸린 실제 설정 (짐작하지 말고 이걸로 확인)
wifi                지금 상태. wifi ap / join / off
wifi ssid <이름>    붙을 WiFi (전원 빼도 남는다). BLE 와 같은 명령이다
wifi pass <비번>    비밀번호
wifi scan           주변 WiFi 훑기 (BLE 안 끊긴다)
wifi idle <초>      아무것도 없을 때 끄기까지 (0 이면 안 끔)
rec                 기록 상태. rec on / rec off / rec mark / rec ls
sdbench [줄수]      SD 쓰기 속도·최대 멈춤 실측
gpscfg mode <0~7>   움직임 종류. 0휴대 1정지 2보행 3자동차 4선박
gpscfg static <m/s> 정지로 볼 속도 문턱값 (지금 0)
smooth <0~5>        다듬기 세기
dead <kn>           잡음 바닥 (기본 0.10)
fix                 GPS 상태 + 최고 속도 세 가지 방식 비교
navpv               NMEA 거치기 전 속도를 RMC 와 나란히
nmea <본문>         아무 NMEA 명령이나 보내고 응답 보기 (체크섬 자동)
help                전체 목록
```

문서는 `docs/` 에 있다. 인터넷에서 다시 찾지 말 것.

---

## 헤딩 센서 — 나중 목표

지금 헤딩은 못 믿는다. 두 가지가 겹쳐 있다.

**1. 센서 자체 오프셋.** AK8963 데이터시트가 초기 오프셋을 ±500 LSB 로 적어
놓았다. 16비트 0.15 µT/LSB 이니 **±75 µT** 다. 지구 자기장 총량이 서울에서
51.4 µT 인데 그보다 크다. 보정 전에는 헤딩이 의미가 없다.

**2. 배의 쇠.** 엔진·배터리·윈치·스틸 킬. 이건 센서와 같이 돌아가므로
뱃머리 방향마다 일정하게 틀어진다. 컴퍼스 스윙으로 잡을 수 있다.
부두의 쇠나 옆 배는 같이 안 돌아가므로 못 잡는다 — 보정은 마리나에서
떨어져 나가서 해야 한다.

### 할 일 (순서대로)

| | 무엇 | 걸리는 느낌 |
|---|---|---|
| 1 | **하드아이언 보정** — 보드를 8자로 돌려 각 축 최대·최소를 잡고 가운데를 뺀다 | 반나절 |
| 2 | **기울기 보정** — 지금은 `atan2(자력Y, 자력X)` 뿐이라 보드가 서 있을 때만 맞다. 힐·피치가 이미 제대로 나오니 그 값을 쓰면 된다 | 반나절 |
| 3 | **자기 편각** — 서울 기준 **서편 8.99°** [확인: BGS WMM2025, 37.5N/126.9E, 2026-08-23]. 상수 하나 빼면 된다 | 10분 |
| 4 | **소프트아이언 보정** — 타원 맞춤. 1번으로 부족하면 | 하루 |
| 5 | **배에서 컴퍼스 스윙** — 한 바퀴 돌면서 방위별 편차표 | 반나절 |

### 센서를 바꾼다면

**MPU-9250 → BNO086 + MMC5983MA** 로 가는 걸 권한다. 근거는 데이터시트에
있다 (`docs/imu/`).

```
BNO086       6축 쿼터니언 (Game RV, 자력계 안 씀)   100 Hz
MMC5983MA    자력 3축 원본                        10~100 Hz
후처리        6축 자세로 기울기 보정 + 보정된 자력  → 헤딩
```

**자세와 헤딩을 갈라놓는 게 핵심이다.**

- 자세(힐·피치)는 BNO086 의 가속·자이로 융합이 잘한다. 자력계를 안 쓰니
  배의 쇠와 무관하다
- 헤딩은 우리가 직접 다루는 자력계로 만든다. 보정을 우리가 쥔다

BNO086 의 9축 회전벡터를 쓰면 자력계 오차가 쿼터니언에 구워져 되돌릴 수
없다. 6축은 되돌릴 수 있다.

**왜 BNO085 가 아니라 086 인가.** 086 에만 Interactive Calibration 이 있다.

> "removal of gyroscope ZRO is critical to reduce heading drift. For
> applications in which low heading drift is vital, the BNO086 offers a means
> of calculating and removing the ZRO more frequently and more accurately"
> — BNO08X 데이터시트 §3.3

**왜 MMC5983MA 인가.** 정밀도 때문이 아니다. 보정이 유지되기 때문이다.

> "the SET/RESET function **clears the sensors of any residual magnetic
> polarization resulting from exposure to strong external magnets**"

요트에는 윈치 모터와 스피커가 있다. AMR 센서가 한 번 자화되면 보정이 통째로
깨지는데, MMC5983 은 매 측정마다 소자한다. 그리고 전 온도 범위에서 영점
변화가 3 mG(0.3 µT)로 묶여 있다 — 아침에 잡은 보정이 한낮에도 맞는다.

서울 수평 자기장 29.8 µT 로 환산한 헤딩 잡음이다.

| 센서 | 잡음 | 헤딩 |
|---|---|---|
| AK8963 (지금) | 0.15 µT | 0.29° |
| MMC5983MA | 0.04 µT | 0.08° |
| PNI RM3100 | 0.0027 µT | 0.01° |

셋 다 보정만 되면 충분하다. RM3100 은 지자기 관측소급이라 과하다.

### 진짜 헤딩이 필요하면

**듀얼 안테나 GNSS.** 안테나 두 개 사이 벡터로 방위를 잰다. 쇠와 무관하다.
u-blox ZED-F9H / ZED-X20D 계열. 베이스라인 66 cm 에서 약 1°.

ILCA 같은 작은 딩기에는 안 맞는다. 470/49er 정도는 된다. 안테나 두 개 +
케이블이라 IP68 포팅이 어려워진다. 2년차 RTK 얘기 나올 때 같이 보는 게 맞다.

**어느 쪽으로 가든, 지금 자력 3축 원본을 100 Hz 로 남기고 있으니 나중에
보정법을 만들어도 예전 훈련 데이터까지 다시 계산할 수 있다.**
