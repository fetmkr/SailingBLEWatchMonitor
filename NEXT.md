# 다음에 할 일

## ★★★ 현재 인수인계 — 여기부터 읽기 (2026-09-16)

아래의 09-15 기록은 역사 자료라 일부가 현재 코드와 다르다. **이 절이 현재 상태의 기준이다.**
새 문서를 만들지 말고 이후 변경도 우선 이 절과 `firmware-idf/CHECKLIST.md`에 반영한다.

### 저장소·Git·보드

- 저장소: `/Users/hojunsong/Desktop/Desktop - hojun’s mbp/SailingBLEWatchMonitor`
- 브랜치: `main`
- 헤딩 기능 기준 커밋: `b34279a feat: add calibrated fused heading and display damping`
- GitHub 원격: `git@github.com:fetmkr/SailingBLEWatchMonitor.git`
- 현재 동기화 여부는 이 문장의 옛 해시를 믿지 말고 `git status -sb`와 `git log -1`로 확인한다.
- 연결 포트는 마지막 확인 때 `/dev/cu.usbmodem101`이었다.
- 2026-09-16에 GPS 품질·LoRa 함대 BLE 통로까지 IDF 전체 빌드한 뒤 앱 파티션만 보드에 플래시했다.
  부팅 버전 `web-picker-fast-124-g9b6ba5c-di`를 확인했고 NVS와 SD 파일은 지우지 않았다.
- USB 시리얼을 열면 보드가 `USB_UART_CHIP_RESET`으로 다시 시작할 수 있다. 부팅 직후 3초의 HDG `?`를
  반복 관측하게 만들 수 있으므로 화면 시험 중에는 불필요하게 포트를 열지 않는다.

### 워치 REC 표시·제어 (2026-09-16)

- **09-17 옛 화면 재설치 원인:** 설치 문서의 `find DerivedData/SailingMonitor-* | head -1`이
  맥에 남은 08-11 빌드를 고를 수 있었다. 번호 없는 현재 프로젝트를 새 전용 경로에 빌드해
  Apple Watch Ultra 3에 직접 설치·실행했다. 설치 문서는 정확한 산출물 경로를 쓰도록 고쳤다.

- 첫 페이지 상단 점은 이제 연결 상태가 아니라 **실제 SD 기록 상태**다. `REC`는 큰 녹색 점,
  비정상 중단은 깜박이는 빨간 점과 `REC 끊김`, 자동 재시작 뒤 과거 저장 오류가 남으면 `REC 오류`다.
  보드 데이터가 3초 이상 끊겨 현재 상태를 확인할 수 없을 때도 `REC 확인 불가`와 빨간 점이 깜박인다.
  Always On에서는 애니메이션 대신 빨간 점을 계속 켠다.
- 세 번째 페이지에 현재 상태와 `REC 시작`/`REC 정지` 버튼을 추가했다. 새 명령을 만들지 않고 기존
  BLE 제어 통로의 `rec on`/`rec off`를 그대로 쓴다.
- 광고 Manufacturer Data 끝에 status 1바이트(bit0 기록 중, bit1 비정상 중단)를 덧붙였다.
  연결이 끊겨 GATT 대신 광고만 받아도 1초 안에 REC 표시가 갱신된다. 새 앱은 옛 11바이트 광고도
  읽고 그때 REC만 `모름`으로 둔다. Scan Response 31바이트 한도 때문에 사용자 이름 최대 길이는 10자다.
- 호스트 회귀검사 153개, 광고 C++→Swift 192줄, IDF 전체 빌드, Arduino 펌웨어, iOS·watchOS 빌드가 통과했다.
- Apple Watch Ultra 3에 새 앱을 설치하고 실행했다(16:42). 보드 펌웨어도 19:26에 최신 소스로
  플래시했다. REC 시작/정지와 광고 폴백을 실물 조합으로 누르는 시험은 아직 남았다.

### 현재 HDG 구현 — 식 5

한 흐름만 쓴다.

```
AK8963 원본 자력
  → 3차원 자력 보정: corrected = 3x3 matrix × (raw - bias)
  → x-io Fusion v1.3.3: 자이로 100 Hz + 가속도 + 자력계 약 8 Hz
  → 최단 각도 원형 IIR, 반감기 1초
  → 장착 오프셋 + 자기편각
  → OLED · BLE · TXT · HLG 모두 같은 HDG
```

- COG/GPS로 HDG를 고치지 않는다.
- Fusion 설정: NED, gain 0.5, gyro ±1000°/s, 가속·자력 거절 10°, 복구 5초.
- 최종 댐핑은 오래된 PNI TCM2 전자나침반의 IIR 방식을 따라 1초로 시작했다.
  설정·모드·필터를 더 겹치지 않는다. 바꿀 때는 `heading_filter.h`의
  `kDisplayDampingHalfLifeSec` 하나만 조정한다.
- `?`는 부팅 초기화, 자력 공백, 자력 거절/복구, gyro overrange 복구 때만 표시한다.
  걷기·파도 때문에 가속도 피드백만 정상 거절된 경우에는 표시하지 않고 내부 진단에만 남긴다.
- 자력 표본 나이는 마지막 **값 변화**가 아니라 마지막 정상 읽기(New/Repeat) 시각으로 계산한다.
  세 축이 5초 동안 변하지 않는 동결 검사는 기존 `mag::Freshness`가 별도로 한다.
- 자력 공백은 2초까지 자이로로 이어가며 `?`; 그보다 길면 `---`.
- 핵심 파일:
  - `firmware-idf/main/heading_filter.{h,cpp}` — Fusion 어댑터와 최종 원형 IIR
  - `firmware-idf/main/mag_calibration.{h,cpp}` — hard/soft-iron 타원체 보정
  - `firmware-idf/main/imu.{h,cpp}` — NVS 로드·자력 보정 적용
  - `firmware-idf/main/app_main.cpp` — 수집·저장·OLED/BLE/HLG 연결
  - `firmware-idf/tests/{heading_test,mag_calibration_test}.cpp`

### 자력 보정 v2

- 기존 보정은 hard-iron 중심만 뺐다. 현재 코드는 80~128개의 3차원 점으로 타원체를 맞춰
  hard-iron bias와 대칭 3×3 soft-iron 행렬을 함께 구한다.
- 품질 검사: 3차원 분포 ≥0.30, 자기장 25~70 µT, 왜곡비 ≤2.5, RMS 잔차 ≤자기장 8%,
  양의 정부호·유한값. 실패하면 **새 값을 쓰지 않고 기존 보정을 유지**한다.
- NVS: 기존 `mag_ox/y/z`, `mag_r`, `mag_res` + `mag_ver=2`, `mag_mtx`, `mag_cond`, `mag_cov`.
- 이번 플래시는 기존 NVS를 보존했다. **실물에서 v2 보정을 완료·저장했는지는 아직 확인하지 않았다.**
- 보정 절차: REC 끔 → 워치/제어에서 보정 시작(`magcal on`) → 수평 한 바퀴만 돌리지 말고
  장치를 앞뒤·좌우·뒤집기까지 천천히 모든 자세로 돌림 → 80점 이상, 가능하면 128점 → 저장(`magcal stop`).
- 저장 결과에 `v2`, 중심, 자기장, 잔차, 왜곡, 분포가 나와야 한다. `안 씀`이면 문구의 실패 이유를 그대로 남긴다.
- 보정 후 모든 방향에서 일정한 20여 도 차이가 남으면 soft-iron 문제가 아니라 장착 방향/자북·진북 기준일 가능성이 크다.
  Iris 50과 자북 기준을 맞춰 0/90/180/270°에서 같은 오차인지 확인한 뒤 `hdg off`를 한 번만 적용한다.
- 09-16 실측은 거의 모든 방향에서 Iris 50보다 보드가 15~17° 작았다. 자북 비교이므로 편각은
  0°로 두고 NVS 장착 오프셋을 +155.7°에서 **+171.7°**로 바꿘다. 플래시·재부팅 뒤 유지를 확인했다.

### 세션 124 GPS 품질 (2026-09-16)

- SD에서 세션 124를 USB로 받아 전체 17분 12초를 확인했다. HLG CRC·NAV 10 Hz·IMU 100 Hz는 정상,
  버린 줄은 0이다. 원본은 `/Users/hojunsong/Documents/ChatGPT/sailing analyzer/session124/`.
- 처음 약 10분은 칩의 수평 위치 오차 추정이 2.9~449 m를 오가며 불안정했고,
  8분 17초 근처에 SOG 15.07 kn이 튀었다.
  TXT 10초 표본에서 1.5 kn 이상인 네 값(2.15, 2.64, 2.91, 11.07 kn)은 모두 칩의 속도 오차가
  1 kn 초과였다. 기존 `sampleOk`가 이미 거절했지만 화면이 RMC 원본을 `?`로 대신 보여줬다.
- 수정: NAV-PV 공백, 속도 표식 무효, 속도 오차 1 kn 초과에서 SOG·COG는 OLED·BLE에 `---`.
  HLG에는 원본을 계속 저장한다. HDOP 1.5 고정 컷은 넣지 않았다.
- `./tools/verify.sh quick`과 IDF 전체 빌드 통과, 보드 플래시·부팅·NVS 오프셋 유지 확인. 실외에서
  위성 획득 중 `---` → 품질 통과 후 숫자 전환은 다음 시험이다.

### SD 완충·LoRa 실시간 함대 전송 (2026-09-16, 보드 적용·두 대 시험 전)

- IDF 기록 링버퍼는 PSRAM 1 MB를 우선 잡는다. 초당 3.08 KB 보수값으로 약 5분 40초이며,
  메모리가 부족하면 512→256→64 KB로 낮춘다. 내부 RAM 대체는 64 KB만 쓴다.
- 한두 번의 SD 쓰기 실패 뒤 살아나면 TXT에 재시도 횟수와 바이트를 사건으로 남긴다.
  세션이 끊겨 새 파일로 자동 재시작하면 새 TXT에 앞 세션 번호와 복구 성공을 남긴다.
  최종 실패 이유는 기존대로 NVS와 다음 TXT에 남고, 사용자 화면은 실제 기록 중단/복구 상태만 보인다.
- LoRa 문서 v1.4의 22바이트를 그대로 구현했다: 위치·SOG·COG·힐·피치·REC·배터리·최근 들은 배·MAC tie.
  코어 0 송신 일꾼이 GPS PPS 뒤 배 번호의 31.25 ms 슬롯에서 1 Hz로 보낸다. PPS를 잃으면
  마지막 PPS 기준으로 최대 20분만 이어가며, 메인 GPS·IMU·BLE 루프는 송신 14~22 ms를 기다리지 않는다.
- 부팅 기본은 SX1262 sleep이다. 워치 3페이지에서 배 번호를 저장하고 `장거리 통신 시작`을 눌러야 켜진다.
  배 번호 1~32면 같은 보드가 자기 슬롯 송신과 나머지 슬롯 수신을 함께 하고, 0이면 코치용 수신 전용이다.
  별도 송신기·수신기 펌웨어는 없다. 종료하면 다시 sleep이다.
  상태는 BLE 43바이트 확장 패킷의 배 번호·통신 on/off·PPS 준비로 되읽는다.
- PPS 전이나 20분 holdover가 끝난 뒤에는 같은 22바이트를 5~8초 무작위 간격의 확인 신호로 보낸다.
  이때 위치·SOG·COG·시각 표식은 비워 수신 앱이 `보드 켜짐 · GPS 시각 대기`로만 표시한다.
- 호스트 회귀시험 162개, C++↔Swift 교차시험, Arduino·IDF·iOS·watchOS 전체 빌드는 통과했다.
  보드에 앱 파티션만 플래시해 NVS를 보존했고, PSRAM 버퍼 1024 KB·전파시간 14144 us·부팅 off·
  SX1262 sleep·배 번호 7 유지까지 확인했다. 실물 워치용 서명 빌드도 통과했다. 워치가 `connecting`일 때
  `devicectl` 설치 두 번은 CoreDevice 3002로 실패해 옛 앱이 남았지만, `available`로 돌아온 뒤 22:01에
  최신 앱 설치와 실행이 성공했다. **워치에서 실제 시작/종료 누르기, 실외 PPS, 두 보드 송수신은 아직이다.**
- 받은 배 목록을 BLE로 코치 아이패드/맥에 보내는 30바이트 통로와 `함대 라이브` 화면을 구현했다.
  전체 함대 지도, 한 척 상세, 두 척 A/B 비교가 되고 같은 수신 PPS 프레임인지도 표시한다.
  작은 화면에서는 함대 목록을 왼쪽, 지도와 상세를 오른쪽 위아래로 두며, 기존 항해 리뷰도 세션을
  고르면 서랍을 닫고 하단 숫자를 SOG·HDG·COG·HEEL·TRIM으로 제한한다. 전체 회귀시험 162개,
  데스크탑 앱 빌드와 보드 부팅까지 확인했다. PPS 전 확인 신호는 실제 두 보드 LoRa 수신→맥 BLE→함대 화면까지 통과했고,
  **실외 PPS 뒤 위치 패킷의 LoRa 수신→아이패드/맥 알림은 아직 시험 전**이다.
- 아직 구현할 제품 기능은 같은 배 번호 충돌을 워치/코치 화면에 알리는 표시와,
  대회별 채널을 정하고 함께 바꾸는 설정이다.
  현재 고정 922.55 MHz 한 함대의 주말 시연에는 필수는 아니지만 여러 함대 제품에는 필요하다.
- 두 보드 시험 뒤에는 거리별 수신률/RSSI, 30척 부하, 장거리 on/off 소비전류와 배터리 시간을 재야 한다.

### HLG·앱 호환

- `hdg_formula=5`: 3D 자력 보정 + Fusion + 1초 원형 출력 댐핑.
- NAV 36~37에는 실제 OLED/BLE와 같은 저장 HDG가 들어간다.
- NAV event bit 3 `0x08`은 위의 HDG `?` 상태다.
- 식 5부터 HLG NAV의 mag는 **센서 원본**이다. 헤더 94~105에는 bias, 106~117에는 대칭 행렬 6개(Q12),
  118에는 보정 버전을 저장한다. 옛 식 1~4의 mag 의미는 바꾸지 않는다.
- 데스크탑 파서·TXT 파서·Python 파서는 식 5와 행렬을 읽도록 수정했다.
  Fusion은 이전 필터 상태가 필요한 상태식이라 앱이 단일 NAV 행으로 재계산하지 않고 저장 HDG를 쓴다.

### 검증 완료

- `./tools/verify.sh quick` 통과
  - 기존 펌웨어 순수 로직 153개
  - 보드↔앱 방위 벡터 960줄, 어긋남 0
  - 576개 정적 자세
  - 45°/s 회전 원본 Fusion 최대 오차 1.955°
  - 걷기 형태 합성 가속: Fusion 최대 0.138°, 순간 기울기 식 45.283°
  - 교번 자력 방위 잡음: Fusion 0.347° → 최종 댐핑 0.116°
  - 자력 보정 합성 시험: worst angle 0.44°, 나쁜 분포/평면/과왜곡 거절
- 데스크탑 `tsc --noEmit` 통과.
- ESP-IDF 전체 빌드 통과: `/tmp/sail-heading-build-live/sail_idf.bin`, 앱 파티션 76% 남음.
- 실제 보드 플래시 검증 통과. 부팅 후 GPS UART/NAV-PV, IMU/FIFO, AK8963, OLED, LoRa, BLE가 올라왔다.
- 책상에서 부팅 초기 `?` 3회 뒤 정상 숫자로 바뀌는 것을 확인했다. USB 포트 시험이 보드를 다시 시작시킬 수 있음에 주의.

### 다음 세션에서 바로 할 일

1. 실물 워치 3페이지에서 배 번호 저장, 장거리 시작/종료, `수신 중·GPS 시각 대기`와
   `송수신 중` 표시, 종료 뒤 보드 `lora`의 SX1262 sleep을 확인한다.
   그 뒤 실외에서 `lora`의 PPS 나이가 1초마다 갱신되는지 본다.
2. 두 번째 RAK3112가 있으면 송신 보드 `boat 1` + `lora live on`, 수신 보드는 앱의 `함대 라이브`에서 연결해 60초 시험한다.
   앱에 약 60번 갱신되고 위치/SOG/COG·REC·배터리가 송신 보드와 같은지, 수신 프레임·RSSI·SNR이 바뀌는지 확인한다.
   수신 보드의 `lora peers`에서도 CRC·버림·차례 놓침 0을 확인한다. 한 대뿐이면 L04는 통과 처리하지 않는다.
3. **v2 자력 보정부터 완료하고 결과 문구를 기록한다.** 보정 전 걷기 결과로 필터를 또 바꾸지 않는다.
4. 보정 후 같은 방향을 향해 걸으며 OLED HDG가 얼마나 흔들리는지 본다. iPhone Compass와 Iris 50도 같은 자북 기준으로 비교한다.
5. 0/90/180/270°와 기울이기에서 오차를 분리한다.
   - 네 방향이 비슷하게 일정: 장착 오프셋/편각 문제
   - 방향에 따라 크기·부호가 달라짐: 자력 보정/축/주변 자기장 문제
   - 걸을 때만 커짐: 동적 자세·댐핑 문제
6. 1초 댐핑이 느리면 다른 필터를 추가하지 말고 반감기 상수 하나만 낮춘다. 빠른 회전 지연과 정지 흔들림을 함께 적는다.
7. 새 항해 HLG를 받은 뒤 원본 자력 크기, 저장 HDG, COG를 시간축으로 비교한다. COG를 HDG 정답으로 맞추지는 않는다.
8. 사용자가 원격과 브랜치를 명시해 승인하면 로컬 커밋들을 `origin/main`으로 푸시한다.

---

## 이전 기록 — 아래 내용은 현재 상태와 충돌할 수 있음

> 09-15 당시 안내. 현재 작업은 문서 맨 위 `현재 인수인계`를 따른다.
> 합격 기준은 `docs/testing/boat-device-checklist.md`, 항목별 막는 것은 `docs/testing/checklist-gaps.md`.

---

## ★ 지금 하는 일 — 펌웨어를 ESP-IDF v6.1 로 옮기는 중 (2026-09-15 오후~)

- ★★ **진행은 `firmware-idf/CHECKLIST.md` 로만 한다** (목적 · 원칙 · 기능별 합격 기준 · 보드 시험 칸). 표에 없는 일은 안 한다. 새 세션은 거기부터.
  사용자 (09-15): "왜 지금 마이그레이션하는거 트래킹안해? 목적없이하는거야? 체크리스트 만들어서 해야할거 아니야."
- **폴더 `firmware-idf/`** · 실측값·결과·막힌 곳의 자세한 기록은 **`firmware-idf/PORTING.md`**.
- 기준 코드: 커밋 `2b18b17` 의 firmware-rak (HLG 1.2). firmware-rak 은 건드리지 않는다.
- **보드에는 firmware-idf 를 올려 둔다** (사용자 결정 "보드 그냥 새코드로 구현해"). 시험은 단계마다 나눠서.
- 빌드: `idf.py -B ~/esp/build-sail` 필수 (경로 빈칸 우회) · `SSL_CERT_FILE` 필요 — memory `firmware-idf-port`
- 0단계(뼈대) ✅ — 부팅·NVS 28키·GPS·I2C 0x3C/0x68·배터리(firmware-rak 과 1 mV 차)·버튼·SD 목록
- 1단계(기록기) ✅ GPS·IMU 없이 — 해시가 firmware-rak 과 같음 · 1분 기록 깨끗 · 쓰기 실패 다시 걸기 · 닫기 지연 · 리셋 이어 시작. 보드에 이 판이 있다
- 2단계(GPS·IMU) 🔶 — 켜짐·fix·gpscfg·imu·1분 기록(IMU 100.00 Hz 등간격 100%)·test gps 통과. **보드에는 아직 이 2단계 판이 있다**
- ★ 17:45 USB 가 두 번째로 멎음 → 사용자 "왜 계속 먹통 만들어. USB 연결 없이 할거 해".
- ★★★ **다시 꽂은 뒤 켜졌다 죽기 되풀이 — 원인 확정 (09-15 19:3x, 재현함)**. 자세한 실측·출처는 PORTING.md "18:35 다시 꽂은 뒤" 부터
  - 원인: 아두이노 판(firmware-rak, HWCDC.cpp:344-345)이 USB 칩의 **버스 리셋 인터럽트 켜짐(bit9)** 을 켜 둔다. 이 칸은 코어 리셋·새 펌웨어 올리기(esptool)에도 **안 지워진다** [실측 0x204].
    USB 를 다시 꽂으면 버스 리셋 **걸림**이 선다 [실측 0x200]. IDF 6.1 드라이버는 이 걸림을 안 지워서, 드라이버를 깔자마자 처리기가 끝없이 불리고 인터럽트 워치독으로 재시작 → 되풀이
  - 재현: firmware-rak 올림 → 고침 없는 판 올림(0x204 확인) → 다시 꽂기 → 20초에 4번 재시작, 매번 int_ena 0x204 · int_st 0x200
  - 고침: 드라이버 깔기 전에 드라이버가 안 다루는 켜짐을 끄고 BUS_RESET 걸림을 지운다 (저장소 app_main 커밋 0138fec).
    ❌ **보드에서는 아직 증명 안 됨.** 재현은 두 번 했다. 고친 판 시험 세 번 모두 켜짐+걸림이 같이 있는 순간을 못 만들었다
    (고친 판은 첫 켜짐에 켜짐을 끄고, 올리기(ROM)는 걸림을 지운다). 논리로는 막힌다: int_st = int_raw AND int_ena 라 켜짐을 끄면 처리기가 안 불린다
    사용자 "이걸 몇번하냐고" — 다시 꽂기 부탁은 여기서 멈춤. 더 증명하려면 새 방법이 필요하다 (PORTING.md)
  - **보드에는 지금 고친 판이 올라가 있고 bit9 켜짐은 꺼졌다** (아두이노 남음 없음)
  - 17:00 · 17:45 멎음(0바이트)은 **다른 현상일 가능성이 크다** (17:00 판은 드라이버가 없어 이 폭주가 날 수 없다) — 아직 원인 모름
  - 출처 조사(14곳): 폭주 두 조건은 출처 5개 확정. 사용자: "여러개 찾아서 결론내려. 니맘대로 한2개 보고 결론 내리지 마"
- **보드에는 지금 고친 판(2단계 df8dbec + USB 고침, worktree scratchpad/wt-df8dbec, `-B ~/esp/build-df8dbec`)이 올라가 있다.** 7단계까지 넣은 전체 판은 빌드만(경고 0)
- 3~6단계 (USB 없이, 09-15 저녁) — 코드·메인 연결·**전체 빌드 경고 0** (커밋 54348a1), 보드 시험 전:
  3 BLE (esp-nimble-cpp) · 4 화면 (U8g2, 맥에서 firmware-rak 과 프레임버퍼 같음) · 5 WiFi·HTTP·mDNS · 6 LoRa (직접 짠 SPI3 HAL)
- ★★ 루프 스택이 IDF 기본 3584 였다 (firmware-rak 8192). 1·2단계 보드 시험은 3584 로 돌았다. 이제 8192
- 7단계(전원·깊은잠·버튼) 나눠 짜는 중. 그 뒤 남는 것: magcal · hdg/heel/pitch/level/smooth/dead 설정 명령 · 진단 명령(sd·sdbench·batt·scan·help·loopstat)
- **꽂은 뒤 순서** (PORTING.md): ① 포트 열기 전 ioreg ② DTR·RTS 내리고 한 번만 열어 [BOOT] 이유·코어덤프 ③ 새 판 올리기 ④ board_rec_test all 한 번 ⑤ BLE·화면·LoRa·WiFi 속도(기본판 vs sdkconfig.tcp 판)
- **사용자 결정 대기**: 켤 때 콘솔을 UART0 으로 돌려 USB 멎음 가르기 · BLE 송신 +9/+12 dBm · CLAUDE.md nimble 스택 5120→4096 고치기 · 카드 시험 세션 84~105 지우기
- 보드 로그 받기는 DTR·RTS 를 내린 채 포트를 연다 (board_rec_test.py 방식). 기본값으로 열면 리셋이 안 될 때가 있다

## 지금 상태 (2026-09-15 11:20 기준)

### 코드 — 전부 커밋됨
| 무엇 | 어디 | 호스트 | 보드 |
|---|---|---|---|
| **기록 제어 — 원하는 상태와 실제 상태 분리.** `recWantOn/recWantOff`(단추·`rec on/off`·앱·끄기 모두) · `recControlTick` · NVS `rec_want`+`rec_open` (옛 `rec_on` 켤 때 옮김) · 쓰기 실패 뒤 3초 뒤 새 파일 3번 · 켤 때 이어 시작 5번 한도 | rec_control.h · main.cpp | ✅ | ✅ |
| **기록기 — 요청만 하고 일꾼이 마무리.** `requestStop/poll/phase` · 상태 전이는 `gStateMux` 안 `toDraining/toClosed` 만 · 쓰기 포기도 Draining · 실패 세션도 머리글·이름 고침 · 결과 하나(`SessionResult`, 첫 오류 한 번만) | hlog.h/.cpp · hlog_write.h | ✅ | ✅ |
| **끄기** — 닫힐 때까지 루프에서 기다림 → 15초 넘으면 "5초 더 누르면 끔" → 두 번째가 강제(NVS `rec_forced`) · 닫는 동안 화면 SAVING | main · display | ✅ | ❌ (보드가 잠들어 못 봄) |
| **REC FAIL** = 원하는데 멈춤 · 다시 걸기 포기 · **마지막 저장 실패(사람이 멈춘 세션도)** | rec_control.h showFailed | ✅ | 일부 |
| **기록 중 설정 잠금** — `hdg off/decl/축` · `magcal stop/clear` · `level` · `heel/pitch 축` 거절 (머리글과 어긋나서) | main.cpp | — | ✅ |
| **SD 사용권** `sdcard::acquire/release/owner` — 기록·파일 전송·진단 모두 여기로 | sdcard.h/.cpp | — | ✅ (기록 중 `sd` 거절) |
| **방위 식 하나** — 보드 `heading_tilt.h` ↔ 앱 `desktop/src/heading.ts`. OLED·BLE·TXT·앱 모두 기울기 보정. 못 구하면 `---` | heading_tilt.h · main · heading.ts | ✅ 960벡터 어긋남 0 · 답 아는 자세 | ✅ `hdg` |
| **HLG 머리글 81~105** — 방위 식·축·부호·오프셋·편각·뺀 하드아이언 | hlog · hlog_parse.py · desktop hlog.ts · SDLOG.md | — | ✅ |
| **데스크탑·아이패드 HDG** — 머리글 설정으로 보드와 같은 식. 옛 `atan2(magY,magX)`·자체 치우침 빼기·comp 줄 지움. IMU 는 NAV 이전·20 ms 안만. 설정 없는 옛 파일은 HDG 안 그림 + 이유 표시 | desktop main.ts | tsc·vite ✅ | 화면 ❌ · 아이패드 올리기 ❌ |
| NAV-PV 켤 때·`power` 뒤 같은 절차 + 프레임 오는지 로그 | main.cpp `gpsApplyNavPv` | — | ✅ 켤 때 ACK·44 ms |
| 속도 규칙 `sog_policy.h` (동작 그대로) | sog_policy.h | ✅ | — |
| 진단 정리 — 운영 상태 쓰기를 함수로(`busSawAddr`·`noteImuTemp`·`setLevelFromNow`), `sd`·`sdbench`·`batt`·`oledw`·`sleepstat` → diagnostics.cpp | diagnostics.* | — | — |
| 기록 시작 시 남은 자리 90 MB(8시간) 검사 · `rec hash` SHA-256 · `rec dump`(TXT 됨, HLG 원인 모르게 실패) | hlog · main | — | hash ✅ |
| 시험 명령 `rec slow <ms>` · `rec fail <n>` / `clear`(흉내 횟수까지 지움) · `test imu <초>` · `test gps <초>` · `off force` | main · hlog | — | slow·fail·imu ✅ |
| 저전압 경고 **3.0 V**(사용자 결정) — 전압 옆 LOW | main · display | — | ❌ |
| 워치·아이폰 광고 경로 끊김 판정 4.0 → 3.0초 | app BLEManager.swift | 빌드 ✅ | 기기 ❌ |
| **보드 기록 HDG (D7, 커밋 전)** — HLG v1.2: NAV 40바이트, 36~37 에 그 순간 방위 0.01°. 읽는 곳 전부 판 번호로 38/40 고름 | hlog.h/.cpp · main buildNav · netsrv · hlog_parse.py · fixture | ✅ | ✅ 세션 84 64줄, 앱 재계산과 차이 중앙 0.08° |
| **옛 세션 TXT 붙이기** — "보드 기록 HDG (TXT 10초)" 줄 + "추정 설정으로 재계산"(COG 안 씀, 구간 여섯 곳 차이) · 보드에서 HLG 받을 때 TXT 도 받음 | desktop boardtxt.ts · main · library | tsc·vite ✅ · 세션 46 +Y +Z 155.72°, 차이 중앙 0.47° | 맥 앱 화면 ✅ (세션 46 TXT 보관함에 붙여 둠) · 아이패드 ❌ |

### 보드 시험 결과 (11:00~11:13, `python3 firmware-rak/tools/board_rec_test.py all` / `clean`)
✅ 옛 NVS 옮김 · `hdg` 두 방위 · 기록 중 설정·`sd` 거절 · 정상 종료·머리글·해시 · 쓰기 실패 → 3번 다시 걸기(71→74) ·
닫기 20초 지연 동안 루프 응답 + 닫는 중 `rec on` → 닫힌 뒤 시작(80→81) · IMU 6.8초 끊긴 채 종료 → IMU 651 + dropped 681 = 1,332 (13 s × 100 Hz) ·
기록 중 리셋 → 이어 시작(78→79) · 사람이 멈춘 뒤 리셋 → 안 함 · **단추로 시작 → 리셋 → 이어 시작(82→83)** (외부 검토 1번 확인).
**시험 중 찾아 고친 것:** `rec fail clear` 가 흉내 횟수를 안 지움 · SD 거절 문구 조사.
**카드 잔재:** 시험 세션 47~83 은 사용자 요청으로 지웠다 (09-15 11:30, 지우기 전 가장 큰 HLG 66 KB 확인). 카드에는 1~46 만 남음 — 파일 67개 49.48 MB.
다음 세션 번호는 NVS `sess_n` 을 따라 84 부터다.

---

## 다음 할 일 (순서대로)

1. **보드에서 남은 확인** — `off` / `off force` (단추 5초로 다시 켜야 함) · 기록 중 카드 빼기 · 단추 시작 세션의 쓰기 실패 뒤 다시 걸기 ·
   GPS 끊기(`test gps`, 하늘 필요) · 저전압 LOW(3.0 V 밑) · 가득 찬 카드(90 MB 미만)
2. **앱 화면 확인** — 아이폰·워치 REC FAIL 빨강(bit5), 데스크탑 HDG 식 표시 칸, **아이패드에 올리기** ([[ipad-app-deploy-and-file-speed]])
3. **체크리스트 먼저 할 다섯** — 02 나침반 8방위 · 03 기울기 · 05 OLED·워치·앱 대조 · 06 정지·저속 · 07 기록 30회
4. **기록 시작 때 폴더 훑기 1.7→2.6초 루프 멈춤** (파일 수에 따라 늘어남, 원래 동작) — 고칠 방법 정하기
5. **저속 정책** — 30초 창이라 0.3 kn 이동이 20초 뒤에야 뜬다 (외부 검토가 재현). 시각 찍힌 정지 5분·걷기 0.3~1 kn 자료 형식부터
6. ~~`rec dump` HLG 받기 실패 원인~~ → 09-15 찾아 고침. 보드는 멀쩡했다. `serial_dump.py` 가 이름 묻기(txt 0 1)의 늦게 온 B·E 줄을 HLG 첫 조각으로 받고 S 를 못 봤다. 이제 맞는 S 뒤의 B·E 만 받는다. 세션 94 HLG 140,179 바이트 받음, 손으로 받은 것과 해시 같음

7. **방위·침로 차트 0/360 세로줄** — 세션 46 "추정 설정으로 재계산" 줄이 북쪽을 지날 때마다 위아래 꽉 찬 막대가 된다 (맥 앱 화면으로 봄). 다른 도구 조사: PX4 Flight Review 그대로 · PlotJuggler 이어 붙이기. 내 추천은 끊기 — 사용자 답 기다림
8. **카드 시험 세션 84~93** (09-15 v1.2 시험, 85~88 쓰기 실패 흉내) — 지울지 사용자에게 묻기
10. **오프라인 지도 남은 확인 (C10)** — 인터넷 끊고 보기 · 묶은 앱에서 글꼴 주소(tauri://) · 아이패드 올리기 · 받기 중 멈추기·실패 뒤 다시 해 보기 · 큰 영역(771 MB). 지도 아래쪽에 까만 띠가 하나 보인다(오프라인 바탕에서, 원인 모름). 앱 데이터 maps/ 에 사용자가 받은 아라뱃길 영역 1개(1.5 MB, 확대 14) 있음
11. **WiFi 파일 받기 241 KB/초** — 원인은 코어 2.0.17 의 TCP 창 5,760 (window scale 꺼짐). 공식 플랫폼 framework=arduino 로는 못 바꿈. 후보: 6.9.0 `arduino, espidf` (아두이노 2.0.17 + IDF 5.3.1 조합이 빌드되는지 모름) · pioarduino `custom_sdkconfig` (코어 3.3.11 로 옮김). 사례: XIAO S3 창 65535 로 3 → 22 Mbit/s (esp-idf #14171). 시험 폴더에서 속도 재기용만 빌드해 보자고 사용자에게 물어 둠
12. **HLG 줄의 GPS 시각이 옛 값을 들고 있다 — 원인 아직 못 가름, 밖에서 한 번 재면 갈린다**
    - 세션 46 (옛 펌웨어 3d05d30, 물 위, 위성 20~30+): 이웃 줄과 같은 값 68~74% (위성 수와 무관), 바뀔 때 200~500 ms. 보드 시계는 3시간에 약 100 ms 만 틀어짐
    - 세션 94 (지금 펌웨어, 실내, fix 없음, 기록 중 1분): 609줄 중 604번 100 ms 씩 늘어남 → 재현 안 됨. 체크섬 실패 0, 루프 최대 66 ms, 모듈 RMC 초당 10개·시각 0.1초씩
    - 줄에 시각을 넣는 코드(gpsWeekTow·logWriteNav)는 두 펌웨어가 같다(md5). 달라진 것은 gpsPoll 의 바이너리 골라내기(d2a8696 에서 casic::Parser 로 바뀜)와 조건(fix·밖)
    - 남은 후보: (가) 옛 파서 (나) 위성을 잡으면 모듈이 NMEA 를 덜 자주 냄. **갈라내는 시험: 지금 펌웨어로 밖에서 fix 잡고 2분 기록 → `serial_dump.py` 로 받아 itow 간격 셈 + `gps` 로 RMC 초당 수**
    - 내보내기는 계산 `utc` + 원본 `gps_utc_row` 로 나눠 둠. 시험 세션 94 가 카드에 있음
13. **세션 관리 남은 확인 (C11)** — 내보내기 저장 창·지우기 단추를 사람이 눌러 보기 · 아이패드 저장 창 · utc 고친 뒤 앱에서 zip 다시 만들기
9. 차트 이름 칸이 좁아 "보드 기록 HDG (…", "Heading · 추정 …" 로 잘린다 — 출처 표시가 가려진다

## 사용자 결정 대기
- 끊긴 뒤 워치 표시: 지금 45% 흐림 → `--` 로 바꿀지 (체크리스트 11 "정상값처럼")
- 옛 세션(46 등) HDG: 머리글에 설정이 없어 앱이 안 그린다. 사람이 설정을 넣는 길을 만들지 (넣으면 "사람이 넣은 설정" 표시)
- 데스크탑·아이패드 **오프라인 지도** (C10) — "지도 담에 고민" (아래 조사 메모)
- IMU·GPS 진단(`imu`·`calib`·`gpscfg`·`navpv`·`hdgtilt`·`fix`·`scan`)을 diagnostics.cpp 로 옮길지 — 전역 창구 20개 넘게 열어야 해서 내 판단은 "안 옮김"

---

## 참고 — 체크리스트 · 지도 조사 · 세션 46

### 체크리스트 (`docs/testing/boat-device-checklist.md`)
판정은 `통과 / 실패 / 미측정`. 기준 장비·로그가 모자라면 통과로 쓰지 않는다.
코드를 고치면 체크리스트 끝 "반복과 수정 후 확인" 표대로 다시 할 항목을 적는다 (헤딩·속도 변경 → 02~06, 기록·통신 변경 → 05·07·10·11·12).
`checklist-gaps.md` 는 09-15 오전 코드 기준이다 — 05 식 불일치·10 해시·12 시험 명령은 그 뒤 고쳤다.

### 오프라인 지도 조사 (09-15)
- 지금 `desktop/src/map.ts` 는 OSM · OpenTopoMap · Esri 위성 · OpenSeaMap 타일을 실시간으로 받는다. OSM 타일 서버는 미리 받기 금지 (`map.ts:19`)
- OSM 정책 원문: 대안으로 자체 타일·벡터 타일 포장을 권함
- Protomaps PMTiles (ODbL) — `pmtiles extract --bbox --maxzoom [--dry-run]`. 최신 빌드 `https://build.protomaps.com/20260914.pmtiles`. 한국 구역 용량은 아직 못 잼
- pmtiles JS `FileSource` 있음 · Tauri 예 github inro-digital/tauri-offline-maps (보관됨)
- 위성: Esri World Imagery(for Export) 는 ArcGIS 앱 안에서만 [검색 요약] · EOxCloudless 비상업 CC BY-NC-SA 4.0
- 해도: 국립해양조사원 2레벨 전자해도 SHP (data.go.kr 15129973, 이용허락범위 제한 없음)
- OSM 항로표지 한강·아라뱃길 67개 · Geofabrik 한국 273 MB · 브이월드 약관 원문 미확인

### 세션 46 (09-13, 2:53)
- 파일 `~/Desktop/sail-logs/S00046_20260913-1333_nosat.HLG` · `.TXT` / 앱 보관 `~/Library/Application Support/kr.fetm.sailanalyzer/logs/3CDC75702FB4_00046.HLG`
- CRC 맞음 · NAV 104,757 · IMU 1,046,711 · 못 읽은 바이트 13 · 쓰기 실패로 끊겨 이름 `_nosat`(지금 코드는 실패 세션도 이름을 고친다)
- 방위−COG (2.5 kn 넘는 47,866줄): 옛 앱 atan2(magY,magX) 흩어짐 49.5° · 보드 평평 식 6.8°(평균 −23.8°) · 기울기 보정 4.7°
- 기울기 보정 + 앞 축을 90° 돌리면 방위−COG +5°, 힐 한쪽 +9.4 / 반대 +1.9 (leeway 모양, 해석은 추측). 앞뒤 축 자동 찾기는 실패
- 오프셋 +155.7° 로 −24° 치우침 → 체크리스트 02 나침반 절차로 다시 잡을 것 (COG 로 맞추지 않는다)

### 앱 쪽 옛 메모
- C9 데스크탑·아이패드는 39바이트 확장 패킷을 안 푼다 (A2) — REC FAIL 빨강 없음
- 데스크탑 깨우기 `wifi ap <앱id>` — 맥에서 세션 46 받기 됨 (09-14)

---

## 2026-09-09 에 한 것 — 한 장 요약

### 1. 배터리가 하루에 비던 것을 잡았다

**보드가 1.3초에 한 번씩 스스로 깼다.** 570초에 445번. 깰 때마다 부팅에 1초가
걸리니 자는 게 아니라 거의 계속 켜져 있었다.

깊은잠에 들면 RTC 주변장치가 꺼지면서 버튼 핀의 내부 풀업도 같이 사라진다.
뜬 핀은 LOW 로 읽히고, 깨우는 조건이 "LOW 면 깨어라" 였으니 그 자리에서 다시 깼다.

    esp_sleep_pd_config(ESP_PD_DOMAIN_RTC_PERIPH, ESP_PD_OPTION_ON)

헛깸 445 → **0**. 300초·60초·10시간 시험 모두 0.

### 2. ★ 전류 측정 방법이 두 번 틀렸다

**첫 번째.** 깰 때 전압을 USB 꽂힌 채로 쟀다. 충전이 65 mV 를 부풀리는데
밤새 잰 낙차가 35 mV 였다. 부풀림이 신호보다 컸다. 이틀치가 무효가 됐다.
사용자가 짚어줬다.

**두 번째.** 끝점 두 개만 재서 **표면 전압과 진짜 방전을 못 갈랐다.**
같은 보드 같은 코드가 세 번 다 다르게 나왔다.

    10.47시간  32 mV → 3.06 mV/h        2.46시간  31 mV → 12.60 mV/h

낙차는 셋 다 30 mV 언저리인데 시간만 달랐다. 맞춰 보면
`D = 29.9 mV(한 번 꺾임) + 0.304 mV/h`, 남은 오차 ±1.1 mV.
**낙차의 대부분이 방전이 아니라 충전기를 뗄 때 한 번 빠지는 표면 전압이었다.**

그래서 `drain` 을 만들었다. 13시간에 15번, 앞은 촘촘히 뒤는 성기게. 사람도 USB 도
안 낀다. `drain fast` 로 같은 길을 13분에 확인한다.

### 3. 회로도에서 답을 찾았다

RAK19007 전원 회로도(그림 파일)를 받아서 읽었다.

  · 배터리 분압에 **콘덴서가 없다.** R3 1M / R4 1.5M 뿐. 끌 수도 없다 (늘 1.4 µA)
  · 전원 갈아타기는 **잘 만든 회로다.** 배터리는 다이오드가 아니라 P채널을 거친다
  · **강압형이라 배터리가 3.4 V 근처면 3.3 V 를 만들 여유가 100 mV 뿐이다.**
    그 구간에서 대기 전류가 나빠진다. 우리가 잰 값은 전부 3.48~3.55 V 였다

**다시 잴 때는 배터리를 3.8 V 위로 올려놓고 해야 한다.**

### 4. 주석을 채우고 구조 문서를 그렸다

설명 없던 함수 55개를 채웠다 (hlog 11→20%, lora 16→25%, netsrv 20→25%).
`ARCHITECTURE.md` 를 새로 썼다. 그림판 사본도 있다.

### 5. 방위에 기울기 보정을 붙였다 (INSLIB 0단계)

배가 기울면 자력계 방위가 틀어진다. 힐 20°에 29°, 30°에 50° 틀린다.
우리가 재려는 leeway 는 3~8° 다. **오차가 재려는 값보다 네 배에서 열 배 크다.**

식은 INSLIB 의 `ahrs_mag_detilt` 를 옮겼다. 검산에서 72가지 전부 0.5° 미만.
손으로 기울여 보니 84° → 27°, 53° → 33° 로 줄었다.

**바꾸지 않았다.** 화면·BLE·기록은 여전히 평평한 쪽이다. 나란히 찍기만 한다.

---

## 다음에 할 일 (순서대로)

### 잠자기 전류 — 배터리 3.8 V 위에서 다시

    충전 3.9 V → USB 뽑고 30분 쉬기 → drain → 아침에 drainstat

★ **"안 보인다" 가 나오면 좋은 것이다.** 3.6 V 위에서 25 µA 라면 13시간에
0.5 mV 도 안 움직인다. ADC 잡음이 ±2~3 mV 라 아예 안 잡힌다.
숫자를 정확히 알려면 멀티미터를 배터리 선에 물려야 한다.

### 방위 — 하드아이언 보정이 먼저다

기울기 보정 식은 정확하다. **남은 오차는 입력이 틀려서 나온다.**
한 판 안에서 자력 크기가 16~45% 흔들린다.

    1. 하드아이언 보정        ← 다음. 배에 달아놓고 해야 한다
    2. 자기 편각 (약 8° 서편)
    3. 자이로 필터 (INSLIB ahrs_t, 656 바이트)

자이로 필터를 먼저 넣어봐야 틀린 입력을 매끄럽게 만들 뿐이다.

★ **책상에서 하면 안 된다.** 판마다 자력 크기가 29~42 / 34~48 / 49~58 µT 로
달랐다. 한국 지구 자기장은 약 50 µT 로 한자리에서 일정해야 한다.
맥·케이블·책상 철제가 자기장을 휘고 있다.

---

---

## 돌고 있는 시험 — drain 첫 실전 (2026-09-09 19:05 시작)

**배터리를 3.97 V 까지 채워서 강압형이 나빠지는 구간을 벗어난 뒤 걸었다.**

    기준값     3.97 V   (19:02, USB 뺐을 때 사람이 화면에서 읽음 — 이게 진짜)
    잘 때      4026 mV  (USB 꽂은 채 기록돼서 부풀려져 있다)
    끝나는 때  2026-09-10 08:05 쯤. 스스로 켜져서 기다린다

15번 찍는다. 앞은 촘촘, 뒤는 성기게.

    5분 · 10 · 20 · 30 · 45 · 60분 · 1.5 · 2 · 3 · 4 · 5 · 7 · 9 · 11 · 13시간

중간에 1.5초씩 깨느라 USB 가 나타났다 사라진다. 정상이다.

### 아침에 읽는 법

    USB 꽂고  →  drainstat

볼 것 셋.

  · **앞쪽이 빠르게 꺾이면** 표면 전압이다. 빼고 본다
  · **뒤쪽 직선의 기울기**가 진짜 새는 양이다
  · **깨어 있던 시간 합계** — 재느라 쓴 것이라 빼야 한다 (15번 x 1.6초 = 24초쯤)

### ★ "안 보인다" 가 나오면 좋은 것이다

3.97 V 에서 25 µA 라면 13시간에 1 mV 도 안 움직인다. ADC 잡음이 ±2~3 mV 니
아예 안 잡힌다. 그러면 **µA 급이라는 뜻이고 그게 목표였다.**
정확한 숫자는 멀티미터를 배터리 선에 물려야 나온다.

앞선 시험들은 전부 3.48~3.55 V 에서 쟀다. 강압형이 3.3 V 를 만들 여유가
100 mV 밖에 안 남는 구간이라 그때 나온 129 µA 는 그 구간의 값이다.
이번 판은 그 위에서 재는 첫 판이다.

---

## ★ 앞선 두 시험은 무효다 — 충전이 섞였다 (2026-09-09 판정)

    11:31  USB 꽂힘   3.605 V
    11:43  USB 뽑음   3.540 V
           차이        65 mV

**충전이 65 mV 를 부풀린다. 밤새 잰 낙차는 35 mV 였다. 부풀림이 신호보다 크다.**

깰 때 값은 부팅 1초 뒤 루프가 잰다. 사람이 버튼을 누른 뒤 USB 를 언제 꽂았는지에
따라 섞이기도 하고 안 섞이기도 하는데, 그걸 안 정해놨다.

    안 섞였다면   35 mV / 14.41h = 2.4 mV/h → 1.0 mA
    섞였다면     100 mV / 14.41h = 6.9 mV/h → 2.9 mA

어느 쪽인지 알 수 없다. 9/8 10:39 시험(3.1 mV/h)도 같은 상황이라 **이틀치가 다
무효다.** "1.30 → 1.03 mA 로 20% 나아졌다" 는 비교도 근거가 없다.

전압과 무관한 값은 그대로 살아 있다.

    헛깸 0            세는 값이라 충전과 상관없다
    3V3_S 꺼짐        GPS 바이트 0
    445번 깨던 버그    잡은 것이 맞다

즉 **고친 것은 맞고, 얼마나 남았는지만 모른다.**

### 더 나은 방법 (아직 안 만듦)

보드가 자다가 30분마다 스스로 깨서 전압만 적고 다시 자게 한다. 스무 점을
배터리로만 찍으면 USB 가 낄 자리가 없고, 직선을 맞춰서 기울기를 뽑으면 끝점
두 개보다 잡음에 강하다. 깨는 데 드는 값은 밤새 쓰는 양의 4% 라 결과를 안 흔든다.

---

## 끝난 시험 — SD 칩셀렉트를 붙들면 전류가 주나 (2026-09-08 21:26 시작)

USB 뽑고 버튼 5초로 껐다. 끌 때 3.618 V (9%, USB 꽂힌 채로 잰 값이라 실제는 조금 낮다).

아침에 버튼 5초로 깨우고 USB 꽂은 뒤 `sleepstat`.

**비교할 값은 3.1 mV/h.** 아래 10:39 시험에서 나온 값이다.

    확 내려감   칩셀렉트가 뜬 것이 원인이었다
    그대로      카드를 빼고 한 번 더 재서 카드가 범인인지 가른다

배터리 걱정은 없다. 9% = 175 mAh 인데 안 고쳐졌어도 1.3 mA 라 5.6일 간다.

---

## 끝난 시험 — 깊은잠 전류 (2026-09-08 10:39 시작, 10.47시간)

결과: 헛깸 0, 3511 → 3479 mV, **시간당 3.1 mV**.
1950 mAh 기준 약 1.3 mA. SD 카드 대기 전류로 의심된다. 경위는 POWER.md.



배터리 7% (3.58 V) 에서 USB 를 뽑고 버튼 5초로 껐다.
깨울 때 버튼 5초 → USB 꽂고 `sleepstat`.

볼 값 두 개.

    헛깸         0 이어야 한다
    시간당 낙차  10 mV/h 밑이면 정상. 수십이면 뭔가 켜져 있다

배터리가 방전 곡선의 가파른 자리라 새는 게 있으면 몇 시간에 크게 티가 난다.
몇 시간 뒤에도 멀쩡히 깨어나면 그것만으로도 좋은 신호다.

★ 이 시험 전에 헛깸 445회(570초)를 잡아 고쳤다. 경위는 POWER.md.
