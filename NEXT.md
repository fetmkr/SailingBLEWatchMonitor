# 내일 할 일

마지막 작업: 2026-08-23. 힐을 가속도 X 축에서 재게 바꾸고, 배터리 전압을
퍼센트 옆에 같이 내보내게 했다. 보드·아이폰은 구워서 올렸다.

---

## 0. 배에서 먼저 — 힐·피치 0점 잡기 ← 이번에 바뀐 것

밖에서 재 보니 **방위(HDG)가 맞으려면 보드를 세워서 달아야 했다.** 그렇게 세우면
예전 계산(roll/pitch)은 엉뚱한 회전을 재게 된다. 그래서 둘 다 가속도 한 축에서
바로 구하게 바꿨다.

| | 축 |
|---|---|
| 힐 | 가속 **Y** |
| 피치 | 가속 **Z** |
| (남은 X) | 위아래를 향하는 축. ±1 g 를 읽는다 |

보드에 이미 걸어 뒀다. 기준각만 아직 0 이다.

배를 평형으로 두고 이 순서로 한다.

```
heel        지금 축과 가속도 세 축 값을 본다 (pitch 도 같은 내용)
level       지금 자세를 힐 0°, 피치 0° 로 삼는다
```

**맞는 축인지 보는 법:** 배가 평형일 때 그 축이 0 g 에 가까워야 한다.
다르면 축을 바꾼다. 다시 구울 필요 없다.

```
heel x      힐을 X 축에서 (기준각은 자동으로 0 이 된다)
heel -y     좌우가 뒤집혀 있으면 부호를 뒤집는다
pitch -z    피치가 앞뒤 반대면
```

축을 바꾸면 기준각이 0 으로 돌아가므로 `level` 을 다시 쳐야 한다.

**부호도 확인한다.** 해양 장비 관례(NMEA 2000 PGN 127257)를 따른다.

| 움직임 | 나와야 할 값 | 반대면 |
|---|---|---|
| 우현으로 누움 | 힐 **양수** (앱에 STBD) | `heel -y` |
| 좌현으로 누움 | 힐 **음수** (앱에 PORT) | |
| 뱃머리 들림 | 피치 **양수** | `pitch -z` |

책상에서도 보드를 손으로 기울여 미리 볼 수 있다. 어느 쪽이 뱃머리인지만
정해 두면 된다.

> 방위 자체는 아직 기울기 보정이 없다. 배가 기울면 방위가 틀어진다.
> 자기 편각(한국 약 8도 서편)도 아직 안 넣었다. 미뤄둔 것 항목 참고.

---

## 0-1. 상태 확인

시리얼에서 이 두 개만 쳐 보면 된다.

```
gpscfg      모듈에 실제로 걸린 설정
smooth      다듬기 세기
```

**이렇게 나와야 정상이다.**

```
통신 속도     115200 bps
측위 간격     100 ms (= 10.0 Hz)
움직임 종류   0  Portable 일반 휴대   — 우리가 고른 값
정지 문턱값   0.00 m/s   — 꺼져 있다
쓰는 위성     GPS BeiDou GLONASS
다듬기 세기   2단계 (시상수 0.6초)
잡음 바닥     0.10 kn
```

다르면 아래로 되돌린다.

```
gpscfg mode 0
smooth 2
dead 0.10
```

---

## 1. 차로 30노트 구간 확인 ← 제일 먼저

**왜:** 걷는 속도(0~3 kn)는 확인했다. 배는 0~30 kn 인데 **위쪽 절반을 아직
안 봤다.** 같은 칩에서 모드가 안 맞아 시속 40km 가 18 로 나온 사례가 있다
(Espruino #7025). 우리가 겪은 것과 같은 고장의 반대쪽이다.

**방법:** 보드만 들고 차로 나간다. 시속 50~60km 로 달린다. 이게 27~32 노트다.

- [ ] 출발 전 `fix` 를 쳐서 기록을 비운다 (보드 리셋으로 충분)
- [ ] 천천히 출발 — 화면 숫자가 바로 따라오나
- [ ] 시속 50~60km 로 정속 주행 — **차 계기판과 화면 숫자를 비교**
- [ ] 신호 대기 — 0 으로 떨어지나, 몇 초 걸리나
- [ ] 돌아와서 `fix` — `본 최고 속도 RMC` 가 실제 최고 속도와 맞나

**판정**

| 결과 | 뜻 | 할 일 |
|---|---|---|
| 계기판과 1노트 안쪽 | 휴대 모드로 확정 | 다음 항목으로 |
| 화면이 계속 낮게 나옴 | 휴대 모드도 위쪽을 누른다 | `gpscfg mode 3`(자동차)로 재시험 |
| 반응이 3초 넘게 늦음 | 다듬기가 센 것 | `smooth 1` 로 낮춰 재시험 |

---

## 2. 다듬기 단계 실물로 고르기

**왜:** 지금 기본값 2단계(0.6초)는 **내가 정한 숫자지 재본 값이 아니다.**
실제 요트 계기는 사용자가 고른다.

- [ ] 차 안에서 `smooth 0` (원본) — 숫자가 얼마나 떠는지 본다
- [ ] `smooth 2` — 읽을 만한가
- [ ] `smooth 4` — 반응이 답답한가
- [ ] 쓸 만한 단계를 정해서 그걸로 둔다 (보드에 저장된다)

---

## 3. 가속도계로 다듬기 세기 자동 전환 ← 여기가 본 작업

**왜:** 세게 다듬으면 숫자는 안정되지만 택 할 때 늦다. 논문(PMC10007219)이
쓴 방법은 **상태를 가려 세기를 바꾸는 것**이다. 속도 오차를 최대 80% 줄였다.

| 상태 | 다듬기 |
|---|---|
| 멈춤 | 갱신 안 함 |
| 속도 변하는 중 | 약하게 (반응 빠르게) |
| 일정 속도 | 세게 (숫자 안정) |

논문은 가속도계로 가른다. **우리도 9축이 이미 붙어 있다.**

- [ ] 배 앞뒤 방향 가속도를 뽑는다 (지금 힐 계산에 쓰는 축 말고 진행 방향)
- [ ] 변화 문턱을 정한다 — 논문은 3 km/h 상당을 썼다. 요트는 더 낮아야 할 듯
- [ ] `dampingUpdate()` 안에서 `tau` 를 두 값 사이로 바꾼다
      (`kDampTau[gDampLevel]` 과 그 한두 단계 아래)
- [ ] **IMU 가 죽어도 멈추면 안 된다.** `gImuOk` 가 거짓이면 지금처럼 고정 세기
- [ ] 시리얼에 지금 어느 상태인지 찍어서 눈으로 확인

건드릴 곳: `firmware-rak/src/main.cpp` 의 `dampingUpdate()` 근처.

---

## 4. 저속에서 COG 처리

**왜:** 배가 느리면 위치 오차가 방향 계산을 지배한다. 정박 중에 침로가 빙빙
돈다. 지금은 그대로 내보내고 있다.

- [ ] 속도가 잡음 바닥 아래면 COG 를 붙들거나 안 보여준다
- [ ] 자력계 방위(HDG)로 갈아탈지 정한다 — 화면에 이미 나란히 있다
- [ ] 앱에도 같은 규칙을 넣는다 (`app/Shared/Protocol.swift`)

---

## 5. 앱 설치 ← 코드는 끝났고 올리기만 하면 된다

메인 화면에서 COG 를 **HDG(뱃머리 방향)** 로 바꿨다. 요트 계기가 하는 방식이다.
멈춰 있거나 느릴 때 GPS 침로는 빙빙 돌지만 자력계 방위는 안 흔들린다.
COG 는 상세 화면(아이폰) / 2페이지(워치)에 HDG 와 나란히 있다.

자력계가 없는 보드(옛 Feather)에서는 **라벨이 저절로 COG 로 바뀐다.**
같은 라벨로 다른 값을 보여주면 읽는 사람이 속기 때문이다.

빌드는 통과했다. 어제는 아이폰·워치가 연결돼 있지 않아 설치를 못 했다.

- [ ] 아이폰 연결 후 설치

```bash
cd app
DD=$(mktemp -d)
xcodebuild -project SailingMonitor.xcodeproj -scheme SailingMonitor \
  -destination 'id=5D03F469-2C6D-5505-91BF-EB8473431364' \
  -configuration Debug -derivedDataPath "$DD" -allowProvisioningUpdates build
xcrun devicectl device install app \
  --device 5D03F469-2C6D-5505-91BF-EB8473431364 \
  "$DD/Build/Products/Debug-iphoneos/SailingMonitor.app"
```

- [ ] 워치는 **Xcode 에서만 된다.** 스킴 `SailingMonitor Watch App` + 기기 워치 → ⌘R
      (기기 목록에 워치가 안 보이면 스킴을 먼저 바꿀 것)
- [ ] 두 화면 다 HDG 로 나오는지 눈으로 확인

## 미뤄둔 것

- SD 카드 로깅 — 밖에서 오래 기록해야 필터를 제대로 맞출 수 있다
- 배에서 `level` 로 힐 0점 잡기
- 멀티미터로 배터리 전압 교차 확인
- WiFi 용도 미정 (`secrets.h` 아직 안 씀)
- 워치 Always On 근본 원인 (지금은 되지만 왜 됐는지 모름)
- 옛 Feather 보드(`firmware/`) — **검증에서 뺐다.** 폴더와 코드는 그대로다.
  필요하면 그 폴더에서 직접 `pio run`. 다시 쓸 일이 없으면 지워도 된다.

---

## 쓸 수 있는 시리얼 명령

```
heel [x|y|z]        힐을 어느 가속도 축에서 볼지 (앞에 - 로 뒤집기)
pitch [x|y|z]       피치를 어느 가속도 축에서 볼지
level               지금 자세를 힐 0°, 피치 0° 로 삼기 (배가 평형일 때)
batt                배터리 전압 실측
gpscfg              모듈에 걸린 실제 설정 (짐작하지 말고 이걸로 확인)
gpscfg mode <0~7>   움직임 종류. 0휴대 1정지 2보행 3자동차 4선박
gpscfg static <m/s> 정지로 볼 속도 문턱값 (지금 0)
smooth <0~5>        다듬기 세기
dead <kn>           잡음 바닥 (기본 0.10)
fix                 GPS 상태 + 최고 속도 세 가지 방식 비교
navpv               NMEA 거치기 전 속도를 RMC 와 나란히
nmea <본문>         아무 NMEA 명령이나 보내고 응답 보기 (체크섬 자동)
help                전체 목록
```

문서는 `docs/` 에 있다. 인터넷에서 다시 찾지 말 것.
