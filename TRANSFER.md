# 기록 회수 — 보드에서 데스크탑으로

보드가 HTTP 서버가 되고 데스크탑 앱이 받아 간다.
펌웨어는 `firmware-rak/src/netsrv.cpp`, 데스크탑은 `desktop/`.

---

## 1. 제일 중요한 것 — 목록만 보고 정할 수 있어야 한다

한 세션이 8시간이면 90 MB 다. 30대면 2.7 GB 다.
**뭘 받을지 정하려고 받아 볼 수는 없다.**

그래서 목록이 이만큼을 준다. 파일당 128바이트짜리 머리글만 읽으면 다 나온다.

```json
{ "name": "S00008.HLG", "size": 95484,
  "session": 8,
  "module": "3C:DC:75:70:2F:B4",
  "utc_start": 1787504400, "utc_start_ms": 120,
  "duration_s": 1832,
  "nav_rows": 18320, "imu_rows": 183200, "dropped": 0,
  "imu_type": 1, "gnss_dyn": 0, "nav_hz": 10, "imu_hz": 100,
  "fixed": true }
```

코치가 이걸 보고 정한다.

- **utc_start** — 언제 찍은 세션인가. 오늘 오전 훈련인가 어제 것인가
- **duration_s** — 5분짜리 시험 기록인가 두 시간 훈련인가
- **module** — 어느 보드인가. 선수 배정과 맞춘다
- **dropped** — 0 이 아니면 구멍이 난 세션이다
- **fixed** — 위성을 한 번이라도 잡았나. 아니면 실내 시험이다

### 이걸 채우려고 머리글을 세션 끝에 다시 쓴다

규격의 머리글에 `utc_start` 칸이 있는데 기록을 **시작할 때는 아직 모른다.**
전원을 켠 직후에는 GPS 가 시각을 모르기 때문이다.

그래서 **세션을 닫을 때 머리글을 다시 쓴다.** 파일 맨 앞으로 되돌아가
128바이트를 새로 쓰고 CRC 를 다시 계산한다. 그때는 다 알고 있다.

- 첫 fix 의 UTC
- 세션 길이
- NAV·IMU 줄 수, 버린 줄

전원이 갑자기 끊겨서 머리글을 못 고친 파일은 `utc_start` 가 0 이다.
데스크탑 앱은 그런 파일도 받을 수 있어야 한다 — 안에는 값이 다 들어 있다.

---

## 2. 주소

```
GET    /                     사람이 보는 안내 (브라우저로 열어도 된다)
GET    /api/status           지금 상태
GET    /api/files            파일 목록 + 요약  ← 위 §1
GET    /file/<이름>          파일 원본. Range 받는다
POST   /api/rec/mark         이벤트 표식
DELETE /file/<이름>?confirm=<세션번호>   지우기
```

### 이어받기

`/file/` 은 `Range: bytes=N-` 을 받는다. 90 MB 를 받다 끊겼을 때 처음부터
다시 받는 건 낭비다. 데스크탑 앱은 받다 만 파일이 있으면 그 지점부터
이어 받는다.

### 지우기는 두 번 확인한다

`?confirm=<세션번호>` 가 파일 안의 세션 번호와 맞아야 지운다.
실수로 URL 을 잘못 쳐서 남의 훈련이 날아가면 안 된다.

**데스크탑 앱은 받아서 검사가 끝난 뒤에만 지운다.** 검사는 CRC 로 한다
(§4). 검사 전에 지우는 길은 앱에 만들지 않는다.

---

## 3. 보드를 어떻게 찾나

### 바닷가 — AP

보드가 스스로 WiFi 를 만든다. 공유기가 없어도 된다.

```
이름       SAIL-random()      (모듈 이름 그대로)
비밀번호   secrets.h 의 SAIL_AP_PASS
주소       http://192.168.4.1/
```

주소가 늘 같아서 찾을 필요가 없다. 대신 **한 번에 한 대**만 붙는다.

### 클럽 — Join + mDNS

보드가 아는 WiFi 에 붙는다. IP 는 공유기가 주므로 미리 알 수 없다.
그래서 mDNS 로 이름을 알린다.

```
http://sail-random.local/
```

**30대를 한꺼번에 회수할 때는 이쪽이다.** 데스크탑 앱이 `_sail._tcp` 를
찾으면 켜져 있는 보드가 다 나온다. 한 대씩 AP 를 바꿔 붙을 필요가 없다.

---

## 4. 받은 뒤 검사

데스크탑 앱이 반드시 한다. 순서대로다.

1. 크기가 목록의 `size` 와 같은가
2. 머리글 CRC 가 맞는가
3. 레코드를 훑으면서 CRC 가 다 맞는가 (`tools/hlog_parse.py` 와 같은 방식)
4. 못 읽은 바이트가 0 인가

하나라도 틀리면 **틀린 자리부터 Range 로 다시 받는다.** 처음부터 다시 받지
않는다.

넷 다 통과해야 "받았다" 로 표시하고, 그때부터 지우기를 열어 준다.

---

## 5. 서버는 막는 쪽이 단순하다

`WebServer` 를 쓴다. 한 번에 한 손님만 받고, 파일을 보내는 동안 메인 루프가
묶인다. 비동기 서버를 안 쓰는 이유는 **기록 중에는 어차피 전송을 안 하기
때문이다.**

- 파일을 보내는 동안 100 Hz 기록이 끊긴다
- WiFi 와 BLE 가 같은 무선을 나눠 쓴다
- 회수는 훈련이 끝난 뒤에 한다

그래서 `wifi ap` 는 기록 중이면 거절하고, `/file/` 도 기록 중이면 409 를
돌려준다. 규칙을 한 줄로 두는 편이 안전하다.

---

## 6. 데스크탑 앱이 하는 순서

```
1. 보드 찾기        AP 면 192.168.4.1, Join 이면 mDNS 로 목록
2. GET /api/files   요약을 표로 보여준다
3. 고른다           이미 받은 것(모듈+세션번호)은 회색으로
4. GET /file/...    Range 로 이어받기. 진행률 표시
5. 검사             §4. 실패하면 그 자리부터 다시
6. 넣는다           로컬 보관함에 넣고 파싱해서 타임라인으로
7. (선택) 지운다    검사 통과한 것만
```

---

## 7. 아직 안 정한 것

- **속도.** SPI 4 MHz 로 SD 를 읽는다. 아직 안 쟀다. 90 MB 를 얼마에
  보내는지 재고 나서 SPI 를 올릴지 정한다
- **선수 배정.** 지금은 모듈 MAC 만 안다. 누가 탔는지는 앱에서 붙인다
- **여러 대 동시 회수.** mDNS 로 찾은 뒤 한 대씩 순서대로 받을지,
  동시에 받을지. 보드마다 따로 붙으므로 동시도 되지만 먼저 한 대로 재 본다
- **한 화면에 몇 척을 올릴 것인가.** 쓰는 방식이 둘로 갈린다.

  | | 몇 척 | 무엇을 보나 |
  |---|---|---|
  | 심층 분석 | **2척** | 같은 시각에 나란히. 속도·힐·트림 그래프를 겹쳐서 |
  | 코스 보기 | 여러 척 | 지도 위 항적. 누가 어느 쪽으로 갔나 |

  둘은 화면도 다르고 필요한 데이터 양도 다르다. 심층 분석은 100 Hz 원본이
  다 필요하고, 코스 보기는 10 Hz NAV 만 있으면 된다. **나중에 정리한다.**
