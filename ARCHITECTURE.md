# 보드 펌웨어가 어떻게 생겼나

RAK3112(ESP32-S3) 펌웨어의 구조와 도는 순서. 코드는 `firmware-rak/`.

세부 규격은 다른 문서에 있다.
로라와 BLE 는 `PROTOCOL.md`, 기록 파일은 `SDLOG.md`, GPS 설정은 `GPS.md`,
끄고 켜기는 `POWER.md`, 파일 보내기는 `TRANSFER.md`.

**그림판 사본** — 같은 내용을 그림으로 그린 것이 있다.
https://claude.ai/code/artifact/35ba1b27-010c-4aa5-b7b0-6d6066cee067

★ 둘은 같은 내용이다. 고칠 때는 **같이 고쳐야 한다.**

---

## 1. 무엇이 무엇에 붙어 있나

```
                        ┌──────────────────────────────┐
     위성 ┄┄┄┄┄┄┄┄┄┄▶ │  GPS   RAK12501 (L76K)       │
                        │  슬롯 A · UART1 115200        │──┐
                        └──────────────────────────────┘  │
                        ┌──────────────────────────────┐  │
                        │  IMU   RAK1905 (MPU-9250)    │  │
                        │  슬롯 C · I2C1 0x68           │──┤
                        └──────────────────────────────┘  │
                        ┌──────────────────────────────┐  │
                        │  화면  RAK1921 (SSD1306)     │  │
                        │  J12 헤더 · I2C1 0x3C         │──┤
                        └──────────────────────────────┘  │      ┌─────────────┐
                        ┌──────────────────────────────┐  ├─────▶│  RAK3112    │
                        │  SD    RAK15002              │  │      │  ESP32-S3   │
                        │  IO 슬롯 · SPI 20㎒           │──┤      │  + SX1262   │
                        └──────────────────────────────┘  │      └─────────────┘
                        ┌──────────────────────────────┐  │         │  │   │
                        │  버튼  J11 1번 (GPIO2)        │──┘         │  │   │
                        └──────────────────────────────┘            │  │   │
                                                                     │  │   │
                        BLE 10 Hz ◀───────────────────────────────────┘  │   │
                        (아이폰 · 워치)                                    │   │
                        WiFi (파일 보낼 때만) ◀───────────────────────────┘   │
                        (데스크탑 · 아이패드)                                  │
                        LoRa 923.3㎒ ◀────────────────────────────────────────┘
                        (다른 배)
```

**전원이 두 갈래다. 이걸 모르면 저전력에서 헤맨다** (`POWER.md`).

```
VDD    늘 켜져 있다        IMU · 화면 · SD · 무전기
3V3_S  GPIO14 로 끈다      GPS 만
```

---

## 2. 코어 두 개를 나눠 쓴다

ESP32-S3 는 코어가 둘이다. 하나에 다 몰면 서로 기다린다.

```
  코어 1 — loop()                         코어 0 — 일꾼 둘
  ────────────────────────────────        ─────────────────────────────
  GPS 읽기                                 hlog writerTask
  IMU 읽기                                   링버퍼 → SD 카드
  화면 그리기                                4KB 모아서 · 5초마다 못 박기
  BLE 알림
  WiFi 요청 받기                           lora rxWorker
  버튼 보기                                  DIO1 기다렸다 → 링버퍼
  시리얼 명령
```

**왜 나눴나. 약속 시간이 서로 다르기 때문이다.**

- SD 카드는 속으로 정리하느라 가끔 오래 대답을 안 한다. 우리 카드 실측 최악이 1.9초다.
- 로라는 함대에서 차례 간격이 25 ms 다. 그 안에 안 꺼내면 다음 배가 덮어쓴다.

둘을 한 코어에 두면 카드가 멈춘 1.9초 동안 배 일흔 척을 놓친다.

**사이는 링버퍼로 잇는다.** 만드는 쪽과 쓰는 쪽이 서로 안 기다린다.

```
   코어 1                    링버퍼 64 KB                코어 0
   ──────                    ────────────                ──────
   writeNav() ──▶ push() ──▶ [][][][]    ──▶ 꺼내서 ──▶ SD.write()
   writeImu()                 ▲      ▲
                            gTail  gHead
```

크기 64 KB 는 초당 3.1 KB 를 21초 버티는 양이다. 실측 최악 1.9초의 열 배다.

---

## 3. 켤 때 — setup()

★ 표시한 곳이 순서를 바꾸면 안 되는 자리다.

```
  Serial.begin
      │
  ★ wakeGate()          ← 제일 먼저. 깊은잠에서 깼나?
      │                    · 붙들어 둔 핀을 푼다 (안 풀면 센서가 영영 안 켜진다)
      │                    · 5초를 세고, 못 채우면 도로 잠든다
      │                    · 플래시에 쓰기 전이어야 한다 (헛깰 때마다 닳는다)
      │                    · 워치독을 걸기 전이어야 한다 (5초 세다 물린다)
      ▼
  reportResetReason()    지난번에 왜 꺼졌나. RTC 영역에 남아 있다
  watchdogBegin()        30초 동안 소식 없으면 스스로 재시작
      │
  loadSettings()         NVS 에서 이름·번호·주기·전원핀
      │
  ★ applySensorPower()  GPIO14 를 HIGH 로. 이게 없으면 GPS 가 통째로 죽어 있다
      │
  배터리 ADC             깼으면 값이 찰 때까지 기다린다 (POWER.md §4)
      │
  hlog::begin()          링버퍼 잡고 코어 0 에 쓰기 일꾼을 띄운다
      │
  gpsBegin()             9600 과 115200 둘 다로 설정을 보낸다
  buttonBegin()          눌린 채면 뗄 때까지 안 본다 (안 그러면 켜자마자 도로 꺼진다)
  imuBegin()             ★ 여기서 Wire.begin 을 한다
  ★ displayBegin()      Wire.begin 뒤여야 한다 (U8g2 가 핀을 OUTPUT 으로 바꿔 버린다)
  lora::begin()          칩 버그 셋을 걸고 코어 0 에 받기 일꾼을 띄운다
  BLE 시작
      │
  이어서 기록할까?        지난번이 그냥 끊겼으면 (NVS 표식)
      ▼
    loop()
```

---

## 4. 도는 동안 — loop()

한 바퀴 끝에 `delay(2)` 가 있다. 나머지는 "지난번에서 얼마나 지났나" 로 갈린다.

```
  ┌─▶ loop() 시작
  │      │
  │   매 바퀴   lora::pump()      링버퍼 비우기. 안 하면 코어 0 이 버리기 시작한다
  │            pollSerial()      명령 받기
  │            netsrv::poll()    WiFi 요청 + 끌 때 됐나
  │            gpsPoll()         쉬지 않고 읽는다. UART 버퍼가 넘치면 문장이 잘린다
  │            buttonPoll()      짧게=마킹 · 2초=기록 · 5초=끄기
  │      │
  │   10 ms    IMU FIFO 퍼오기        칩이 100 Hz 로 뜬다. 우리는 퍼오기만
  │   100 ms   IMU + 자력계           화면과 BLE 가 쓰는 값
  │   100 ms   BLE notify             10 Hz. `hz` 명령으로 바꾼다
  │   250 ms   화면 그리기            ★ 기록 중에는 1000 ms 로 늦춘다
  │      │                              (한 장에 25 ms 걸린다. SD 에 자리를 내준다)
  │   1 s      배터리 · 센서 붙었나 · 광고 갱신 · 시리얼 로그
  │   10 s     텍스트 사본 한 줄
  │      │
  │   feedWatchdog()   여기까지 왔으면 살아 있다는 뜻
  │   delay(2)
  └──────┘
```

**주기를 다르게 둔 이유는 하나다. 한 바퀴가 25 ms 를 넘으면 안 된다.**
로라 차례가 25 ms 간격이라서다. 그래서 오래 걸리는 일을 뜸하게 나눠 놓았다.

---

## 5. 값이 어디서 와서 어디로 가나

```
  GPS ──┐
        ├──▶ gLatest ──┬──▶ BLE 알림      10 Hz   아이폰 · 워치
  IMU ──┤   (지금 값)   ├──▶ 화면          4 Hz    보드 위 OLED
        │              ├──▶ 로라           차례마다  다른 배
  ADC ──┘              ├──▶ 시리얼 로그    1 Hz    사람이 본다
                       │
                       └──▶ hlog ──▶ 링버퍼 ──▶ SD 카드
                                                  HLG 바이너리 (원본)
                                                  TXT 사본 (10초에 한 줄)
```

**★ 카드에는 다듬기 전 원본만 적는다.**
화면과 앱에 보이는 속도는 다듬고 잡음 바닥 0.1노트를 씌운 값이다. 그걸 저장하면
원본을 못 되살린다. 잡음 바닥 때문에 진짜 저속 데이터가 조용히 0 이 되어 버린다.

**★ 값이 없으면 없다고 그린다.** 위성을 못 잡았으면 `--.--` 를 쓴다.
0 을 쓰면 "멈춰 있다" 로 읽혀서 배 위에서 잘못 판단한다.

---

## 6. 끄고 켜기

버튼 하나가 셋을 한다. 자세한 것은 `POWER.md`.

```
     꺼져 있을 때                        켜져 있을 때
     ───────────                        ───────────
     버튼 누름                           짧게(~1초)   마킹
        │  (칩이 그 순간 깬다)           2초 이상     기록 시작/종료
        ▼                                5초 이상     끄기
     5초 세기 ── 못 채움 ──▶ 도로 잠
        │
      채움
        ▼
     정상 부팅
```

**끄는 순서를 바꾸면 안 된다.**

```
  1) hlog::stop()      기록을 먼저 닫는다. 카드 전원 내리기 전에 끝나야 한다
  2) SD.end()          SPI 를 놓는다
  3) lora::sleep()     무전기 (모듈 안이라 3V3_S 밖이다)
  4) gImu.sleep(true)  IMU  (VDD 라 3V3_S 밖이다)
  5) displayOff()      화면 (VDD 라 3V3_S 밖이다)
  6) 손 뗄 때까지 기다림   ← 빼먹으면 잠들자마자 다시 깬다
  7) 3V3_S 내리고 붙들기 · SD 칩셀렉트 HIGH 로 붙들기 · 버튼 풀업 살리기
```

---

## 7. 고장 나도 계속 도는 것들

배 위에서는 하나가 죽었다고 통째로 멈추면 안 된다.

| 죽는 것 | 그러면 | 나머지는 |
|---|---|---|
| 화면 안 보임 | 그리기만 멈춘다. 다시 꽂으면 붙는다 | 계속 돈다 |
| IMU 없음 | 힐을 시뮬레이터 값으로 채우고 (SIM) 표시 | 계속 돈다 |
| 카드 빠짐 | 기록을 멈춘다 | 계속 돈다 |
| 카드 쓰기 실패 | 스스로 멈추고 이어시작 표시를 지운다 | 계속 돈다 |
| 무전기 실패 | 조용히 지나간다 | 계속 돈다 |
| 루프가 멈춤 | 워치독이 30초 뒤 재시작 | 다시 켜진다 |
| 전원 갑자기 끊김 | NVS 표식이 1 로 남는다 | 켤 때 이어서 기록 |

---

## 8. 어느 파일에 무엇이 있나

| 파일 | 줄 | 맡은 일 |
|---|---|---|
| `src/main.cpp` | 4800 | setup·loop·GPS·IMU·BLE·버튼·잠들기·시리얼 명령 |
| `src/hlog.cpp` | 1100 | 기록. 링버퍼와 코어 0 쓰기 일꾼, 되읽어 검사 |
| `src/netsrv.cpp` | 1100 | WiFi 서버. 파일 보내기, 저절로 끄기 |
| `src/lora.cpp` | 300 | SX1262 설정, 코어 0 받기 일꾼 |
| `src/display_rak.cpp` | 350 | OLED 그리기 |
| `include/board_rak.h` | 280 | 핀 번호와 그 근거. **여기가 실측 기록이다** |
| `include/protocol.h` | 280 | BLE·로라 짐 모양. ★ 사본이 넷이라 같이 고쳐야 한다 |

★ `protocol.h` 는 `firmware/`, `firmware-rak/`, `app/Shared/Protocol.swift`,
데스크탑까지 네 벌이 있다. 하나만 고치면 조용히 어긋난다.
