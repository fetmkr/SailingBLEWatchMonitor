# 이 저장소에서 일하는 법

**이 파일은 세션마다 통째로 읽힌다.** 메모리 본문과 다르다 — 메모리는 목록의
한 줄만 오고 본문은 내가 열어야 온다. 그래서 **다시 밟으면 안 되는 것**을 여기 둔다.

---

## 시작할 때 — 세 가지를 먼저 편다

1. `REQUIREMENTS.md` — 넷(보드·아이폰·워치·데스크탑)이 어디까지 왔나
2. `NEXT.md` — 다음 할 일과 **돌고 있는 시험**
3. 손댈 영역의 문서 — `POWER.md` · `GPS.md` · `SDLOG.md` · `PROTOCOL.md` ·
   `TRANSFER.md` · `ARCHITECTURE.md` · `COMMANDS.md` · `docs/imu/HEADING.md`

**★ 어떤 값을 쓰기 전에 그 값에 붙은 제약을 먼저 확인한다.**
실제로 "깬 직후 배터리 값은 못 믿는다" 를 문서에 적어 놓고 **다음 날 그 값 위에
13시간짜리 측정을 두 번 쌓았다.** 이틀을 날렸다.

---

## 절대 어기면 안 되는 것

### 비밀은 올라가면 안 된다

WiFi AP 이름과 암호는 커밋에 들어가면 안 된다. `secrets.h` 는 gitignore 되어 있다.

**푸시 전에 반드시 센다. 0 이 아니면 멈춘다.**

찾을 낱말은 `firmware-rak/include/secrets.h` 에 있는 실제 값 두 개다.
**이 파일에 그 값을 적지 않는다** — 적는 순간 이 파일이 유출이 된다.
(실제로 이 문단을 처음 쓸 때 예시랍시고 적었다가 검사에 걸렸다.)

    ssid=$(grep -oE '"[^"]+"' firmware-rak/include/secrets.h | head -2 | tr -d '"' | paste -sd'|' -)
    git diff --cached | grep "^+" | grep -cE "$ssid"        # 0 이어야 함
    git log origin/main..HEAD -p | grep "^+" | grep -cE "$ssid"

### 사용자 맥의 WiFi 를 갈아타지 않는다

보드 확인은 **USB 시리얼로만** 한다.

### 값이 없으면 없다고 보여준다

지어낸 값도 0 도 쓰지 않는다. 위성을 못 잡으면 `--.--` 다.
0 을 쓰면 "멈춰 있다" 로 읽혀서 바다에서 잘못 판단한다.

### ★ 못 믿는 값은 문서에 적지 말고 코드에서 없앤다

적어두면 **내가 또 쓴다.** 실제로 그랬다.

    제일 셈    코드가 막는다 (변수를 지운다)
    중간       출력이 말한다 (`--` 를 찍는다, 검사가 "못 쓴다" 고 한다)
    제일 약함  문서와 메모리 (읽어야 작동한다)

### ★ 한 번 실패했다고 영영 멈추지 않는다 — 다시 해 보고, 멈추면 크게 알린다

기록이 이 장비의 본체다. 옛 코드는 카드 쓰기가 **한 번** 덜 들어가면 그 자리에서
기록을 끝냈다. 화면에도 앱에도 아무 표시가 없었다. 세션 27(29분)·46(2시간 53분)이
그렇게 끊겨서 나머지 항해를 잃었다 (2026-09-13 에 찾음).

    다시 해 본다      정해진 횟수만큼 (쓰기는 3번)
    그래도 안 되면    이유를 **저장**하고, 새로 다시 건다 (횟수 한도)
    멈춰 있는 동안    보드 화면 · 폰 · 워치에 보이게 (REC FAIL, 배경 빨강)

설계 문서(SDLOG.md)에는 "3번 다시 해 본다" 고 적혀 있었는데 코드는 안 그랬다.
**문서에 적은 동작이 코드에 있는지 확인한다.**

### 되던 코드는 건드리지 않는다

실기기에서 검증된 코드는 중복이 있어도 그대로 둔다.
새 방식은 **나란히 놓고 둘 다 기록**한 뒤, 물 위에서 대조하고 나서 바꾼다.
지금 그렇게 돌고 있는 것 — 속도(도플러 vs 위치차분), 방위(평평 vs 기울기보정).

---

## ★ 콜백에서 일하지 않는다 — 표식만 세우고 루프가 한다

**BLE 콜백은 `nimble_host` 작업에서 돈다. 그 스택은 5120 바이트다.**
거기서 명령을 처리하다 보드가 죽었다 — `magcal stop` 을 BLE 로 받는 순간
`PANIC`. 같은 명령이 시리얼로는 멀쩡했다. loop 스택은 8192 라서다.
[확인: 2026-09-11 코어덤프 — Crashed task 'nimble_host']

스택 말고 이유가 하나 더 있다. **다른 작업에서 같은 값을 만지면 언젠가 어긋난다.**

    콜백이 하는 일    줄을 베껴 담는다 · 표식을 세운다 · digitalWrite · 로그 한 줄
    루프가 하는 일    그걸 꺼내서 처리한다

이 저장소는 원래 그렇게 짜여 있었다. `onConnect` 는 `gAdvNeedsApply` 만 세우고,
`onWifiEvent` 는 `gGoneAt` 만 적고, 로라 ISR 은 세마포어만 준다.
**내가 새로 넣은 제어 콜백만 혼자 어긋나 있었다.**

같은 규칙이 스택 전체에 걸린다 — [[esp32-stack-and-leak-rules]].
지역 배열에 KB 단위를 올리지 않는다. 올리려면 `static` 을 붙인다.

## 재는 법

### 실제 에러·로그를 보기 전에 코드를 고치지 않는다

증상 → **실제 값** → 가설 → 수정. 두 번째를 건너뛰지 않는다.

### 숫자를 내는 것을 만들면 답을 아는 경우를 먼저 통과시킨다

오늘 틀린 것들의 공통점이다. **"돌아간다" 를 "맞다" 로 착각했다.**

    기울기 보정 식   72가지 자세를 컴퓨터에서 돌려보고 넣었다   → 한 번에 맞았다
    공 맞추기        아는 치우침을 넣고 되찾는지 봤다           → 한 번에 맞았다
    깬 뒤 전압       숫자가 나오길래 통과시켰다                 → 이틀 날림
    자이로 적분      숫자가 나오길래 통과시켰다                 → 값이 통째로 틀림

### 재는 자리는 값이 오는 자리여야 한다

진단 명령 안에서 4 Hz 로 자이로를 적분했다가 값이 통째로 틀렸다.
`imuDrainFifo` 가 100 Hz 벌을 꺼내는 그 자리로 옮기니 맞았다.

### 시리얼 명령이 루프를 몇 초 붙잡으면 센서가 언다

FIFO 를 아무도 안 비운다. 여러 초 도는 명령 안에서는 `imuDrainFifo()` 를 직접 부른다.

### ★ 이유는 램에 두지 않는다 — 시리얼을 열면 보드가 리셋된다

맥에서 시리얼 포트를 여는 순간 보드가 다시 켜진다. 램에 있던 것은 다 날아간다.
기록이 멈춘 이유(`gLastError`)가 램에만 있었는데, USB 로 붙자마자 내가 포트를 열어
**증거를 내 손으로 지웠다** (2026-09-13).

- 멈춤·실패·죽음의 이유는 **NVS 나 카드에** 적는다. 램 변수로 끝내지 않는다.
- 켤 때 그 기록을 로그로 찍고, 명령(`rec`)으로 다시 볼 수 있게 한다.
- 무엇이 잘못됐다는 말을 들으면, 포트를 열기 전에 **무엇이 리셋에 살아남나**부터 따진다.

### ★ 껐다 켜도 남는 표시를 건드리면, 끄고 켜는 길까지 시험한다

NVS 표시(`rec_on` 등)는 재부팅을 넘어 살아남는다. 그걸 세우거나 지우는 줄을 바꾸면
**그 표시를 읽고 쓰는 곳을 전부 표로 적고**, 아래를 다 돌려 본다.

    지금 켜진 상태에서      → 내가 만든 길 (보통 이것만 시험한다)
    그 뒤 재부팅            → 켤 때 무엇이 저절로 시작되나
    그 뒤 단추로 끄고 켜기   → 사람이 끈 것과 끊긴 것이 구분되나

실제로 어겼다. 쓰기 실패 때 표시 지우는 줄을 뺐고, "다시 걸기" 만 시험했다.
다음 날 장비를 켜자 **느닷없이 REC 가 떴다** (세션 58 이 시험 세션 57 을 이어받음).

### 시험으로 만든 기록은 표시하고, 끝나면 지운다

`rec fail <n>` 같은 흉내 실패는 기록에 `(시험)` 이 붙는다. 끝나면 `rec fail clear`.
안 지우면 다음 세션의 나는 그걸 진짜 실패로 읽는다.

### 끝점 두 개로는 "한 번 꺾인 것" 과 "계속 새는 것" 을 못 가른다

둘 다 "떨어졌다" 로만 보인다. 점을 여럿 찍어야 갈린다.

### "내 시험 방법 탓이다" 가 나오면 거기서 멈추지 않는다

그럼 원래 자리가 맞나를 이어서 본다. 워치 세션이 그렇게 드러났다 —
`onAppear` 에서 걸고 있었고, 그건 앱이 앞에 나오기 전이라 watchOS 가 막는다.

---

## 이 배의 값에 대해

### 방위를 COG 에 맞추지 않는다 — leeway 를 지운다

요트는 뱃머리와 가는 방향이 원래 다르다. **그 차이가 우리가 보려는 값이다.**
고치는 순서가 있다.

    1 축 맞추기      기하학          → COG 안 씀
    2 기울기 보정     가속도          → COG 안 씀
    3 치우침 빼기     자기장 세기      → COG 안 씀
    4 돌아앉은 각     상수 하나       → 여기서만 COG. 태킹 고르게 한 자료로

### ★ 분석 앱이다 — 식과 보정을 몰래 넣지 않는다

사용자: "과학적인 데이터 분석용 앱인데 맘대로 공식을 적용해?" (2026-09-15)
실제로 방위 식이 셋(OLED · 앱 HDG · 앱 comp)으로 갈라져 있었고, 앱은 자력 치우침을 스스로 구해
"좋아 보이면" 뺐고, 뱃머리를 보드 X 로 가정했다. 앱 HDG 는 COG 와 흩어짐 49.5° 였다.

    원본          손대지 않고 저장·표시
    계산값        식 이름 + 쓴 보정값 + 그 값을 언제 어떻게 구했나 를 같이 보여준다
    보정값        사람이 정한 절차로만 들어간다. 앱·보드가 "좋아 보여서" 넣지 않는다
    식            한 곳에서 정의. 보드와 앱이 같은 입력에 같은 출력인지 시험한다
    식을 바꾸기    내가 정하지 않는다. 근거와 데이터를 보이고 묻는다

제안하기 전에 그 방법이 코드 주석·문서에 **이미 틀렸다고 적혀 있나** 찾는다
(세션 중력 평균을 수평으로 삼기 — `desktop/src/main.ts` 에 틀렸다고 적혀 있는데 또 제안했다).

### 카드에는 다듬기 전 원본만 적는다

화면에 보이는 속도는 다듬고 잡음 바닥을 씌운 값이다. 그걸 저장하면 못 되살린다.

### GPS 는 저속에서 옛 값을 들고 있다

속도는 1노트, 침로는 2노트 밑에서 얼어붙는다. 우리 탓이 아니다.

### GPS 는 선박 모드(4)를 코드에 박았다 — 저장소에 기대지 않는다

NVS 에 적어 두는 방식이 두 번 조용히 비었다. 그때마다 휴대 모드(0)로 떠서
1노트 밑 속도가 통째로 0 이 됐다. `kGpsBoatMode`. 화면 전압 옆에 모드가 늘 뜬다.

### 속도는 숫자를 평균하지 않는다 — 북·동 성분을 평균한다

선박 모드는 멈춰 있어도 0.2~0.6 kn 가 뜬다 (칩이 밝힌 오차 ±0.35 안쪽).
속도 숫자는 늘 0 이상이라 30초 평균해도 0.23 에 머문다. 성분을 따로 평균하면 0.05 로 간다.
칩이 밝힌 속도 오차가 1 kn 넘는 표본은 버린다 (멈춰 있는데 20~28 kn 가 튄 적이 있다).
자세한 숫자와 근거는 GPS.md 끝.

---

## 손댈 때

### 넷을 같이 본다

기능 하나를 만들면 `REQUIREMENTS.md` 의 그 줄을 고친다.
**넷 중 하나만 했으면 나머지는 ❌ 로 남긴다.** 커밋 메시지에 번호를 적는다.

    magcal — 자력계 치우침을 뺀다  (D3, B4)

### 프로토콜 사본은 네 벌이다

`firmware/include/protocol.h` · `firmware-rak/include/protocol.h` ·
`app/Shared/Protocol.swift` · `desktop/src/protocol.ts`.
하나만 고치면 **에러 없이 조용히 어긋난다.**

### 아이패드와 데스크탑은 한 벌이다

둘 중 하나에만 해당하는 게 아니면 **늘 같이 바꾼다.**

### 단추를 잠그지 않는다 — 눌리게 두고 이유를 말한다

`.disabled()` 로 잠가 놓고 그 안에 "왜 안 되는지" 를 넣으면 그 말이 절대 안 나온다.
눌러도 아무 일이 없어서 사람은 고장으로 본다. 실제로 그렇게 만들었다.

### 누르는 자리는 44

워치·아이패드 기준이다. 글자를 키우는 게 아니라 **눌리는 넓이**를 키운다
(`minHeight: 44` + `contentShape`). [[tap-target-audit]]

### 스크립트로 큰 덩어리를 끼워 넣었으면 위아래를 본다

`@ViewBuilder` 가 엉뚱한 함수에 붙은 적이 있다. 앞뒤 몇 줄을 보면 막힌다.

### 메모리 본문을 고치면 목록 줄도 같이 고친다

목록의 한 줄이 내가 보는 전부다. 본문만 고치면 다음 세션의 나는 틀린 것을
사실로 알고 시작한다. 실제로 "워치는 Xcode 로만 된다" 가 8일 동안 그랬다.

---

## 배포

보드·아이폰·워치 **셋 다 명령줄로 된다.**

    pio run -t upload                                   보드
    xcrun devicectl device install app --device <id> …  아이폰·워치

워치에서 `CoreDeviceError 3002` 나 `4000` 이 나면 **한 번 더 하면 된다.**
한 번 보고 "안 된다" 고 단정한 적이 있다. 아이폰도 설치가 몇 번 연달아 실패했다가
다음 번에 들어간 적이 있다 (2026-09-13). 실패하면 **오류 원문을 그대로 찍어서** 본다.

---

## 나머지

메모리 45개에 더 있다. 그 영역을 손대기 전에 해당하는 것을 연다.
목록은 `MEMORY.md`.
