# 이 저장소에서 일하는 법

**이 파일은 세션마다 통째로 읽힌다.** 메모리 본문과 다르다 — 메모리는 목록의
한 줄만 오고 본문은 내가 열어야 온다. 그래서 **다시 밟으면 안 되는 것**을 여기 둔다.

---

## 시작할 때 — 세 가지를 먼저 편다

1. `REQUIREMENTS.md` — 넷(보드·아이폰·워치·데스크탑)이 어디까지 왔나
2. `NEXT.md` — 다음 할 일과 **돌고 있는 시험**
3. 손댈 영역의 문서 — `POWER.md` · `GPS.md` · `SDLOG.md` · `PROTOCOL.md` ·
   `TRANSFER.md` · `ARCHITECTURE.md` · `COMMANDS.md` · `docs/imu/HEADING.md`

**★ 어떤 값을 쓰기 전에 그 값에 붙은 제약을 먼저 확인한다.**
실제로 "깬 직후 배터리 값은 못 믿는다" 를 문서에 적어 놓고 **다음 날 그 값 위에
13시간짜리 측정을 두 번 쌓았다.** 이틀을 날렸다.

---

## 절대 어기면 안 되는 것

### 비밀은 올라가면 안 된다

WiFi AP 이름과 암호는 커밋에 들어가면 안 된다. `secrets.h` 는 gitignore 되어 있다.

**푸시 전에 반드시 센다. 0 이 아니면 멈춘다.**

찾을 낱말은 `firmware-rak/include/secrets.h` 에 있는 실제 값 두 개다.
**이 파일에 그 값을 적지 않는다** — 적는 순간 이 파일이 유출이 된다.
(실제로 이 문단을 처음 쓸 때 예시랍시고 적었다가 검사에 걸렸다.)

    ssid=$(grep -oE '"[^"]+"' firmware-rak/include/secrets.h | head -2 | tr -d '"' | paste -sd'|' -)
    git diff --cached | grep "^+" | grep -cE "$ssid"        # 0 이어야 함
    git log origin/main..HEAD -p | grep "^+" | grep -cE "$ssid"

### 사용자 맥의 WiFi 를 갈아타지 않는다

보드 확인은 **USB 시리얼로만** 한다.

### 값이 없으면 없다고 보여준다

지어낸 값도 0 도 쓰지 않는다. 위성을 못 잡으면 `--.--` 다.
0 을 쓰면 "멈춰 있다" 로 읽혀서 바다에서 잘못 판단한다.

### ★ 못 믿는 값은 문서에 적지 말고 코드에서 없앤다

적어두면 **내가 또 쓴다.** 실제로 그랬다.

    제일 셈    코드가 막는다 (변수를 지운다)
    중간       출력이 말한다 (`--` 를 찍는다, 검사가 "못 쓴다" 고 한다)
    제일 약함  문서와 메모리 (읽어야 작동한다)

### 되던 코드는 건드리지 않는다

실기기에서 검증된 코드는 중복이 있어도 그대로 둔다.
새 방식은 **나란히 놓고 둘 다 기록**한 뒤, 물 위에서 대조하고 나서 바꾼다.
지금 그렇게 돌고 있는 것 — 속도(도플러 vs 위치차분), 방위(평평 vs 기울기보정).

---

## 재는 법

### 실제 에러·로그를 보기 전에 코드를 고치지 않는다

증상 → **실제 값** → 가설 → 수정. 두 번째를 건너뛰지 않는다.

### 숫자를 내는 것을 만들면 답을 아는 경우를 먼저 통과시킨다

오늘 틀린 것들의 공통점이다. **"돌아간다" 를 "맞다" 로 착각했다.**

    기울기 보정 식   72가지 자세를 컴퓨터에서 돌려보고 넣었다   → 한 번에 맞았다
    공 맞추기        아는 치우침을 넣고 되찾는지 봤다           → 한 번에 맞았다
    깬 뒤 전압       숫자가 나오길래 통과시켰다                 → 이틀 날림
    자이로 적분      숫자가 나오길래 통과시켰다                 → 값이 통째로 틀림

### 재는 자리는 값이 오는 자리여야 한다

진단 명령 안에서 4 Hz 로 자이로를 적분했다가 값이 통째로 틀렸다.
`imuDrainFifo` 가 100 Hz 벌을 꺼내는 그 자리로 옮기니 맞았다.

### 시리얼 명령이 루프를 몇 초 붙잡으면 센서가 언다

FIFO 를 아무도 안 비운다. 여러 초 도는 명령 안에서는 `imuDrainFifo()` 를 직접 부른다.

### 끝점 두 개로는 "한 번 꺾인 것" 과 "계속 새는 것" 을 못 가른다

둘 다 "떨어졌다" 로만 보인다. 점을 여럿 찍어야 갈린다.

### "내 시험 방법 탓이다" 가 나오면 거기서 멈추지 않는다

그럼 원래 자리가 맞나를 이어서 본다. 워치 세션이 그렇게 드러났다 —
`onAppear` 에서 걸고 있었고, 그건 앱이 앞에 나오기 전이라 watchOS 가 막는다.

---

## 이 배의 값에 대해

### 방위를 COG 에 맞추지 않는다 — leeway 를 지운다

요트는 뱃머리와 가는 방향이 원래 다르다. **그 차이가 우리가 보려는 값이다.**
고치는 순서가 있다.

    1 축 맞추기      기하학          → COG 안 씀
    2 기울기 보정     가속도          → COG 안 씀
    3 치우침 빼기     자기장 세기      → COG 안 씀
    4 돌아앉은 각     상수 하나       → 여기서만 COG. 태킹 고르게 한 자료로

### 카드에는 다듬기 전 원본만 적는다

화면에 보이는 속도는 다듬고 잡음 바닥을 씌운 값이다. 그걸 저장하면 못 되살린다.

### GPS 는 저속에서 옛 값을 들고 있다

속도는 1노트, 침로는 2노트 밑에서 얼어붙는다. 우리 탓이 아니다.

---

## 손댈 때

### 넷을 같이 본다

기능 하나를 만들면 `REQUIREMENTS.md` 의 그 줄을 고친다.
**넷 중 하나만 했으면 나머지는 ❌ 로 남긴다.** 커밋 메시지에 번호를 적는다.

    magcal — 자력계 치우침을 뺀다  (D3, B4)

### 프로토콜 사본은 네 벌이다

`firmware/include/protocol.h` · `firmware-rak/include/protocol.h` ·
`app/Shared/Protocol.swift` · `desktop/src/protocol.ts`.
하나만 고치면 **에러 없이 조용히 어긋난다.**

### 아이패드와 데스크탑은 한 벌이다

둘 중 하나에만 해당하는 게 아니면 **늘 같이 바꾼다.**

### 스크립트로 큰 덩어리를 끼워 넣었으면 위아래를 본다

`@ViewBuilder` 가 엉뚱한 함수에 붙은 적이 있다. 앞뒤 몇 줄을 보면 막힌다.

### 메모리 본문을 고치면 목록 줄도 같이 고친다

목록의 한 줄이 내가 보는 전부다. 본문만 고치면 다음 세션의 나는 틀린 것을
사실로 알고 시작한다. 실제로 "워치는 Xcode 로만 된다" 가 8일 동안 그랬다.

---

## 배포

보드·아이폰·워치 **셋 다 명령줄로 된다.**

    pio run -t upload                                   보드
    xcrun devicectl device install app --device <id> …  아이폰·워치

워치에서 `CoreDeviceError 3002` 나 `4000` 이 나면 **한 번 더 하면 된다.**
한 번 보고 "안 된다" 고 단정한 적이 있다.

---

## 나머지

메모리 45개에 더 있다. 그 영역을 손대기 전에 해당하는 것을 연다.
목록은 `MEMORY.md`.
