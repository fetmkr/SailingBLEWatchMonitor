# ── PlatformIO ───────────────────────────────────────────────────────────
firmware/.pio/
firmware/.vscode/
firmware/.cache/
firmware/compile_commands.json
firmware/tools/sim_test

firmware-rak/.pio/
firmware-rak/.vscode/
firmware-rak/.cache/
firmware-rak/compile_commands.json

# ── 비밀 ─────────────────────────────────────────────────────────────────
# WiFi SSID/암호 같은 것. secrets.example.h 만 커밋한다.
firmware-rak/include/secrets.h
firmware/include/secrets.h

# ── Xcode ────────────────────────────────────────────────────────────────
# .xcodeproj 는 project.yml 에서 생성하므로 커밋하지 않는다
app/*.xcodeproj/
app/*.xcworkspace/
DerivedData/
*.xcuserstate
xcuserdata/
build/

# ── 검증 산출물 ──────────────────────────────────────────────────────────
.verify/

# ── Claude Code 로컬 설정 (머신별 퍼미션 — 공유하지 않음) ────────────────
.claude/settings.local.json

# ── macOS ────────────────────────────────────────────────────────────────
.DS_Store

# PlatformIO 가 자동으로 만드는 파일
firmware/src/idf_component.yml
firmware-rak/src/idf_component.yml

# 데스크탑 앱
desktop/node_modules/
desktop/dist/
desktop/src-tauri/target/
desktop/src-tauri/gen/
desktop/fixtures/
desktop/public/sample.HLG
