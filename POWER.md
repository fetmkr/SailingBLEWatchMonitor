# 보드 끄고 켜기 — 깊은잠

버튼을 5초 누르면 꺼지고, 5초 누르면 켜진다. 끄면 ESP32-S3 의 깊은잠으로 들어간다.

관련 코드는 `firmware-rak/src/main.cpp` 의 `goToSleep()` · `wakeGate()` · `armButtonWake()`.
재보는 명령은 `sleepstat` · `battboot` · `off [초]`.

---

## 1. 하루 만에 배터리가 빈 일 (2026-09-07)

처음 만든 것을 켜 두고 하루 조금 넘게 뒀더니 배터리가 바닥까지 닳았다.
자는 게 10 µA 라면 몇 달이 가야 한다. 뭔가 안 잔 것이었다.

**그런데 잠든 보드는 아무 말도 못 한다.** 깨어나면 램이 비어 있어서 무슨 일이
있었는지 알 방법이 없었다. 그래서 먼저 계측기를 만들었다.

- RTC 메모리에 잠든 횟수 · 헛깬 횟수 · 잠든 시각 · 그때 전압을 적는다.
  RTC 영역은 깊은잠에도 안 지워진다.
- 깨어나면 그 값으로 셈을 해서 `sleepstat` 이 뱉는다.
- 같은 내용을 SD 카드 `/SLEEP.TXT` 에도 붙인다. 리셋에도 안 없어진다.

한 번 재니 바로 나왔다.

```
잔 시간 570초       헛깸 445번
```

**1.3초에 한 번씩 스스로 깼다.** 한 번 깰 때마다 부팅에 1초 남짓 걸리니
보드는 자는 게 아니라 거의 계속 켜져 있었다.

### 왜 깼나

깊은잠에 들면 칩이 RTC 주변장치를 끈다. 그때 버튼 핀(GPIO2)에 걸어 둔
**내부 풀업도 같이 사라진다.** 풀업이 없어진 핀은 떠 있고, 뜬 핀은 LOW 로
읽힌다. 깨우는 조건을 "LOW 가 되면 깨어라" 로 걸어 놨으니 잠들자마자 그
조건이 참이 되어 다시 깼다.

헤더에 그 경고가 적혀 있었다.

> Internal pullups and pulldowns don't work when RTC peripherals are shut down.
> ... Alternatively, RTC peripherals (and pullups/pulldowns) may be kept
> enabled using esp_sleep_pd_config function.
> [확인: esp_sleep.h:298-301]

### 고친 법

자는 동안 RTC 주변장치만 켜 둔다. 그래야 풀업이 산다.

```c
esp_sleep_pd_config(ESP_PD_DOMAIN_RTC_PERIPH, ESP_PD_OPTION_ON);
```

그만큼 전류를 더 쓰지만, 1.3초마다 보드가 통째로 켜지는 것에 비하면 아무것도 아니다.

**확인.** 헛깸 445 → **0**. 300초·60초 시험 모두 0.

### 같이 나온 두 번째 실수

헛깨서 도로 자는 길에서 **깨우는 설정을 다시 걸지 않았다.** 그래서 시험용
타이머가 첫 헛깸에 사라져 보드가 영영 안 깨어났다.

지금은 잠드는 모든 길이 `armButtonWake()` 하나를 부른다. 그 함수 밖에서
직접 `esp_sleep_enable_*` 를 부르지 말 것.

---

## 2. 어느 부품이 어느 전원에 물려 있나

**센서 전원 스위치(3V3_S)는 생각보다 적게 끈다.** 문서만 보고는 안 갈려서 쟀다.

| 부품 | 전원 | 3V3_S 를 내리면 | 재울 때 해야 할 일 |
|---|---|---|---|
| GPS RAK12501 | 3V3_S | 꺼진다 | 없음 |
| 화면 RAK1921 | **VDD** | 안 꺼진다 | `displayOff()` |
| IMU RAK1905 | **VDD** | 안 꺼진다 | `gImu.sleep(true)` |
| SD RAK15002 | **VDD** | 안 꺼진다 | `SD.end()` |
| 무전기 SX1262 | 모듈 안 | 안 꺼진다 | `lora::sleep()` |

재는 법 — `power off` 를 친 뒤에

```
gps    5초 동안 한 바이트도 안 오면 3V3_S 다
scan   0x3C / 0x68 이 그대로 보이면 VDD 다
sd     카드를 찾고 글씨까지 써지면 VDD 다
```

`board_rak.h` 에 "전원을 끄면 화면도 GPS 도 IMU 도 한꺼번에 안 보인다" 고
적혀 있었는데 **틀렸다.** 화면과 IMU 는 VDD 다. 고쳤다.

---

## 3. 끄는 순서 — 바꾸면 안 된다

```
1) hlog::stop()        기록을 먼저 닫는다. 카드 전원 내리기 전에 끝나야 한다
1b) SD.end()           SPI 를 놓아준다
2) lora::sleep()       무전기
3) gImu.sleep(true)    IMU
4) displayOff()        화면
5) 손을 뗄 때까지 기다림
6) 3V3_S 를 LOW 로 내리고 rtc_gpio_hold_en 으로 붙든다
```

**5번을 빼먹으면 안 된다.** 버튼이 눌린 채로 자면 깨우는 조건이 이미 참이라
잠들자마자 다시 깬다. 10초가 지나도 안 떨어지면 **끄지 않는다.** 물이 접점을
붙들고 있는 상황인데, 그때는 켜져 있는 쪽이 안전하다.

**깨어난 뒤에는 붙든 것을 반드시 푼다** (`wakeGate()`).
안 풀면 `applySensorPower` 가 HIGH 를 써도 패드가 안 움직인다.
로그에는 "센서 전원 ON" 이 찍히는데 GPS 와 SD 는 죽어 있는, 보드가
거짓말하는 상태가 된다.

---

## 4. 깬 직후 배터리 값이 낮게 나온다

깬 직후 첫 순간에 재면 3.58 V 짜리 배터리가 1.96 V 로 잡힌다.
방전 곡선으로는 0% 다. 바다에서 멀쩡한 배터리를 다 됐다고 판단하게 된다.

분압 저항이 1 MΩ + 1.5 MΩ 이라 ADC 가 보는 저항이 2.5 MΩ 이다.
자는 동안 분압 마디가 비었다가 다시 차는 것으로 보인다.

곡선을 쟀다 (`battboot`).

```
보통 부팅        처음부터 2133 mV. 맞다
깨어난 직후      setup 안에서는 1178 mV
켠 지 1초        2145 mV. 이때부터 평평하다
```

그래서 **깼을 때 전압은 loop 가 1초 뒤에 잰 값을 쓴다.** setup 안에서 잰 값은
안 쓴다.

---

## 5. 남은 새는 곳 — SD 카드로 의심된다

고친 뒤 10.5시간 재우고 나온 값이다.

    잔 시간 37691초(10.47시간)   헛깸 0
    3511 mV → 3479 mV           시간당 3.1 mV/h

배터리 1950 mAh 기준으로 10.47시간에 0.7%, 즉 **약 1.3 mA** 다.
목표 10 µA 와 두 자릿수 차이가 난다. 아직 뭔가 새고 있다.

**SD 카드가 제일 의심스럽다.** 카드는 VDD 라 전원을 못 끊고, 대기 전류가
카드마다 0.2~1 mA 다. 자릿수가 맞는다.

### 소프트웨어로 카드를 끄는 방법은 없다

  · SPI 모드에는 카드를 재우는 명령이 없다. CMD15(GO_INACTIVE_STATE)가
    SPI 에서 "No" 다 [확인: docs/sd/SD_Physical_Layer_...txt §7 명령표]
  · SD.end() 가 CMD0(GO_IDLE_STATE)를 보내는 것이 닿을 수 있는 제일 깊은 자리다
    [확인: framework-arduinoespressif32 SD/src/sd_diskio.cpp 의 sdcard_uninit]
  · RAK15002 데이터시트의 공급 전류는 "TBD" 로 비어 있다

### 그래서 한 것 — SPI 선을 눕히고 잔다 (2026-09-08)

SD.end() 는 **핀을 안 놓아준다.** 깊은잠에 들면 패드가 놓여서 뜬다.
뜬 칩셀렉트가 LOW 로 읽히면 카드는 선택된 채로 남아 계속 깨어 있는다.

칩셀렉트에 바깥 풀업이 없는 것을 쟀다.

    pin 12  →  풀업 HIGH · 풀다운 LOW  → 비어 있음

규격서도 같은 취지를 말한다. §6.4.2 —
"DAT, CMD, and CLK should be disconnected or driven to logical 0 by the host
 to avoid a situation that the operating current is drawn through the signal lines."

그래서 자는 동안 이렇게 붙든다.

    칩셀렉트 GPIO12   HIGH   카드를 확실히 떼어 놓는다. 이게 핵심
    클럭     GPIO13   LOW
    MOSI     GPIO11   LOW
    MISO     GPIO10   건드리지 않는다 (카드가 모는 선이다)

GPIO12 는 S3 의 strapping 핀이 아니라 붙들어도 부팅에 지장이 없다.
깰 때는 wakeGate 가 같이 풀어준다.

**카드가 그대로 되는지 확인했다.** 45초 재우고 깨운 뒤
  · `sd` — 카드 찾음, 122112 MB, 쓰기 OK
  · 실제 세션 — NAV 70줄 / IMU 694줄, 버린 줄 0, 최대 멈춤 7 ms

**아직 전류가 줄었는지는 안 쟀다.** 같은 10시간 시험을 다시 돌려야 안다.
그래도 안 줄면 카드를 빼고 재서 카드가 범인인지 가른다.

## 6. 회로도를 보고 알아낸 것 (2026-09-09)

[확인: RAK19007 datasheet 의 Figure 15 전원 회로도 그림 파일]

### 배터리 분압에 콘덴서가 없다

    VBAT ──[R3 1M]──┬── ADC_VBAT ── 코어 핀21 (GPIO1)
                    └──[R4 1.5M]── GND

**콘덴서도 스위치도 없다.** 그래서 두 가지가 확정된다.

  · "깬 뒤 값이 낮은 것은 콘덴서가 차느라 그렇다" 는 내 짐작은 **틀렸다.**
    (실측으로 이미 아니었는데 회로도가 못을 박았다)
  · 이 분압은 **끌 수가 없다.** 3.6 V 에서 늘 1.4 µA 가 흐른다.

### 전원 갈아타기 — 잘 만든 회로다

    USB (VBUS_D) ──[D8 MBR140SFT1G 쇼트키]──┬── VCC ──[U3 SGM6036-ADJ]── 3V3
                                             │
    배터리 (VBAT) ──[Q3 NCE40P05Y P채널]────┘
                        게이트: VBUS_D 와 R9 4.7K 로 GND 사이

    USB 없음 →  게이트가 GND 로 끌린다 → P채널 켜짐 → 배터리가 mΩ 로 직결
    USB 있음 →  게이트가 5 V         → P채널 꺼짐 → 배터리는 떨어지고 USB 가 전원

**배터리는 다이오드를 안 거친다.** 전압 손실이 없다. 전원 두 개를 손실 없이
갈아타는 표준 방식이다.

★ 처음에 이 그림을 반대로 읽고 "배터리가 쇼트키를 건너서 간다" 고 적었다.
  틀렸다. 쇼트키는 USB 쪽에 있다. 확대해서 다시 보고 고쳤다 (2026-09-09).

### ★ 그래도 전압이 낮으면 나빠진다 — 이유는 더 단순하다

    배터리    4.2 V ──────────────▶ 3.0 V
    만들 값                 3.3 V

**강압형(buck)은 내리기만 한다.** 배터리가 3.3 V 밑으로 내려가면 만들 수가 없다.
3.4 V 근처면 여유가 100 mV 뿐이라 제대로 조절을 못 하고, 그 언저리에서
대기 전류가 나빠진다.

RAK 포럼에도 같은 증상이 있다. "3.4 V 밑으로 내려가면 전류가 올라가고,
3.6 V 위에서는 25 µA 가 나왔다"
[확인: forum.rakwireless.com/t/where-is-the-leakage-rak19007/14034]

**우리가 잰 129 µA 는 전부 3.48~3.55 V 에서 잰 값이다. 딱 그 나쁜 구간이다.**
다시 재려면 배터리를 3.8 V 위로 올려 놓고 해야 한다.

### 왜 이렇게 설계했나

셋 중에 골라야 했다.

    강압형 (지금)  싸고 작고 배터리가 높을 때 효율이 좋다 / 3.4 V 밑을 포기
    LDO           제일 단순하다 / 4.2 V 에서 21% 를 열로 버린다
    승강압형       3.0 V 까지 지킨다 / 비싸고 크고 가벼운 부하에서 효율이 떨어진다

RAK 은 흔한 경우를 최적화했다. 리튬폴리머는 수명의 대부분을 3.6~4.2 V 에서
보낸다. 바닥 근처의 짧은 구간을 포기한 것이다.

EN 을 VCC 에 묶어 둔 것도 이유가 있다. 이 판에는 "시스템 꺼짐" 이라는 상태가
없다. MCU 의 깊은잠이 곧 꺼짐이라 레귤레이터를 끌 이유가 없다.

배터리 분압을 늘 붙여 둔 것도 같다. 스위치를 달면 핀과 부품이 하나씩 더 드는데
1.4 µA 는 자기네 목표 10 µA 안에 들어간다.

**진짜 문제는 설계가 아니라 안 적어 둔 것이다.** 데이터시트는 "10 µA 미만" 이라고만
하고 **어느 전압에서인지를 안 적었다.** 그런데 리튬폴리머는 마지막 구간을
그 밑에서 보낸다.

### 나머지는 작다

    레귤레이터 되먹임 R16 1M + R19 800K + R18 1.5M = 3.3 MΩ  →  약 1 µA
    SGM6036 자체 대기 전류                                   →  450 nA
    EN 은 R8 4.7K 로 VCC 에 묶여 있다 — 끌 수 없다
    초록·파랑 LED 는 DTC043ZEBTL 로 몰아서 핀이 LOW 면 안 흐른다
    빨강 LED 는 VBUS_D 쪽이라 USB 를 꽂았을 때만 켜진다

3V3_S 스위치는 우리가 실측한 그대로다. Q7(NCE40P05Y P채널)을 Q8 이 몰고,
Q8 은 IO2 가 몬다.

RAK3312 판은 모듈에 커넥터와 콘덴서 둘(C2 4.7µF, C5 100nF)만 붙인 것이라
샐 자리가 없다.

---

## 7. 잠자기 전류 재는 법 — `drain`

### 왜 만들었나

끝점 두 개만 재면 **표면 전압과 진짜 방전을 못 가른다.** 충전기를 뗀 배터리는
표면에 전압이 떠 있고 수십 분에 걸쳐 저절로 빠진다. 크기가 30 mV 라 우리
신호와 같은 자리다.

실제로 그것 때문에 같은 보드 같은 코드가 세 번 다 다르게 나왔다.

    10.47시간  32 mV  →  3.06 mV/h
    14.41시간  35 mV  →  2.43 mV/h
     2.46시간  31 mV  → 12.60 mV/h

낙차는 셋 다 30 mV 언저리인데 시간만 달랐다. 셋을
`낙차 = D(한 번 꺾임) + r × 시간` 으로 맞춰 보면

    D = 29.9 mV,  r = 0.304 mV/h,  남은 오차 ±1.1 mV (ADC 잡음 안)

즉 낙차의 대부분이 방전이 아니었다.

### 쓰는 법

    drain        13시간 동안 15번 찍는다. 앞은 촘촘히, 뒤는 성기게
    drain fast   같은 길을 초 단위로. 13분이면 끝난다 (코드 확인용)
    drainstat    표를 본다

★ **USB 를 뽑고 30분쯤 쉬게 한 뒤에 시작해야 한다.** 갓 충전한 배터리는
표면 전압 때문에 방전처럼 보인다.

### 깨자마자 잰 값이 낮은 문제 — 아직 원인 모름

깨어난 직후에는 배터리가 3.65 V 인데 1.98 V 로 읽힌다.
천천히 오르는 게 아니라 3.3초 내내 평평하다가 setup 이 다 돌면 제자리로 온다.

세 가지를 짚어 봤는데 **셋 다 아니었다.**

    핀 떼어놓기 (rtc_gpio_isolate)   → 그대로
    디지털 입력 버퍼 끄기            → 그대로
    3V3_S 켜기                       → 그대로

**그런데 비율이 아주 안정적이다.**

    1417/2644 = 0.5359   1421/2645 = 0.5372   1420/2647 = 0.5365

그래서 원인을 모르는 채로도 쓸 수 있게 만들었다. **찍은 값이 모양을 주고,
시작과 끝에 제대로 잰 값 두 개가 눈금을 준다.** `drainstat` 이 두 앵커로
비율을 되짚어서 0.536 근처인지 스스로 검사한다. 어긋나면 그 표는 못 쓴다고 말한다.

## 8. 아직 안 잰 것

**실제로 몇 µA 로 자는지는 모른다.** 60초 시험에서 3583 → 3581 mV 였는데
2 mV 는 ADC 잡음 수준이라 µA 를 가릴 수 없다.

제대로 재려면 둘 중 하나다.

- 멀티미터를 배터리 선에 물린다
- USB 를 뽑고 배터리로만 몇 시간 재운 뒤 `sleepstat` 의 mV/h 를 본다

목표는 10 µA 미만이다
[확인: docs/board/RAK19007_datasheet.txt "the sleep current is lower than 10 µA"].

---

## 9. 재보는 법

```
off              끈다
off 60           60초 뒤 스스로 깬다 (시험용). 사람 없이 돌릴 수 있다
sleepstat        지난번에 정말 잤나 — 헛깸 횟수, 잔 시간, 전압 낙차
battboot         켠 뒤 1초마다 담은 배터리 값
```

**헛깸이 0 이 아니면 그 자리에서 멈추고 원인을 찾을 것.** 그 숫자 하나가
배터리 수명을 통째로 결정한다.

★ 맥에서 보드가 깼는지 볼 때 `system_profiler` 를 쓰면 안 된다.
보드가 깨어 있는데도 안 보인다고 나온 적이 있다. **포트를 실제로 열어보는**
방법만 믿을 수 있다.
