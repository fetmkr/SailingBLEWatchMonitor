# 보드 시리얼 명령

USB 로 꽂고 115200 bps. 보드에서 `help` 를 쳐도 같은 목록이 나온다.

```
pio device monitor            평소 볼 때
python3 -c "..." 로 붙여도 된다  ★ dtr/rts 를 내리면 보드가 리셋된다. 기본값으로 열 것
```

명령은 여든 가지쯤 된다. 다 외울 필요 없다. **`check` 하나면 붙어 있는 것을
한 번에 훑는다.** 나머지는 뭔가 이상할 때 찾아 쓰면 된다.

---

## 처음 켰을 때

| 명령 | 하는 일 |
|---|---|
| `check` | **★ 이것부터.** GPS·IMU·화면·SD·배터리를 한 번에 훑는다 |
| `info` | 지금 설정 (이름·번호·주기·전원핀) |
| `status` | 한 줄 상태 |
| `help` · `?` | 도움말 |

---

## 기록 (SD 카드)

| 명령 | 하는 일 |
|---|---|
| `rec` | **★ 기록 상태.** 버린 줄·최대 멈춤·버퍼 최고치까지 |
| `rec on` / `rec off` | 시작 / 종료 |
| `rec mark` | 이벤트 표식. 다음 항법 줄에 붙는다 (최대 1초 늦다) |
| `rec ls` | 카드에 있는 세션 목록 |
| `rec check [번호]` | 보드가 직접 되읽어 검사 (CRC·주기·IMU 간격) |
| `rec head`/`rec tail [번호] [줄수]` | TXT 사본 앞뒤 몇 줄 |
| `rec rm <번호>` | 그 세션을 지운다. **못 되돌린다** |
| `sd` | 카드 마운트 + 쓰기 시험 |
| `sdbench [줄수]` | 쓰기 속도와 최대 멈춤 실측 (기본 3600줄) |
| `sess [번호]` | 세션 번호 보기·고치기 (NVS 에 남는다) |
| `tz [분]` | 파일 이름에 쓸 시각 기울기 (기본 540 = 한국) |

---

## GPS

| 명령 | 하는 일 |
|---|---|
| `fix` | 파싱 상태 — 위성 수·위치·속도·침로 |
| `gps` / `gps d` | UART1 원시 NMEA 5초. `d` 는 슬롯 D 에 꽂았을 때 |
| `gpshz <1\|2\|5\|10>` | 갱신율 (기본 5). 실제로 걸렸는지 세어 준다 |
| `gpscfg` | **모듈에 실제로 걸린 설정을 되물어본다** |
| `gpscfg mode <0~7>` | 움직임 종류 (0휴대 1정지 2보행 3자동차 4선박) |
| `gpscfg static <m/s>` | 정지로 볼 속도 문턱 |
| `nmea <본문>` | NMEA 명령 직접 보내기 (체크섬 자동) |
| `navpv` / `navpv l` | NMEA 거치기 전 속도를 RMC 와 나란히 |
| `smooth <0~5>` | 속도·침로 다듬기 세기 (0 원본, 기본 2) |
| `dead <kn>` | 잡음 바닥. 이보다 작은 속도는 0 (기본 0.10) |

★ L76K 는 **PMTK 가 아니라 PCAS** 명령을 쓰고, 부팅할 때마다 다시 걸어야 한다.

---

## 9축 · 방위

| 명령 | 하는 일 |
|---|---|
| `imu` | 9축 값 5초 출력. 기울여 보면서 본다 |
| `level` | **★ 지금 자세를 힐·피치 0° 로 삼기.** 배가 평형일 때 |
| `calib` | 자이로 0점 다시 잡기 (기울어 있어도 된다) |
| `heel [x\|y\|z]` | 힐을 어느 가속도 축에서 볼지. 앞에 `-` 로 뒤집기 |
| `pitch [x\|y\|z]` | 피치를 어느 축에서 볼지 |
| `hdg <A> <B>` | 방위를 만들 자력계 두 축. 예) `hdg -z x` |
| `hdg off <도>` | 방위 0점 보정 (자기 편각 + 보드 어긋남) |
| `hdgtilt` | **기울기 보정 방위 확인.** 손으로 40° 넘게 기울이며 5초 |
| `magcal` | **자력계 치우침 재기.** `on` / `stop` / `clear`. BLE 로도 된다 |

---

## 무전기 (LoRa)

| 명령 | 하는 일 |
|---|---|
| `lora` | 상태 — 주파수·전파시간·받은 개수·**버린 개수** |
| `lora on` | 켜기 |
| `lora regs` | **칩 버그 세 개가 실제로 걸렸는지** 레지스터로 확인 |
| `lora rssi` | 이 주파수의 바닥 잡음. 보드 한 대로 하는 확인 |
| `lora tx` / `lora watch` | 보내기 / 받는 것 찍기 (두 대로 거리 시험) |
| `boat <0~32>` | 배 번호. 0 은 번호 없음 |

★ `lora` 의 **"버린 개수"가 0 이 아니면** 메인 루프가 링버퍼를 제때 안 꺼내 간 것이다.
그건 배를 놓쳤다는 뜻이다.

---

## 파일 내보내기 (WiFi)

| 명령 | 하는 일 |
|---|---|
| `wifi` | **★ 상태와 사용법** |
| `wifi ap` | 보드가 스스로 망을 연다 |
| `wifi join` | 있는 망에 들어간다 (NVS 의 이름·비밀번호로) |
| `wifi ssid <이름>` / `wifi pass <암호>` | 그 이름·비밀번호를 NVS 에 넣는다 |
| `wifi scan` | 주변 망 목록 |
| `wifi status` | 지금 상태 |
| `wifi idle <초>` | 아무도 안 쓰면 저절로 꺼지는 시간 |
| `wifi off` | 끄기 |

★ WiFi 는 **평소에 꺼져 있는 것이 정상이다.** 켜면 전기를 먹고 BLE 를 방해한다.
파일 보낼 때만 켰다가 끈다.

---

## 전원 · 잠자기

| 명령 | 하는 일 |
|---|---|
| `off` | **보드를 끈다 (깊은잠).** 버튼 5초로 켜진다 |
| `off <초>` | 그 초 뒤에 스스로 깬다 (시험용) |
| `sleepstat` | 지난번에 정말 잤나 — **헛깬 횟수**·잔 시간·3V3_S |
| `batt` | 배터리 전압 실측 |
| `battboot` | 켠 뒤 1초마다 담은 배터리 값 |
| `power 14` / `power 2` / `power off` | 센서 전원 스위치 |

★ **`sleepstat` 의 헛깸이 0 이 아니면 그 자리에서 멈추고 원인을 찾을 것.**
그 숫자 하나가 배터리 수명을 통째로 정한다.

★ **깰 때 전압은 안 나온다.** 깬 직후 값은 못 믿어서 `--` 를 찍는다.
잠자기 전류는 **멀티미터를 배터리 선에 물려야** 나온다. `POWER.md` 맨 위.

---

## 그 밖의 진단

| 명령 | 하는 일 |
|---|---|
| `scan` | I2C 목록 — 화면 0x3C / IMU 0x68 |
| `oled` | 화면을 나중에 꽂았을 때 다시 붙이기 |
| `oledw` | 화면에 쓸 한글 줄의 폭 (128px 안에 드나) |
| `pin <번호>` | 그 GPIO 를 5초 지켜본다 (버튼 달 자리 찾기) |
| `loopstat` | 루프 한 바퀴가 어디에 시간을 쓰나 |
| `usbbench [KB]` | USB 시리얼 속도 실측 |
| `name <이름>` | 보드 이름 (최대 11자) |
| `hz <1~100>` | BLE notify 주기 (기본 10 Hz) |

---

## 값 뒤에 붙는 표시

    (GPS)  위성으로 잰 실측
    (IMU)  9축으로 잰 실측
    (SIM)  ★ 위성을 못 잡아 시뮬레이터로 채운 값

**가짜 값이 진짜처럼 보이면 안 된다.** 그래서 출처를 늘 붙인다.
값이 아예 없으면 `--.--` 로 그린다. 0 을 쓰면 "멈춰 있다" 로 읽힌다.
