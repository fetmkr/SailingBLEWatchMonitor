# 체크리스트 대비 지금 코드 — 시험할 수 있나, 막는 것은 무엇인가

기준 문서: `boat-device-checklist.md`. 2026-09-15 코드를 읽고 적었다. **시험은 하나도 안 했다.**
"예상" 칸은 코드로 본 예상이지 판정이 아니다. 판정은 시험해서 `통과 / 실패 / 미측정` 으로 적는다.

| # | 항목 | 지금 시험 가능? | 코드로 본 예상 · 막는 것 | 근거 |
|---|---|---|---|---|
| 01 | 자기 간섭 | 가능 | 막는 것 없음. 세기(µT)는 시리얼 `hdg` 로만 보인다 | |
| 02 | 나침반 8방위 | 가능 | 워치 HDG 는 BLE 로 받은 보드 값이라 OLED 와 같은 식. 세션 46 에서 보드 식이 COG 대비 −24° 한쪽으로 치우침 → 장착 오프셋을 `hdg ref` 네 방향으로 다시 잡기 전엔 실패 예상 | `WatchLiveView.swift:244` · NEXT 헤딩 표 |
| 03 | 기울기 | 가능 | **실패 예상.** OLED·BLE·TXT 는 평평한 식. 힐 20° 에 29° 틀린다고 코드 주석에 계산돼 있음. 계산 불가 자세(세움)에서도 숫자를 낸다 — `headingDeg()` 에 기울기 문턱 없음 | `main.cpp:2151` 부근 |
| 04 | 회전 반응 · 재부팅 | 가능 | 설정은 NVS 에 남는다. 화면 갱신 주기·다듬기는 확인 안 함 | `main.cpp:385` |
| 05 | OLED·워치·앱 대조 | 가능 | **실패 예상.** 앱 HDG `atan2(magY,magX)` 가 보드와 다른 식. HLG 에 축·오프셋·편각·치우침 설정이 없어 앱이 재현 불가 | `desktop/src/main.ts:415` · `main.cpp:3742` |
| 06 | 정지 · 저속 | 일부 | HLG 에 도플러 원본은 10 Hz 로 있다. **화면에 보인 속도(다듬은 값, 0 표시, `--`)는 기록 안 됨** → "0 표시 비율" 을 로그로 못 구함. 영상으로만 가능 | `hlog.h` NavRecord · CLAUDE.md "원본만 적는다" |
| 07 | 기록 30회 | 가능 | 막는 것 없음. 파일별 끝까지 검사는 `rec check <번호>` 또는 받아서 `hlog_parse.py` | |
| 08 | 출항 전 | 가능 | 30초 기록 → 앱으로 받기·재생 됨 (세션 46 을 앱으로 받음) | |
| 09 | 항해 중 | 가능 | 단추 마킹 있음 (C3) | |
| 10 | 항해 후 | 일부 | **해시 비교 불가** — 보드에 해시 명령 없음. 앱은 CRC 검사. IMU 간격 분포·NAV 주기는 `hlog_parse.py` 가 보여줌 | `grep sha/md5` 없음 |
| 11 | 연결 끊김 | 가능 | 연결 경로 2.0 초, 광고 경로 4.0 초 지나면 "살아있지 않음" → 워치 화면 45% 흐림. **광고 경로 4초는 목표 3초를 넘는다.** 흐리게라도 마지막 값은 계속 보인다 — 체크리스트 기준에 맞는지 판단 필요 | `BLEManager.swift:76` · `WatchLiveView.swift:251` |
| 12 | 카드 오류 · 느린 종료 · 전원 차단 | 일부 | SD 없이 시작 → "카드 없음" 거절 있음. **남은 공간 검사 없음** (값만 읽고 안 씀). 닫기 지연(`rec slow`)·IMU/GPS 끊기 주입 명령 없음 (NEXT 1번 시험 목록). 카드 무응답 끄기는 NEXT 1번 설계 | `hlog.cpp:443` · `gFreeBytes` |
| 13 | 하루 운용 | 일부 | **잔량 경고·저전압 끄기 없음.** OLED 는 전압만 표시 | `grep` 결과 없음 |
| 14 | 젖음 · 진동 | 하드웨어 | 펌웨어와 무관 | |
| 15 | 처음 쓰는 사람 | 일부 | 인터넷 없으면 지도 안 뜸 (C10). 한 장 사용법 있는지 확인 안 함 | `map.ts` |

## 먼저 할 다섯(02·03·05·06·07)을 막는 것만

1. 03·05 — 방위 식 하나로 (OLED·BLE·TXT·앱 같은 식, 기울기 보정, 계산 불가면 `--`). **사용자 결정 대기** (NEXT 헤딩 항목)
2. 02 — 장착 오프셋을 나침반 절차로 다시 잡기 (`hdg ref`). COG 로 안 맞춘다
3. 05 — 식에 쓴 설정을 HLG 에 남기기
4. 06 — 화면에 보인 속도(또는 그 판정)를 따로 기록할지 결정. 원본 원칙과 충돌하지 않게 "표시값" 칸으로 구분
5. 07 — 없음. 바로 할 수 있다
