# SD 기록 계획

기능명세는 `docs/spec/기능명세_2025-07-30.md` 에 있다. 그 문서의
"경기정용 센서 모듈 → 데이터 저장" 항목을 이 보드에서 어떻게 할지 정한다.

명세가 요구하는 것은 다섯 줄이다.

- 저장 버튼으로 기록 시작·종료. 선수가 명확히 통제
- 32GB microSD, 10 Hz, 수천 시간
- GPS UTC 시각 기준 → 다른 배·영상과 자동 정렬
- 코치보트 없어도 단독 기록
- 저장 항목: 시각 / GPS / 자세 / 9축 / 시스템 + 세션ID + 이벤트 + CRC

---

## 만든 것 (2026-08-24)

규격은 `docs/spec/로그포맷_v1.0_draft_2026-08-24.md` 다. 아래는 우리 시제품
보드에서 어떻게 채웠고 실제로 어떻게 나왔는지다.

```
firmware-rak/include/hlog.h    포맷 정의 (파서와 같은 규칙)
firmware-rak/src/hlog.cpp      링버퍼 + 코어 0 쓰기 작업
tools/hlog_parse.py            파서 참조 구현 (명세 §8 이 요구하는 것)
```

### 실제로 나온 값 (S00006, 52초)

```
NAV 실제 주기    10.01 Hz   (규격 10 Hz)
IMU 실제 주기   100.00 Hz   (규격 100 Hz)
IMU 이웃 간격   10 ms 가 5,228개 중 5,228개  → 100.00%
재동기 0회      못 읽은 바이트 0      CRC 전부 맞음
버린 줄 0       기다린 횟수 0         버퍼 최고 3%
카드 최대 멈춤  53 ms
```

### 등간격은 칩한테 시킨다

우리가 100 Hz 로 읽으면 간격이 고르지 않다. 실측으로 **78 Hz** 밖에 안 나왔다.
원인을 재 보니 화면이었다.

```
루프      359바퀴/초 (한 바퀴 평균 2.8 ms)
화면      한 장 33.6 ms,  초당 134 ms   ← 제일 큼
IMU읽기   초당 55 ms
GPS읽기   초당 18 ms
```

I2C 는 이미 400 kHz 로 SSD1306·MPU-9250 둘 다의 규격 최대다. 더 못 올린다.

그래서 **MPU-9250 의 FIFO** 를 쓴다. `SMPLRT_DIV = 9` 로 칩이 자기 클럭으로
정확히 100 Hz 마다 값을 떠서 512바이트 FIFO 에 쌓는다. 우리는 10 ms 마다
퍼 오기만 한다. **언제 퍼 오든 값들 사이 간격은 칩이 만든 10 ms 그대로다.**

```
한 벌   가속 6바이트 + 자이로 6바이트 = 12바이트
FIFO    512바이트 = 42벌 = 420 ms 치
```

기록을 시작할 때 FIFO 를 비운다. 카드를 마운트하는 동안 쌓인 옛 값이 세션
첫머리에 섞이면 안 된다. 이걸 안 했을 때 "넘칠 뻔 2번" 이 찍혔다.

기록 중에는 화면을 1 Hz 로 떨어뜨린다. **실제 모듈에는 화면이 아예 없다**
(기능명세 「내구성」). 이 문제는 개발 보드에만 있다.

### 칸을 더해 나가야 한다

`lastNotify = now` 로 두면 한 바퀴(2.8 ms)만큼씩 밀려서 NAV 가 9.85 Hz 가
됐다. `lastNotify += 100` 으로 바꾸니 10.01 Hz 가 나왔다. IMU 가 78 Hz 였던
것도 같은 원인이었다.

### 규격에 없어서 우리가 정한 것

freeze 전에 원문에 넣어야 한다.

| 무엇 | 왜 |
|---|---|
| **값 없음 표식** | `sog 0xFFFF`, `cog 0xFFFF`, `lat/lon 0x80000000`, `h_acc 0xFFFF`, 쿼터니언 넷 다 0. 0 을 쓰면 "정박 중 0노트" 와 "위성 못 잡음" 이 구별되지 않는다 |
| **파일 이름** | 규격은 `/LOGS/YYYYMMDD/SNNN.HLG` 인데 전원을 켠 직후에는 날짜를 모른다. 선수가 해변에서 버튼을 누르는 순간이 그렇다. `/LOGS/SNNNNN.HLG` 로 하고 시각은 헤더에 넣었다 |
| **헤더 reserved 사용** | `imu_type`, `time_ref`, `mag_scale`, `gnss_dyn`, `gnss_hz`, `sog_src`, `quat_src`. 자세히는 `hlog.h` |
| **버퍼 4 KB → 64 KB** | 규격 §6 의 4 KB 는 1.3초치다. 우리가 실측한 카드 최악 멈춤이 1.9초라 넘친다 |
| **파서 재동기 규칙** | CRC 만으로는 우연히 맞을 수 있다. `local_ms` 가 뒤로 가지 않는다는 조건을 같이 본다 |

### 제안 (freeze 전에 정할 것)

- **`s_acc`(속도 정확도) 를 Type A 에 넣자.** 요트에서 제일 중요한 값이
  속도인데 그 값이 믿을 만한지 알 방법이 없다. 모듈이 알려 준다
  (L76K 도 NAV-PV 로 준다). u16 mm/s 면 2바이트다
- **어느 회전벡터인지 명시하자.** BNO085 는 9축(자력계 씀)과 6축(안 씀)을
  둘 다 낸다. 우리가 방금 확인했듯 배의 쇠가 자력계를 크게 흔든다.
  9축을 쓰면 그 오차가 쿼터니언에 구워져 되돌릴 수 없다
- **`mag` 의 "raw LSB" 스케일을 적자.** 규격이 "포맷 버전이 스케일을 정의"
  한다고 해 놓고 자력계만 안 적혀 있다
- **`imu_stat`(캘리브레이션 상태) 를 Type A 에 넣자.** 헤더에 한 번만 있으면
  세션 중에 바뀌는 걸 못 잡는다. 10 Hz 면 1바이트로 충분하다
- **정렬 주의를 적자.** `local_ms` 가 오프셋 1이라 구조체 포인터 캐스팅이
  안 된다. 파서·펌웨어 모두 memcpy 를 쓸 것

---

## 0. 지금 확인된 것

### 카드 (2026-08-23 실측)

128 GB microSD 를 FAT32 로 포맷해서 꽂았다. 붙는다.

```
카드 종류  SDHC/SDXC
크기       122112 MB
쓰기       OK
```

처음 꽂았을 때는 안 붙었다. 이유는 짐작하지 않고 라이브러리가 찍는 줄을 봤다.

```
[E][sd_diskio.cpp:806] f_mount failed: (13) There is no valid FAT volume
```

카드는 읽히는데 FAT 가 아니라는 뜻이다. 128 GB 는 공장에서 exFAT 로 나오고
우리 빌드는 exFAT 를 안 읽는다. FAT32 로 다시 포맷하니 붙었다.
`sd` 명령이 이제 이 오류 번호별로 무엇을 해야 하는지 말해 준다.

### 쓰기 속도 (`sdbench` 실측)

실제 기록과 같은 모양으로 쟀다. 153바이트 줄을 512바이트씩 모아 내보내고
5초치마다 flush 한다.

| 시험 길이 | 쓴 양 | 평균 속도 | **제일 오래 멈춘 시간** |
|---|---|---|---|
| 1시간치 | 5.5 MB | 201 KB/초 | **14 ms** |
| 5.5시간치 | 30.6 MB | 314 KB/초 | **70 ms** |

**평균 속도는 문제가 아니다.** 우리가 쓸 양은 초당 1.5 KB 인데 200배가 넘는
여유가 있다.

**문제는 가끔 오래 멈추는 것이다.** 그리고 오래 돌릴수록 나빠진다. 시험을
5.5배 길게 했더니 최대 멈춤이 14 ms 에서 70 ms 로 늘었다. 점 두 개로 법칙을
말할 수는 없지만, **최악이 아직 안 드러났다는 것만은 분명하다.** notify 주기
100 ms 를 안 넘는다고 장담할 수 없다.

20시간치까지 늘려 보니 **1.9초**까지 멈췄다. notify 주기 100 ms 의 19배다.

→ 그래서 §4 의 "쓰기를 따로 띄운다" 를 그대로 간다. 짐작이 아니라 측정에서
나온 결론이다.

**데이터를 잃지는 않았다.** 파일 크기가 110,160,000 바이트로 720000줄 ×
153바이트와 정확히 맞았다. 느려질 뿐 실패하지는 않는다.

### 왜 멈추나 — 표준 문서로 확인

원문을 `docs/sd/` 에 넣어 뒀다 (SD Physical Layer Simplified Specification
v3.01). 인터넷에서 다시 찾지 말 것.

**1) 카드가 "바쁘다" 를 놓지 않는 것이다. 표준이 허락한 동작이다.**

> "In case of High Capacity SD Memory Card, maximum length of busy is defined
> as 250ms for all write" … "the card can indicate write busy up to 500ms"
> — §4.6.2.2

> "It is strongly recommended for hosts to implement more than 500ms timeout
> value even if the card indicates the 250ms maximum busy length." — 같은 절

드라이버도 딱 그렇게 짜여 있다. 쓰기를 보내고 카드가 놓아줄 때까지 최대
500 ms 를 기다린다 [확인: `sd_diskio.cpp:94` `sdWait()`]. 우리가 잰 1.9초는
한 번의 `f.write()` 안에서 이 기다림이 여러 번 겹친 것이다.

**2) 카드는 AU(Allocation Unit)라는 큰 덩어리 단위로 관리한다.**

> "The User Area is divided into units called *Allocation Unit (AU)*. AU is
> physical boundary in User Area of a card and is not defined by the file
> system boundary." — §4.13.1.1

**그리고 그 덩어리 크기는 카드 용량에 따라 커진다.** 여기가 "용량이 커서
그런가" 에 대한 답이다.

| 카드 용량 | 최대 AU 크기 (Table 4-42) |
|---|---|
| ~32 GB | **4 MB** |
| ~2 TB (SDXC) | **64 MB** |

> "Capacities of up to 2TB and the UHS high speed interface require larger AU
> sizes. In the case of SDXC the maximum AU size is increased to 64MB."
> — §4.13.2.1.1

**3) 카드의 속도 보증에는 조건이 붙어 있고, 우리는 그 조건을 못 지킨다.**

> "To record the stream data, a Speed Class host shall manage the memory area
> in units of an AU and **use only completely free AUs (zero fragmentation)**
> to record the data." — §4.13.2.1.1

> "r: Ratio of Used RU over an AU … r=1 means all RUs are used and
> **performance indicates zero at this point**." — §4.13.1.5

FAT32 + FatFs 는 Speed Class 호스트가 아니다. 빈 AU 를 골라 쓰지 않는다.
파일을 지우고 같은 자리에 다시 쓰면 그 AU 는 "쓰인 채로 남은 조각" 이 되고,
표준의 말대로 성능이 0 을 향해 내려간다.

### 보드에서 확인 — 멈춤이 나는 자리

멈춤이 파일 어디에서 나는지 찍었다 (110 MB 를 쓰면서 18만 번 기록).

```
     ~1 ms   148663번  (82.6%)
  10~20 ms    24114번  (13.4%)   ← 낸드 페이지 쓰는 평소 값
  50~100 ms     107번  ( 0.06%)
  200 ms 위       7번  ( 0.004%)  ← 이게 문제
```

**긴 멈춤은 드물고, 흩어져 있지 않고 뭉쳐서 온다.**

```
1882 ms  파일  12.66 MB 자리
 282 ms  파일  12.69 MB 자리   (앞것과 0.03 MB)
 130 ms  파일  12.72 MB 자리   (앞것과 0.03 MB)
1713 ms  파일  76.96 MB 자리   (앞것과 64.24 MB)   ★
1828 ms  파일  76.97 MB 자리
1253 ms  파일  76.97 MB 자리
1593 ms  파일 103.93 MB 자리   (앞것과 26.96 MB)
1121 ms  파일 103.93 MB 자리
```

세 번씩 뭉쳐서 온다. 매번 나는 게 아니라 **카드가 가끔 큰 정리를 한다**는
뜻이다. 그리고 뭉치 사이 간격 하나가 **64.24 MB** 인데, 표준이 정한 SDXC 의
AU 최대치가 정확히 **64 MB** 다. 점 하나로 단정할 수는 없지만 딱 맞는다.

**카드가 꽉 차서가 아니다.** 이때 쓴 자리는 122066 MB 중 108 MB 였다.
0.09% 다. 원인은 "가득 참" 이 아니라 "관리 덩어리가 크다" 와 "지우고 같은
자리에 다시 썼다" 다.

### 그래서 카드는 32 GB 가 낫다

용량이 작아서가 아니라 **AU 가 작아서** 다. 32 GB 이하는 표준이 AU 를 4 MB
로 묶어 놓았다. 128 GB 는 64 MB 까지 커질 수 있다. 카드가 한 번 정리할 때
옮겨야 할 짐이 16배 차이 난다.

- 카드를 꽉 채우지 않는다
- 지우고 다시 쓰지 말고, 시즌마다 통째로 다시 포맷한다
- 그래도 멈춤은 남는다. 그래서 §4 의 버퍼가 필요하다

### 저장 버튼

**없다.** RAK19007 에는 리셋 버튼 하나뿐이다
[확인: `docs/board/RAK19007_datasheet.txt` 35번째 줄, 261번째 줄].
"two user-definable LEDs, one power supply/charging indicator LED, and a
**reset** button". 그래서 시작·종료는 당분간 시리얼과 앱으로 한다.

쓸 수 있는 것은 있다. 초록·파랑 LED 두 개다 (GPIO46 / GPIO45).

---

## 1. 제일 중요한 원칙 — 원본을 남긴다

가공한 값은 나중에 다시 만들 수 있다. 원본은 못 되살린다.

지금 헤딩은 거칠다. 자기 보정도 안 됐고 기울기 보정도 없다. 그래도
**자력 3축 원본을 그대로 저장해 두면** 나중에 보정법을 만들었을 때 예전
훈련 데이터까지 다시 계산할 수 있다. 헤딩 각도만 저장하면 그게 안 된다.

속도도 같다. 앱에 보이는 속도는 다듬고 잡음 바닥을 적용한 값이다. 그것과
**모듈이 준 도플러 원본**을 나란히 저장한다. 다듬기 세기를 바꿔서 다시 볼 수
있어야 한다.

힐·피치도 각도만 남기지 않는다. 가속도 3축 원본이 같이 들어간다.

---

## 2. 파일 배치

```
/SAIL/
  INDEX.CSV      세션 목록. 한 세션에 두 줄 (시작 / 종료)
  S00042.CSV     42번 세션
  S00043.CSV
```

세션 번호는 NVS 에 두고 시작할 때마다 1 올린다. **같은 이름 파일이 이미
있으면 절대 덮어쓰지 않는다.** 번호를 건너뛴다.

### 이름을 시각으로 안 짓는 이유

명세는 UTC 기준 정렬을 요구한다. 그런데 전원을 켠 직후에는 GPS 가 아직
시각을 모른다. 파일 이름을 시각으로 지으려면 fix 를 기다리거나 나중에
이름을 바꿔야 한다. 둘 다 실패할 구석이 생긴다.

**이름은 세션 번호로, 시각은 파일 안에 넣는다.** 머리글에 첫 fix 의 UTC 를
적고, 모든 줄에 UTC 밀리초를 적는다. 정렬은 파일 안을 보고 하면 된다.
그러면 fix 없이도 기록이 시작되고, 나중에 fix 가 잡히면 그때부터 시각이
채워진다. 명세의 "코치보트 없어도 단독 기록" 과도 맞는다.

---

## 3. 파일 안

### 머리글

`#` 로 시작하는 줄이다. **그때의 설정을 통째로 박아 둔다.** 이게 없으면
나중에 힐 값을 해석할 수가 없다. 어느 축이었는지, 기준각이 얼마였는지
모르기 때문이다.

```
#sail-log v1
#session=42
#module=SAIL-random() id=0xA3
#proto=1
#heel=-Y off=+1.2   pitch=+Z off=-0.4
#gps=mode0 10Hz  smooth=2 dead=0.10
#utc_start=2026-08-23T05:12:34.100Z
```

`utc_start` 는 첫 fix 때 덧붙인다. fix 를 끝내 못 잡으면 안 적는다.

### 컬럼

| # | 이름 | 단위 | 없을 때 |
|---|---|---|---|
| 1 | `ms` | 보드 켜진 뒤 밀리초 | 항상 있음 |
| 2 | `utc_ms` | UTC epoch 밀리초 | 빈칸 |
| 3 | `fix` | 0 없음 / 1 있음 | |
| 4 | `lat` | 도, 소수점 7자리 | 빈칸 |
| 5 | `lon` | 도, 소수점 7자리 | 빈칸 |
| 6 | `sog` | 노트. **앱에 보이는 값** (다듬기·잡음바닥 적용) | 빈칸 |
| 7 | `sog_raw` | 노트. **모듈이 준 도플러 원본** | 빈칸 |
| 8 | `cog` | 도 | 빈칸 |
| 9 | `sats` | 개 | |
| 10 | `hdop` | 측위 정확도 | 빈칸 |
| 11 | `sacc` | 속도 정확도 (m/s). NAV-PV 의 sAcc | 빈칸 |
| 12 | `heel` | 도. 우현 양수 | 빈칸 |
| 13 | `pitch` | 도. 뱃머리 들리면 양수 | 빈칸 |
| 14 | `hdg` | 도. 자력계 방위 **(보정 전)** | 빈칸 |
| 15~17 | `ax ay az` | g | |
| 18~20 | `gx gy gz` | °/s | |
| 21~23 | `mx my mz` | µT **(원본)** | |
| 24 | `batt` | % | |
| 25 | `volt` | V | |
| 26 | `ev` | 이벤트 표식. 0 = 없음 | |
| 27 | `crc` | 앞의 26칸에 대한 CRC-16, 16진수 4자리 | |

**값이 없으면 빈칸이다. 0 을 쓰지 않는다.** 정박 중 0.0 노트와 구별되어야
한다. BLE 프로토콜에서 쓰는 규칙과 같다 (`PROTOCOL.md` §2.1).

### 한 줄 예시

```
1234500,1787492994100,1,37.5123456,126.9123456,5.53,5.61,315.0,11,1.4,0.05,-12.3,2.1,344,-0.034,0.012,-1.005,0.2,0.4,-0.1,3.0,-21.2,-16.7,68,3.91,0,A3F2
```

### 용량 계산

| | |
|---|---|
| 한 줄 | 약 **153 바이트** (위 예시 실측) |
| 10 Hz | 1.5 KB/초 = **5.5 MB/시간** |
| 32 GB | 약 **5800 시간** |
| 하루 6시간 훈련 | 약 **970일** |

명세의 "수천 시간" 을 넘긴다. FAT32 의 파일 하나 4 GB 한계는 700시간쯤
연속으로 기록해야 닿으므로 파일 쪼개기는 안 넣는다.

### 카드는 몇 기가까지 되나

| 용량 | 되나 | 왜 |
|---|---|---|
| ~2 GB (SDSC) | **된다** | 공장 출하가 FAT16 인데 그것도 읽는다. 363시간 |
| 4~32 GB (SDHC) | **그냥 된다** | 공장 출하가 이미 FAT32 |
| 64~512 GB (SDXC) | **FAT32 로 다시 포맷하면 된다** | 출하는 exFAT 인데 우리 빌드가 exFAT 를 못 읽는다 |
| 1~2 TB (SDXC) | 이론상 된다 | 안 해 봤다 |
| 2 TB 초과 (SDUC) | 안 된다 | SPI 모드를 지원 안 할 수 있다 |

**진짜 벽은 용량이 아니라 파일 시스템이다.**

- exFAT 가 꺼져 있다
  [확인: `framework-arduinoespressif32/tools/sdk/esp32s3/include/fatfs/src/ffconf.h:255`
  `#define FF_FS_EXFAT 0`]. 64 GB 이상은 출하 때 exFAT 라 그냥 꽂으면 안 붙는다.
- FatFs 의 섹터 주소가 32비트다 (`FF_LBA64` 가 정의돼 있지 않다).
  512바이트 섹터 기준 2 TiB 가 천장이다.
- 아두이노 SD 드라이버의 섹터 수 계산 `(C_SIZE+1) << 10` 도 32비트다
  [확인: `libraries/SD/src/sd_diskio.cpp:417-456`]. CSD 의 최댓값인 2 TB 에서만
  넘친다.
- SPI 모드는 SDXC(2 TB 까지)까지 쓴다. 그보다 큰 SDUC 는 SPI 를 지원 안 할 수
  있다 [확인: https://elm-chan.org/docs/mmc/mmc_e.html].

**32 GB 를 권한다.** 5800시간이면 하루 6시간씩 970일이다. 더 큰 카드를 써도
SPI 에서 더 빨라지지 않고, 다시 포맷하는 일만 는다.

작은 쪽도 막히지 않는다. 2 GB 짜리 옛날 카드는 SDSC 라 FAT16 으로 나오는데
그것도 읽고 쓴다 [확인: `ffconf.h:20` `FF_FS_MINIMIZE 0`, `ffconf.h:13`
`FF_FS_READONLY 0` — FAT12/16/32 모두 켜져 있다. 드라이버도 SDHC 가 아닌
카드에 `SET_BLOCKLEN 512` 를 걸고 바이트 주소로 읽는다,
`sd_diskio.cpp:596`]. 363시간이라 명세의 "수천 시간" 에는 못 미치지만
만들고 시험하는 데는 넉넉하다.

FAT16 은 최상위 폴더에 넣을 수 있는 항목 수가 보통 512개로 묶여 있다.
우리는 `/SAIL/` 아래에 넣으므로 걸리지 않는다.

지금 SPI 를 4 MHz 로 열고 있다 (`SD.begin(..., 4000000)`). 그 상태로 실측
**314 KB/초** 가 나왔다 (§0). 우리가 쓸 양은 초당 1.5 KB 라 200배 여유다.
드라이버가 25 MHz 까지 올려 주지만 [확인: `sd_diskio.cpp:605-607`] 올릴
이유가 없다. 속도를 올려도 §0 에서 본 "가끔 오래 멈춤" 은 안 없어진다.
그건 카드 내부 사정이지 버스 속도가 아니다.

---

## 4. 쓰는 방법 — 10 Hz 를 안 흔들리게

SD 는 가끔 오래 걸린다. 이 카드로 실제로 쟀다 (§0). 5.5시간치를 쓰는 동안
제일 오래 멈춘 것이 **70 ms** 였다. notify 주기 100 ms 를 아직 안 넘지만,
시험을 길게 할수록 그 값이 계속 커졌다. 하루를 돌리면 넘길 것으로 본다.
그게 메인 루프 안에서 벌어지면 BLE notify 10 Hz 가 끊긴다.

그래서 **모으는 쪽과 쓰는 쪽을 나눈다.**

```
메인 루프 (코어 1)          쓰기 작업 (코어 0)
  10 Hz 로 한 줄 만들기   →   링버퍼 16 KB   →   500 ms 마다 또는
                                                512 바이트 넘으면 한 번에 쓰기
                                                5초마다 flush()
```

- 아두이노 loop 는 코어 1 에서 돈다
  [확인: `framework-arduinoespressif32/tools/sdk/esp32s3/sdkconfig:233`
  `CONFIG_ARDUINO_RUNNING_CORE=1`]
- NimBLE 는 코어 0 에 붙어 있다
  [확인: `NimBLE-Arduino/src/nimconfig.h:213`
  `CONFIG_BT_NIMBLE_PINNED_TO_CORE 0`]

쓰기 작업을 어느 코어에 둘지는 **재 보고 정한다.** 코어 0 에 낮은 우선순위로
두고 시작하되, 기록을 켰을 때 앱의 수신율 표시가 흔들리면 옮긴다. 지금
정해 놓지 않는다.

**버퍼가 넘치면 버린 줄 수를 센다.** 파일에 `#drop=N` 을 남긴다.
조용히 버리지 않는다.

**전원이 그냥 끊기면** 마지막 flush 이후가 날아간다. 최대 5초다.
flush 를 더 자주 하면 손실은 줄지만 카드 수명과 지연이 나빠진다. 5초로
시작하고 실측 뒤에 조정한다.

---

## 5. 시작·종료·표식

### 지금 (시리얼)

```
rec              상태 보기 — 기록 중인지, 파일명, 줄 수, 버린 줄, 남은 용량
rec on           시작
rec off          종료
rec mark         이 순간에 이벤트 표식 한 줄
rec auto on|off  fix 잡히면 저절로 시작 (NVS 에 저장)
```

`rec auto` 는 명세의 "코치보트 없어도 단독 기록" 을 버튼 없이 채우는 길이다.
켜 두면 배에 올라 전원만 넣어도 기록이 남는다.

### 다음 (앱)

BLE 에 제어용 특성을 하나 더 만든다. 워치에서 큰 버튼으로 시작·종료하고,
좋았던 순간에 표식을 찍는다. **선수한테는 이게 진짜 쓰는 길이다.**

### 나중 (버튼)

보드에 버튼을 달면 그때. 명세의 "저장 버튼" 은 그것이다.

### 눈으로 보이는 표시

- **초록 LED** 기록 중이면 1초에 한 번 깜박 (명세대로)
- **화면** 왼쪽 위 이름 자리에 `REC 12:34` — 기록 중일 때만

### 전원이 끊기면 스스로 다시 건다 (2026-08-31)

2026-08-30 세션 27 이 29분 4초 만에 그 자리에서 끊겼다. 카드도 배터리도
멀쩡했다. 코어덤프가 안 남은 것으로 보아 코드가 죽은 게 아니라 전원 쪽이
순간 끊긴 것이다. 배 위에서는 아무도 그걸 모르고, 그때부터 기록이 없다.

그래서 켜질 때 스스로 다시 건다.

```
start() 가 NVS 에 rec_on = 1 을 적는다
stop()  이 rec_on = 0 으로 지운다
전원이 끊기면 지울 틈이 없어 1 이 남는다
다음에 켜질 때 1 이면 이어서 시작한다
```

실측한 구멍은 **약 4.5초**다 (켜짐 3.9초 + 기록 시작 0.6초).

**같은 파일에 이어 붙이지 않는다.** 레코드의 `local_ms` 가 `millis()` 라서
다시 켜면 0 부터 시작한다. 한 파일 안에서 시간이 거꾸로 간다. 새 파일을
만들고 머리글 오프셋 77 에 앞 세션 번호를 적는다 (`prev_session`, U4).
0 이면 사람이 시작한 세션이다. 읽는 쪽은 이걸 보고 두 파일을 이어 붙인다.

#### 되풀이 막이

사람이 전원을 끊은 것과 접점이 튄 것을 보드는 구분하지 못한다. 그래서
무조건 이어 시작하되 횟수를 센다.

```
이어 시작할 때마다 rec_try 를 1 올린다
5 를 넘으면 멈추고 "전원선·배터리 접점을 보세요" 라고 알린다
이어 시작한 기록이 1분 넘게 멀쩡하면 rec_try 를 0 으로 되돌린다
```

카드 쓰기가 실패해서 저절로 멈춘 경우는 `stop()` 을 안 거치므로 표시가
남는다. 그때는 `healthCheck()` 가 대신 지운다 — 카드가 죽었는데 다시
걸어봐야 똑같이 실패한다.

#### 확인한 것 (실기기, 2026-08-31)

```
기록 중 갑자기 리셋   → 새 세션으로 이어서 시작. 머리글에 앞 번호가 적힘
rec off 뒤 리셋       → 안 살아난다
연속 6번 리셋         → 6번째에 멈추고 알린다. 그 뒤 켬은 깨끗하다
```

---

## 6. 잘못될 때

| 무슨 일 | 어떻게 |
|---|---|
| 카드 없음 | `rec on` 을 거절하고 이유를 말한다 |
| 기록 중 카드 빠짐 | 즉시 멈추고 표시. GPIO39 를 1초마다 본다 |
| 쓰기 실패 | 3번 다시 해 보고, 그래도 안 되면 멈추고 표시 |
| 버퍼 넘침 | 버린 줄 수를 세고 파일에 `#drop=N` |
| 카드 가득 | 남은 용량을 1분마다 보고, 50 MB 아래면 알린다 |

**어느 경우에도 나머지 기능은 계속 돈다.** SD 가 죽었다고 계기가 멈추면
안 된다. 지금 펌웨어가 센서에 대해 하고 있는 것과 같은 규칙이다.

---

## 7. 재 봐야 할 것

계획대로 됐는지는 이걸로 확인한다.

1. ~~카드를 꽂고 `sd` 로 마운트되는지~~ → 됐다 (§0)
2. ~~쓰기 속도와 최대 멈춤~~ → 쟀다 (§0). `sdbench` 로 언제든 다시 잰다
3. 10 Hz 로 한 시간 돌려서 줄 수가 **36000** 인지 센다
4. 기록을 켰을 때 앱의 BLE 수신율 표시가 흔들리는지
5. **진짜 기록에서 제일 오래 멈춘 시간** — `rec` 상태에 늘 보이게 둔다.
   sdbench 는 6분짜리다. 하루 훈련에서 어떤 값이 나오는지는 다르다
6. 전원을 그냥 뽑고 마지막 줄이 몇 초 전인지
7. CRC 로 깨진 줄이 잡히는지 — 일부러 한 줄 망가뜨려 본다

---

## 8. 순서

| 단계 | 무엇 | 걸리는 느낌 |
|---|---|---|
| ~~1~~ | ~~카드 꽂고 `sd` 확인, 쓰기 속도 실측~~ | **끝났다** |
| 2 | 한 줄 만들기 + CRC + 컬럼 확정 | 반나절 |
| 3 | 링버퍼 + 쓰기 작업 + flush 정책 | 반나절 |
| 4 | `rec` 명령 + LED + 화면 표시 | 반나절 |
| 5 | 한 시간 돌려서 §7 항목 재기 | 한 시간 |
| 6 | BLE 제어 특성 → 워치에서 시작·종료·표식 | 하루 |

2~5 가 이번 몫이다. 6 은 그다음이다.

---

## 안 하는 것 (명세에 있지만 나중)

- **저장 버튼** — 보드에 버튼이 없다. 하드웨어부터 필요하다
- **LoRa 전송** — 모듈이 없다
- **18650 교체형 배터리** — 지금은 1950 mAh 리튬폴리머
- **IP68 포팅, 클래스별 브래킷** — 기구 쪽
- **BLE OTA 업데이트**
- **세션ID ↔ 선수 매핑, 클라우드 업로드** — 앱·서버 쪽
- **지자기 캘리브레이션** — 로그에 자력 3축 원본을 남기므로, 나중에
  만들어도 예전 데이터까지 다시 계산할 수 있다
