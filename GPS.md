# GPS 설정

RAK12501 (Quectel L76K, 칩은 URANUS5) 을 어떻게 걸어 두었고 왜 그렇게 했나.
문서 원본은 `docs/gps/` 에 있다.

```
docs/gps/CASIC_protocol_en.txt          바이너리 규격. 우리가 쓰는 것
docs/gps/Quectel_L76K_protocol_v1.1.txt NMEA 와 $PCAS 명령
```

L76K 문서에는 저속 처리 이야기가 없다. CASIC 문서는 설정 항목 이름만 있고
그게 무슨 일을 하는지는 안 적혀 있다. 그래서 **u-blox 통합 매뉴얼을 곁에
두고 읽는다** — CASIC 의 모드 표가 u-blox 것과 순서까지 같아서 베낀 설계로
보인다. 다만 u-blox 문서는 참고이지 우리 칩의 규격이 아니다.

---

## 1. 지금 걸려 있는 값

```
통신 속도      115200 bps
측위 간격      100 ms (10 Hz)
움직임 종류     4  At sea 선박          ← 2026-08-31 에 저장.  아래 3장을 볼 것
정지 문턱값     0.00 m/s (꺼짐)
쓰는 위성      GPS · BeiDou · GLONASS
최소 신호      8 dB-Hz,  최소 고도각 0도
PPS 핀        GPIO21 (슬롯 A).  슬롯 D 면 GPIO38
```

보드 명령으로 언제든 되물어본다.

```
gpscfg              모듈에 실제로 걸린 값을 읽어 온다
gpscfg mode <0~7>   움직임 종류를 바꾸고 보드에 저장한다
gpscfg static <m/s> 정지로 볼 속도 문턱값
gpshz <1|2|5|10>    갱신율
fix                 파싱 상태 (위성 수 · 체크섬 통과/실패 · 최고 속도)
gps                 원시 NMEA 5초
navpv               NMEA 거치기 전 속도를 RMC 와 나란히
nmea <본문>          NMEA 명령 보내기 (체크섬 자동)
tz [분]             파일 이름에 쓸 시각 기울기 (기본 540 = 한국)
```

---

## 2. CASIC 바이너리 — 되물어볼 수 있는 게 핵심이다

`$PCAS` 는 보내기만 되고 확인이 안 된다. CASIC 바이너리는 **조회가 된다.**
전에는 보내 놓고 걸렸는지 확인한 적이 없었다.

```
틀    0xBA 0xCE | 길이(U2,LE) | class | id | 내용 | 체크섬(U4,LE)
조회   길이를 0 으로 보낸다
```

**체크섬 공식을 두 문서가 다르게 적어 놨다. L76K 쪽이 맞다** (실측).

```
L76K   Checksum = (ID << 24) + (Class << 16) + Len     ← 이게 먹힌다
CASIC  ckSum = (class << 24) + (id << 16) + len        ← 오타
```

설정 명령은 일곱 개뿐이다.

```
CFG-PRT     0x06 0x00   통신 포트
CFG-MSG     0x06 0x01   어떤 문장을 얼마나 자주 내보낼지
CFG-RST     0x06 0x02   재시작
CFG-CFG     0x06 0x05   저장·불러오기
CFG-NAVX    0x06 0x07   항법 엔진 (44바이트).  움직임 종류가 여기 있다
CFG-GROUP   0x06 0x08   GLONASS 그룹 지연
CFG-POLLMSG 0x06 0x10   문장 한 번 요청
```

**「저속에서 침로를 위치로 구하는 거르개」 같은 항목은 없다.** u-blox 에는
있는데 (2.2.4.3) CASIC 에는 아예 없다. 그래서 우리가 앱에서 만들었다.

### CFG-NAVX (0x06 0x07) 44바이트

```
 0  U4  mask          이 비트가 선 항목만 적용된다
 4  U1  dyModel       움직임 종류      ← 제일 중요하다
 5  U1  fixMode       측위 모드
 6  U1  minSVs        최소 위성 수
 7  U1  maxSVs        최대 위성 수
 8  U1  minCNO        최소 신호 세기 (dB-Hz)
10  U1  iniFix3D      첫 측위는 3D 여야 하나
11  I1  minElev       최소 고도각
12  U1  drLimit       위성 없이 버틸 시간 (초)
13  U1  navSystem     쓸 위성 종류
14  U2  wnRollOver    GPS 주차
16  R4  fixedAlt      2D 일 때 고정 고도
20  R4  fixedAltVar
24  R4  pDop          위치 DOP 한도
28  R4  tDop
32  R4  pAcc          위치 정확도 한도 (m²)
36  R4  tAcc
40  R4  staticHoldTh  정지로 볼 속도 문턱 (m/s)
```

### NAV-PV (0x01 0x03) 80바이트 — 여기에 다 있다

```
 4  U1  posValid    측위 표식
 5  U1  velValid    속도 표식     ← 5장을 볼 것
40  R4  hAcc        수평 위치 오차 (m²)
48  R4  velN        북쪽 속도 m/s
52  R4  velE        동쪽 속도 m/s
64  R4  speed2D     지면 속도 m/s
68  R4  heading     침로 °
72  R4  sAcc        속도 오차 ((m/s)²)
76  R4  cAcc        침로 오차 (°²)   ← 침로가 얼면 커진다
```

---

## 3. 움직임 종류 (dyModel) — 저속이 살고 죽는 자리

### 실측: 같은 저속에서 두 모드가 정반대다

```
위치로 잰 속도       세션 26 (모드 2 보행)   세션 27 (모드 0 휴대)
                   보드가 0 으로 적은 비율
 0.0 ~ 0.5 kts            0%   (23줄)         98%  (6,194줄)
 0.5 ~ 1.0 kts            0%  (112줄)         77%    (473줄)
 1.0 ~ 1.5 kts            0%  (103줄)          1%  (2,685줄)
 1.5 ~ 2.5 kts            0%  (265줄)          0%  (3,666줄)
```

**1노트 위는 둘 다 멀쩡하다. 아래에서만 갈린다.**

우리 문턱값 탓이 아니다. 파일에 적히는 값은 다듬기 전 도플러 원본이고
(`main.cpp` 의 `buildNav`), 문턱값 0.1노트는 화면·BLE 로 나가는 값에만
걸린다 (`sogOut`). 모듈의 `staticHoldTh` 도 0 으로 꺼져 있었다.

### 왜 그런가

u-blox 표 [확인: MAX-M10S 통합 매뉴얼 표 9·10].

```
                     최대 수평속도   위치 흔들림 허용
Portable  휴대          310 m/s        Medium
Stationary 정지          10 m/s        Small
Pedestrian 보행          30 m/s        Small
Automotive 자동차       100 m/s        Medium
At sea    선박           25 m/s        Medium
Wrist     손목           30 m/s        Medium
```

본문이 이렇게 말한다.

> 높은 가속을 가정한 모델을 쓰면 **보고되는 위치의 표준편차가 커진다.**

「흔들림 허용」이 넓다는 건 필터가 큰 움직임도 진짜로 받아들인다는 뜻이다.
그러면 잡음도 같이 받아들이고, **그 잡음 폭에 묻히는 느린 움직임은 0 이
된다.** 휴대는 310 m/s 를 대비한다. 초속 310미터를 대비하는 필터가
초속 0.15미터(0.3노트)를 가려내지 못한다.

### 휴대(Portable)는 언제 쓰나

문서의 설명은 한 줄이다.

> 낮은 가속도의 응용, 예를 들어 휴대 기기. **대부분의 상황에 적합.**

**이 한 줄이 함정이다.** 그 말만 보고 고르면 아래 표를 안 본다 — 우리가
그랬다. 뜻은 「어떤 움직임인지 모를 때 두루 쓰는 기본값」이지 「제일 좋은
값」이 아니다. 빠르고 갑작스러운 움직임을 놓치기 싫을 때 쓰는 것이고,
그 대가로 느린 움직임을 버린다. 정지 상태에서 덜 튀는 것도 아니다 —
흔들림 허용이 Medium 이라 오히려 더 튄다.

**우리 딩기는 0~15노트(0~8 m/s)다. 310 m/s 를 대비할 이유가 없다.**

### 그럼 뭘 쓸까 — 아직 안 끝났다

```
보행(2)   30 m/s(58노트) · 흔들림 Small    저속이 유일하게 확인된 모드
선박(4)   25 m/s(49노트) · 흔들림 Medium   이름은 맞는데 등급이 휴대와 같다
```

지금은 **4(선박)** 로 걸어 뒀다. 물 위에서 쓰는 사람들이 그걸 쓰기 때문이다.
그런데 위 표를 보면 선박도 흔들림 허용이 Medium 이라 **휴대와 같은 등급**이고,
저속에서 어떨지는 **아직 안 재봤다.**

→ 다음에 나가서 같은 자리에서 4 로 한 판, 2 로 한 판 찍어
  **0.2~0.6노트 구간을 견주면 끝난다.**

### 뒤집힌 기록 — 같은 실수를 세 번 했다

```
8/22  선박(4)에서 걸으면 0.  "선박이 원인" 으로 보고 0(휴대)로 바꾸고 「해결」.
      고친 뒤 걸어서 다시 재보지 않았다
8/27  0(휴대)인 채로 걸어도 여전히 0.  2(보행)로 바꾸니 561줄 전부 값이 나왔다
8/28  20초마다 선박↔보행을 번갈아 걸며 걸어 봤다. 둘 다 잘 나왔다
      → "모드가 원인이 아니었다" 고 적었다.  **그 시험에 0(휴대)이 없었다**
8/31  세션 24·25·27·28 이 전부 0(휴대)이었고 저속이 다 죽어 있었다.
      모드가 원인이 맞았다
```

**의심받는 값을 뺀 시험은 그 값을 풀어줄 수 없다.**

---

## 4. 저장 — "적어 뒀습니다" 를 믿지 말 것

모듈은 전원이 오르내리면 기본값으로 돌아간다. 그래서 보드가 NVS 에 적어
두고 부팅 때마다 다시 건다 (`gpsBegin` 안).

```
1) NVS 의 gps_dyn 을 읽는다 (255 면 정한 적 없음)
2) 모듈에 걸린 값과 다르면 다시 건다
3) 실제로 걸린 값을 기억한다 → 기록 파일 머리글에 들어간다
```

### 여기서 오래 속았다

`gpscfg mode` 가 NVS 에 적는 줄에 `gPrefs.begin()` 이 빠져 있었다.
**Preferences 는 닫힌 손잡이에 써도 아무 말 없이 실패한다.**

그래서 「보드에 적어 뒀습니다」라고 말해 놓고 안 적혔고, 모듈은 전원이
오르내릴 때마다 기본값 0(휴대)으로 돌아갔다. 8/28 에 「4 로 걸어 뒀다」고
적었는데 8/30 세션 27·28 이 둘 다 0 이었던 이유다.

고쳤다 (2026-08-31). 여닫고 쓰고, **되읽어서 진짜 적혔을 때만** 적었다고
말한다. 안 적혔으면 이렇게 나온다.

```
★ 보드에 못 적었습니다 (되읽으니 255). 껐다 켜면 돌아갑니다.
```

**부팅 로그가 헷갈린다.** 「다시 걸었습니다」면 NVS 가 살아 있는 것이고,
그냥 「움직임 종류 N」이면 NVS 가 비었거나 이미 같은 값이라는 뜻인데
두 경우가 같은 문장으로 나온다.

### 화면에도 띄운다

정해 둔 모드와 실제로 걸린 모드가 **다를 때만** OLED 의 SOG 옆에 낱말이
뜬다 (port/stat/ped/car/boat). 같으면 자리를 안 뺏는다. 물 위에서 알아챌
수 있는 유일한 자리다.

### `ab` 명령은 지웠다

두 모드를 10초마다 번갈아 걸어 견주는 명령이었는데 지웠다. 켜 두면 기록을
시작하기 직전 모드를 사람이 예측할 수 없다. 게다가 인자 없이 `ab` 만 쳐도
켜지는 명령이라 위험했다.

---

## 5. 저속에서는 옛 값을 들고 있다

배가 느리면 수신기가 속도·침로를 못 구한다. 그때 **값을 비우지 않고 마지막
값을 그대로 계속 내보낸다.** 그래서 파일에 「유효한 값」으로 들어온다.

```
세션 27 (29분)   COG 가 같은 값으로 굳어 있던 시간이 전체의 65%
그때 속도         COG 가 직전 줄과 달라진 비율
 1.0 ~ 1.5 kts          0%
 2.0 ~ 2.5 kts         25%
```

**문턱이 다르다 — 속도는 1노트, 침로는 2노트.**

u-blox 문서에 절 제목으로 있다.

```
2.2.6 Freezing the course over ground
  속도가 아주 낮으면(0.1 m/s 아래) 침로가 얼어붙는다.
  직전 값을 들고 있고 정확도만 시간이 갈수록 나빠진다
```

### 칩이 알려준다 — velValid 한 바이트

```
0    속도를 아예 모름
1    바깥에서 넣은 값
2    대충 어림잡은 값
3    마지막에 잰 값을 그대로 들고 있음   ← 지금 잰 게 아니다
4~8  방금 제대로 잰 값
```

우리 코드는 `!= 0` 으로 봤다. 그러면 3 번도 잰 값 취급이라 **없는 값을 있는
것처럼 적게 된다.** `>= 4` 로 고쳤다 (2026-08-31).

TXT 사본에 이렇게 남는다.

```
2.31kn  pv 2.28  pos 2.19  f7  c  2    넷이 맞으면 진짜다
0.00kn  pv ---   pos 0.31  f3  c142    도플러 0, 칩은 "옛날 값", 위치로는 0.31노트
```

```
앞의 kn  도플러(RMC).  바이너리 파일에 들어가는 값
pv       NAV-PV.  표식이 4 이상일 때만 적는다
pos      위치 차분
f        속도 표식 (velValid)
c        침로 오차 (cAcc).  얼면 커진다
```

### 위치로 계산하는 건 칩이 안 해준다

```
u-blox   침로만. 설정으로 켜야 하고 기본은 꺼져 있다 (2.2.4.3).
         속도에는 그런 항목이 없다
CASIC    설정 명령이 일곱 개뿐이고 그런 항목이 아예 없다
L76K     저속 처리 이야기가 문서에 없다
```

그래서 앱에서 만들었다. 위치 두 점으로 구하고 창은 5초다. 세션 27 로 골랐다.

```
창     정박 때 흔들림   도플러와 차이 (1.5kn 위, 중앙값)
 2초      0.15 kn            0.19 kn
 5초      0.16 kn            0.13 kn     ← 제일 잘 맞는다
10초      0.17 kn            0.15 kn
30초      0.22 kn            0.27 kn
60초      0.29 kn            0.51 kn
```

길게 잡을수록 나빠진다. 배가 돌면 두 점을 잇는 직선이 실제로 간 거리보다
짧아진다.

**도플러를 대신하는 값이 아니다.** 흔들림이 0.16 kn 이라 0.2 kn 대를 겨우
가른다. 도플러가 0 일 때 견주는 값이다. 앱에서 SOG·COG 줄의 `cal` 단추로
갈아 끼워 본다.

---

## 6. 속도는 도플러로 얻는다

**위치 차분은 안 쓴다** (앱의 `cal` 은 견주기용이다). 실측에서 걷는 동안
위치 차분 쪽에 30.31 kn 이 찍혔다. 같은 구간 도플러는 7.98 kn 이었다.
위치 차분은 초당 몇 m, 도플러는 초당 몇 cm 수준이다.

**정지 중 0.1 kn 은 고장이 아니다.** 0.1 kn = 초당 5 cm 이고 도플러의 이론
잡음이 딱 그 수준이다. Velocitek ProStart V2 의 사양도 ±0.1 kn 이다.

파일에는 **다듬기 전 원본**이 들어간다. 머리글의 `sog_src` 가 늘 0 이어야
한다. 다듬기와 문턱값은 화면·BLE 로 나가는 값에만 건다 (`smooth`, `dead`).

---

## 7. 아직 모르는 것

```
선박(4)이 1노트 아래에서 어떤가        저속 정상 기록은 보행(2) 뿐이다
보행(2)이 요트 속도를 안 뭉개나        최대 30 m/s 라 넉넉하지만 "낮은 가속도
                                    가정" 이 딩기 활주에 걸릴 수 있다
PCAS11 과 CFG-NAVX 가 같은 값을 건드리나  우리는 바이너리만 쓴다.
                                    따로 있는 값이면 서로 싸울 수 있다
PPS 가 위성을 잡은 뒤 1초마다 나오나    실내에서 선이 연결된 것까지만 봤다.
                                    밖에서 `pin 21` 한 번이면 끝난다
```
